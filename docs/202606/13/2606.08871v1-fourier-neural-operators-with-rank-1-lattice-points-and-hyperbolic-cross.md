---
title: Fourier Neural Operators with rank-1 lattice points and hyperbolic cross
title_zh: 基于秩1格点和双曲交叉的傅里叶神经算子
authors: "Jakob Dilen, Alexander Keller, Frances Y. Kuo, Dirk Nuyens"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08871v1"
tags: ["query:flow-field"]
score: 9.0
evidence: 改进的傅里叶神经算子用于函数空间学习
tldr: 该论文针对傅里叶神经算子（FNO）提出改进，通过引入秩一格点和双曲交叉频率索引集，替代传统张量积网格和二维FFT。理论证明了在空间和参数变量上的正则性边界，表明该方法能减少网络参数、空间点和训练样本，同时提高逼近精度和效率。在环面椭圆偏微分方程上的实验验证了其优势。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08871v1/fig-001.webp\", \"caption\": \"Figure 1: Modes h ∈ A of the both the box index set (a) and the hyperbolic cross index set (b) for M = 10.\", \"page\": 6, \"index\": 1, \"width\": 800, \"height\": 369}, {\"url\": \"assets/figures/arxiv/2606.08871v1/fig-006.webp\", \"caption\": \"Figure 3: Real and imaginary parts of the forcing term f(x).\", \"page\": 20, \"index\": 6, \"width\": 808, \"height\": 390}, {\"url\": \"assets/figures/arxiv/2606.08871v1/fig-002.webp\", \"caption\": \"Figure 4: Real and imaginary parts of a single realization y∗ ∈ Y of an affine random input field a(x,y∗) and the corresponding output field u(x,y∗).\", \"page\": 21, \"index\": 2, \"width\": 808, \"height\": 812}, {\"url\": \"assets/figures/arxiv/2606.08871v1/fig-004.webp\", \"caption\": \"Table 1: Approximate generalization error with total and relative (to the regular FNO) number of parameters for the models described in Section 4.4.\", \"page\": 23, \"index\": 4, \"width\": 861, \"height\": 227}, {\"url\": \"assets/figures/arxiv/2606.08871v1/fig-003.webp\", \"caption\": \"Figure 5: Real and imaginary parts of output fields generated by FNO-HC-LAT for the realization of the affine input field a(x,y∗) from Figure 4.\", \"page\": 23, \"index\": 3, \"width\": 808, \"height\": 393}, {\"url\": \"assets/figures/arxiv/2606.08871v1/fig-005.webp\", \"caption\": \"Figure 6: Training time and approximate generalization error comparison between the regular FNO and the FNO-HC-LAT when training on CPU.\", \"page\": 24, \"index\": 5, \"width\": 932, \"height\": 345}]"
motivation: 传统FNO使用张量积网格导致计算复杂度高，且泛化误差受限于网格点分布，需要更高效的采样和频率选择策略。
method: 采用秩一格点进行空间离散和参数训练点采样，并利用双曲交叉频率索引集，将高维FFT简化为一维FFT。
result: 实现了更少的网络参数、空间点和训练样本，同时提高了逼近精度和计算效率。
conclusion: 基于格点的双曲交叉FNO在环面椭圆PDE问题上优于传统FNO架构。
---

## 摘要
傅里叶神经算子（FNO）是一种学习函数空间之间映射的神经网络架构。其高效实现基于多维傅里叶变换。通过推导FNO关于空间变量和参数变量的通用正则性界，我们证明，用专门构造的秩1格点替代空间张量积网格，并用精心构造的第二个格点作为参数空间中的训练点，可以改善FNO的泛化误差。我们以更少的网络参数、更少的空间点和更少的训练样本实现了更精确和高效的逼近。此外，架构得到了简化，因为秩1格点上的高维傅里叶变换仅需一维快速傅里叶变换，并且我们可以使用带有格点的双曲交叉频率指标集。我们展示了基于格点的双曲交叉FNO在环面上的椭圆型偏微分方程上的优势。

## Abstract
The \emph{Fourier neural operator} (FNO) is a neural network architecture that learns mappings between function spaces. Its efficient implementation is based on the multi-dimensional Fourier transform. By deriving general regularity bounds for the FNO with respect to both the spatial and parametric variables, we prove that the generalization error of the FNO can be improved by replacing spatial tensor product grids with purpose-built rank-1 lattice points, and by using a second lattice carefully constructed as training points in the parametric space. We achieve more accurate and efficient approximations from fewer network parameters, fewer spatial points, and fewer training samples. In addition, the architecture is simplified, because the high-dimensional Fourier transform on rank-1 lattices requires only a \emph{one-dimensional fast Fourier transform}, and we can use a \emph{hyperbolic cross} frequency index set with lattice points. We demonstrate the benefits of our \emph{lattice-based hyperbolic-cross FNOs} for an elliptic PDE on the torus.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）

- **背景**：傅里叶神经算子（FNO）是一种学习函数空间之间映射的深度学习架构，其核心是基于多维快速傅里叶变换（FFT）在频域实现卷积。传统FNO在空间域使用张量积型均匀网格，在高维（$d \ge 3$）时网格点数随维度指数增长，导致计算成本极高。同时，训练数据的参数采样通常采用简单随机或伪随机，泛化误差的收敛速度较慢。
- **问题**：如何减少FNO的空间点数和训练样本数，降低网络参数规模，同时提高逼近精度和泛化能力？
- **核心贡献**：本文将拟蒙特卡洛（QMC）方法中的秩一（rank-1）格点引入FNO的两个前端：
    1. 用秩一格点替代空间张量积网格，将高维FFT降为一维FFT，大幅减少空间点数；
    2. 用另一个精心构造的秩一格点作为参数域的训练点，提高泛化误差的收敛率；
    3. 同时在频域采用双曲交叉（hyperbolic cross）索引集替代传统盒子索引集，进一步减少所需频率模式数。
    理论研究推导了FNO关于空间变量和参数变量的正则性界，结合格点积分理论给出了泛化误差的收敛速率。数值实验在环面椭圆PDE上验证了方法的有效性。

### 2. 方法论

- **FNO结构**：形如 $G_\theta(a)(x) = (Q \circ T_L \circ \cdots \circ T_1 \circ P(a))(x)$，其中 $T_\ell(v) = \sigma(W_\ell v + K_\ell v + b_\ell)$，$K_\ell$ 为卷积核算子。频域截断至有限索引集 $\mathcal{A}$（原为盒子，现双曲交叉）。
- **空间离散**：使用秩一格点 $\{x_k\}_{k=1}^m$，定义为 $x_k = \frac{k z \bmod m}{m}$，其中 $z \in \mathbb{Z}^d$ 为生成向量。此时傅里叶系数的计算可通过一维FFT完成（$O(m \log m)$），且空间点数 $m$ 可自由选择（需保证 $|\mathcal{A}| < m$）。
- **参数域训练点**：同样使用秩一格点（可随机移位）$\{y_i\}_{i=1}^n$，$y_i = \frac{i w}{n} + \Delta \bmod 1$，$w \in \mathbb{Z}^s$ 为另一生成向量。
- **随机场模型**：输入场 $a(x,y)$ 采用仿射模型 $a(x,y) = \psi_0(x) + \sum_{j}(y_j - \frac12)\psi_j(x)$ 或 Chebyshev 模型 $a(x,y) = \psi_0(x) + \sum_j \sin(2\pi y_j)\psi_j(x)$。基函数 $\psi_j$ 基于频率索引的衰减函数构造，使得 $a(x,y)$ 具有良好正则性。
- **正则性分析**：
    - 对于参数变量 $y$，定理1给出FNO的混合偏导数的上界（形式类似于乘积和阶数依赖型（POD）或光滑驱动型（SPOD））。结合目标函数的类似正则性假设，定理2表明在适当构造的格点权重下，由参数空间积分误差引起的泛化误差部分可达到 $O(n^{-r})$ 收敛，其中 $r$ 依赖于随机场的可求和性指数 $p_*$。
    - 对于空间变量 $x$，定理3给出空间混合偏导数的上界（含 $|ν|!$ 因子）。定理4在假设目标函数具有类似正则性下，得到空间积分误差部分收敛率为 $O(m^{-(\omega-\epsilon)})$，其中 $\omega$ 为Korobov空间光滑度。
- **组合泛化误差界**（定理5）：全局泛化误差 $E_G \le \text{训练误差} + O(n^{-r/2}) + O(m^{-(\omega-\epsilon)/2})$，从而保证了理论上的加速收敛。

### 3. 实验设计

- **问题**：环面椭圆PDE（无边界）
  $$-\nabla \cdot (a(x,y) \nabla u) = f(x),\quad \int_D u = 0,$$
  定义在 $D=[0,1]^2$ 上。输入场为仿射随机场，基函数按双曲交叉频率衰减构造（$\omega=1, \rho_u=1$），取 $M=9$ 得到 $|\mathcal{A}_{hc}|=33$。
- **数据集生成**：高精度真解通过截断至盒子索引集（$M=25$）求解频域线性系统获得。训练集使用 $n=1024$ 个参数格点样本，空间格点 $m=1024$。泛化误差评估使用 $n^*=4096$，$m^*=4096$。
- **基准方法**（四种对比模型）：
    - **FNO**：常规盒子频段（$M=9$，$|\mathcal{A}_{box}|=361$） + 张量积网格（$32\times32$）。
    - **FNO-LAT**：盒子频段 + 秩一格点（$m=1024$）。
    - **FNO-HC**：双曲交叉频段（$M=9$，$\omega=1$） + 张量积网格。
    - **FNO-HC-LAT**：双曲交叉频段 + 秩一格点。
    - 另设**参数匹配对比**： $M=3$ 的 FNO（$|\mathcal{A}_{box}|=49$） vs. FNO-HC-LAT（$M=9$）使参数量接近。
- **训练配置**：Adam优化器，学习率自适应衰减（factor 0.8，patience 50），初始学习率1e-2，batch size 16，训练2000 epochs。所有模型使用2个隐藏层，每层16通道，线性升降算子 $P,Q$。使用NeuralOperator库实现常规FNO，格点版本基于GitHub代码。

### 4. 资源与算力

- **GPU实验**（4.4-4.5节）：使用单块 NVIDIA GeForce P100，训练2000 epochs。具体显卡数量和总耗时未提及，但表1给出各模型平均训练时间（秒）：FNO ~7092s，FNO-LAT ~6669s，FNO-HC ~4039s，FNO-HC-LAT ~3838s。
- **CPU时间分析**（4.6节）：为了更好地暴露参数数对训练时间的影响，在CPU上运行，结果如图6(a)所示，FNO-HC-LAT参数更少且训练速度更快。

### 5. 实验数量与充分性

- **实验组数**：
    - 表1：四种对比，每种独立运行5次，报告均值与标准差。
    - 表2：参数匹配对比，同样5次独立运行。
    - 图6(a)(b)：绘制不同 $M$ 值下四种变体的训练时间和泛化误差。
- **分析**：
    - 实验覆盖了核心变量（频段类型、空间离散类型），且各有多次重复，统计合理。
    - **对比**：未与DeepONet、PCA-Net等其他算子学习架构比较；仅针对FNO家族内部分析。
    - **充分性**：消融设计清晰（有无双曲交叉、有无格点），但仅测试了二维椭圆PDE，未扩展至更高维（$d\ge3$）或其他类型PDE（如Navier-Stokes），也未改变随机场模型（仅仿射）。
    - **超参数**：$M, \omega, \eta, L, d_\ell$ 等固定，缺乏敏感性分析。

### 6. 主要结论与发现

- **参数效率**：FNO-HC-LAT 使用 $M=9$ 双曲交叉索引集，参数量仅为常规FNO的9.44%（35010 vs. 370850），而泛化误差几乎不变（约2.93e-2 vs. 2.74e-2）。
- **计算效率**：训练时间减少约46%（3838s vs. 7092s）；时间优势随 $M$ 增大而更明显（图6(a)）。
- **匹配参数数**：当参数量相近时，FNO-HC-LAT（$M=9$）与FNO（$M=3$）精度相当（2.85e-2 vs. 2.88e-2），说明双曲交叉+格点能更有效地利用参数。
- **泛化误差界**：理论结果（定理5）在实际中得到了符合预期的表现，虽然直接收敛率验证未做，但数值精度接近。

### 7. 优点

- **理论驱动**：首次为FNO建立了关于空间变量和参数变量的混合正则性界，并利用格点积分理论给出了泛化误差的严格收敛速率。
- **架构简化**：秩一格点使高维FFT转化为一维FFT，无需多维张量网格，简化了实现。
- **参数节约**：双曲交叉索引集自然匹配混合正则函数的频谱衰减，大幅减少频域参数。
- **实验可复现**：代码开源（GitHub），详细列出了超参数和训练流程。
- **消融完整**：四种变体两两组合，清晰展示了各成分贡献。

### 8. 不足与局限

- **实验维度限制**：仅测试 $d=2$ 物理域，未在 $d=3$ 或更高维上验证，而格点的优势在高维更显著。
- **问题类型单一**：仅考察椭圆PDE，未涉及非线性或时间演化问题（如反应扩散、流体动力学）。
- **随机场假设**：需要基函数按频率衰减率构造（公式(49)），且要求 $a(x,y)$ 下界有正性，限制了应用范围。
- **理论条件**：周期性边界和加权Korobov空间假设，将物理域视为环面，排除了常见Dirichlet/Neumann边界条件。
- **超参数固定**：$M=9, \omega=1$ 等取为常数，未讨论如何自动选取最优值；学习策略（如自适应学习率）仅为一种选择，未与其他训练策略比较。
- **与SOTA对比缺失**：未与DeepONet、PCA-Net、SA-FNO等当前主流算子学习方法进行横向比较，难以评估本文方法的绝对优势。
- **计算资源细节不完整**：未说明使用的GPU数量、总GPU小时或是否并行训练，不利于他人估计复现成本。

（完）

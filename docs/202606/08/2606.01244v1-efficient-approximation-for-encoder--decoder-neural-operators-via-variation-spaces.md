---
title: Efficient Approximation for Encoder--Decoder Neural Operators via Variation Spaces
title_zh: 通过变分空间实现编码器-解码器神经算子的高效逼近
authors: "Jia-Qi Yang, Lei Shi"
date: 2026-05-31
pdf: "https://arxiv.org/pdf/2606.01244v1"
tags: ["query:flow-field"]
score: 9.0
evidence: 编码器-解码器神经算子逼近理论
tldr: "本文研究编码器-解码器神经网络的算子学习，引入基于向量值测度的变分空间作为非线性算子的无限维结构类。在该空间下，建立了Bochner L^q范数下的逼近误差界，分解为输入编码误差、输出编码误差和N^{-1/2}阶有限宽度项，且常数与编码维度无关。当编码误差多项式衰减时，得到代数逼近和学习率，为超越Lipschitz/Fréchet类的算子学习提供了理论保证。"
source: arxiv
selection_source: fresh_fetch
motivation: 现有算子学习方法缺乏对高效逼近的严格理论保证，尤其是针对非线性算子的通用类。
method: 引入变分空间作为无穷维结构类，基于向量值测度定义算子，并推导编码器-解码器双层网络的逼近误差界。
result: "误差界分解为输入/输出编码误差和N^{-1/2}阶宽度项，常数与编码维度无关；编码误差多项式衰减时获得代数逼近率。"
conclusion: 为非线性算子学习提供了超越经典Lipschitz/Fréchet类的理论保证，验证了编码器-解码器网络的高效性。
---

## 摘要
我们研究使用编码器-解码器神经网络进行算子学习。受神经网络函数空间理论的启发，我们引入了一个变分空间作为非线性算子的无限维结构类。该空间通过直接在输入和输出空间上的向量值测度来定义。对于该空间中的算子，我们建立了编码器-解码器双层网络在Bochner $L^q$范数下的逼近界。得到的误差界分解为输入编码误差、输出编码误差以及阶为$N^{-1/2}$的有限宽度逼近项，且常数与输入和输出编码维度无关。当输入和输出编码误差在编码维度上多项式衰减时，这些估计产生了代数逼近和学习速率。这些结果为超越一般Lipschitz或Fréchet可微算子类的高效神经算子学习提供了理论保证。

## Abstract
We study operator learning using encoder--decoder neural networks. Inspired by the function-space theory of neural networks, we introduce a variation space as an infinite-dimensional structural class for nonlinear operators. This space is defined through vector-valued measures directly on the input and output spaces. For operators in this space, we establish approximation bounds for encoder--decoder two-layer networks in the Bochner $L^q$ norm. The resulting error bound decomposes into the input encoding error, the output encoding error, and a finite-width approximation term of order $N^{-1/2}$, with a constant independent of the input and output encoding dimensions. When the input and output encoding errors decay polynomially in the encoding dimensions, these estimates yield algebraic approximation and learning rates. The results provide an theoretical guarantees for efficient neural operator learning beyond general Lipschitz or Fréchet differentiable operator classes.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：算子学习旨在学习将无限维函数空间映射到无限维函数空间的算子（例如 PDE 的解算子）。现有神经网络架构（如 DeepONet、FNO、PCA‑Net）虽已实现通用逼近，但对一般 Lipschitz 或 Fréchet 可微算子类，存在“维度灾难”现象，即所需的参数数量或样本复杂度随精度要求呈指数增长。因此，需要寻找具有更好逼近效率的算子子类，使得神经网络能够实现多项式或代数的逼近/学习速率。
- **背景**：对于有限维空间中的函数逼近，变分空间（variation space）和 Barron 空间已被证明可实现避免维度灾难的逼近率。本文将这一思想推广到无限维算子学习场景，为编码器-解码器神经算子提供严格的理论保证。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：定义一种新的算子变分空间 $\mathcal{V}(U;V)$，其中算子可表示为对向量值测度的积分：
  $$G(u) = \int_{B_{\tilde{U}}} \sigma(\langle \bar{u}, \bar{h}\rangle_{\tilde{U}}) \, d\Lambda(\bar{h}),$$
  其中 $\bar{u}=(1,u)$，$\tilde{U}=\mathbb{R}\oplus U$，$\Lambda$ 是取值于输出空间 $V$ 的有限 Borel 测度。变分范数定义为表示中测度的总变分的最小值。该空间不依赖于特定的正交基或编码维度，直接定义在 Hilbert 空间 $U,V$ 上。
- **关键技术细节**：
  - 使用正交基 $\Phi=\{\phi_i\}$ 和 $\Psi=\{\psi_j\}$ 构造编码器 $E_U^m$ 和解码器 $D_V^n$，形成编码器-解码器架构 $G = D_V^n \circ g \circ E_U^m$。
  - 中间映射 $g: \mathbb{R}^m \to \mathbb{R}^n$ 采用两层神经网络，其形式为 $g(x) = \sum_{k=1}^N b_k \sigma(\alpha_k + a_k\cdot x)$，满足 $\alpha_k^2 + |a_k|^2 \leq 1$ 且 $\sum_{k=1}^N |b_k| \leq B$。
  - 主要定理 (Theorem 3.1)：对于变分空间中的任意算子 $G^\dagger$，存在这样的 $g_{m,n,N}$ 使得误差满足：
    $$\begin{aligned}
    \|G^\dagger - D_V^n \circ g_{m,n,N} \circ E_U^m\|_{L^q_\rho(V)} \leq\ &\underbrace{\|(I-P_V^n)G^\dagger\|_{L^q_\rho(V)}}_{\text{输出编码误差}} \\
    &+ \Big[ L_\sigma \underbrace{\|I-P_U^m\|_{L^q_\rho(U)}}_{\text{输入编码误差}} + C_q \frac{|\sigma(0)| + L_\sigma \bar{M}_q}{\sqrt{N}} \Big] \|G^\dagger\|_{\mathcal{V}(U;V)}.
    \end{aligned}$$
    其中 $C_q$ 仅依赖于 $q$，常数与编码维度 $m,n$ 无关。
  - 证明思路：将算子表示为积分，用概率测度化后采样构造有限宽度网络，并利用 Hilbert 空间中的 Marcinkiewicz–Zygmund 不等式控制采样误差。
- **算法流程**（文字说明）：给定目标算子 $G^\dagger$，先选正交基并固定编码维度 $m,n$；然后根据变分范数 $\|G^\dagger\|_{\mathcal{V}}$ 确定宽度 $N$；通过从表示测度中随机采样获得神经元的参数（权重、偏置和输出层权重）；最后将所得网络与编码器-解码器组合。

### 3. 实验设计

- **实验情况**：论文为纯理论工作，不包含任何数值实验。未使用任何数据集、基准方法或对比实验。

### 4. 资源与算力

- 文中未提及 GPU、训练时长等算力资源。作为理论论文，无需此类信息。

### 5. 实验数量与充分性

- 不适用。论文无实验，充分性在于数学证明的严谨性。

### 6. 论文的主要结论与发现

- 对于变分空间中的算子，编码器-解码器双层神经网络的逼近误差可分解为三项：输入编码误差、输出编码误差和 $N^{-1/2}$ 阶的有限宽度项。
- 常数 $C_q$ 不依赖于编码维度 $m,n$，仅依赖于 $q$（Bochner 空间指数）和激活函数的 Lipschitz 常数及输入分布的高阶矩。
- 当输入/输出编码误差随编码维度 $m,n$ 呈多项式衰减时（例如使用傅里叶基或 PCA 基时，对于足够光滑的测度），整体逼近误差可实现代数逼近速率。
- 该结果提供了超越 Lipschitz 和 Fréchet 可微算子类的“免于维度灾难”的理论保证，验证了编码器-解码器架构对良好结构算子的高效性。

### 7. 优点：方法或实验设计上的亮点

- **理论创新**：首次将变分空间概念系统性地推广到无限维算子学习，定义了适用于算子的向量值测度表示，避免了前人工作中对输出空间的 Riesz 空间结构和弱拓扑连续性的苛刻要求。
- **紧致误差分解**：将逼近误差明确分解为数据逼近误差（输入/输出投影残差）和函数逼近误差（有限宽度网络近似），常数与编码维度解耦，便于分析算子学习随维度缩放的行为。
- **广泛的适用性**：方法适用于任意可分的 Hilbert 空间 $U,V$，只要存在正交基；编码器/解码器可采用傅里叶、小波、多项式或 PCA 基，具有灵活性。
- **证明技巧**：使用了 Hilbert 空间中的 Marcinkiewicz–Zygmund 不等式和 Kahane–Khintchine 不等式，揭示了两层网络 Monte Carlo 采样的本质。

### 8. 不足与局限

- **纯理论无实验验证**：论文未提供任何数值例子，无法验证理论界在实际问题中的尖锐性及与现有方法（如 FNO、DeepONet）的比较。
- **假设较强**：要求输入分布 $\rho$ 具有有限 $q$ 阶矩；变分范数假设算子具有测度表示，对于非光滑或高度振荡的算子可能不成立；激活函数需为 Lipschitz 函数，不含可微分性或特殊结构性假设。
- **实际适用性问题**：宽度 $N$ 的界依赖于变分范数，但变分范数在实际中难以计算或估计；误差界中的常数 $C_q$ 可能很大，影响实际应用效果。
- **仅考虑两层网络**：虽然两层网络的变分表示是 classic，但实际神经算子往往采用更深层的架构，本文结果不能直接解释深层网络的性能。
- **未考虑泛化误差**：仅讨论逼近误差（即表达能力），未涉及基于有限样本的泛化误差界，尽管文中提到“generalization bounds”是未来方向之一。

（完）

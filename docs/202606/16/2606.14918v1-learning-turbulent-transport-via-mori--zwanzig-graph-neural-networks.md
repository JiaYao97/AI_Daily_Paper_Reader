---
title: Learning turbulent transport via Mori--Zwanzig graph neural networks
title_zh: 基于Mori–Zwanzig图神经网络的湍流输运学习
authors: "André Freitas, Xander M. de Wit, Alessandro Gabbana, Michael Woodward, Federico Toschi, Yen Ting Lin, Daniel Livescu"
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14918v1"
tags: ["query:pclso"]
score: 9.0
evidence: 用于降阶拉格朗日湍流建模的Mori-Zwanzig图神经网络，保持物理对称性
tldr: 针对湍流示踪粒子传输，提出Mori-Zwanzig图神经网络（MZ-GNN），将加速度建模为邻域图的延迟记忆展开，通过等变消息传递网络参数化。训练后自回归生成轨迹，证明记忆对恢复间歇性重尾加速度统计至关重要，并准确再现单/对粒子弥散及四粒子几何。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.14918v1/fig-001.webp\", \"caption\": \"FIG. 1. Problem setup. Turbulent transport. A puff of passive tracers in homogeneous isotropic turbulence, dispersed throughout the turbulent flow across increasingly larger scales. The surrounding panels summarize key challenges for reducedorder Lagrangian modelling: intermittency, evidenced by the increasing non-Gaussianity of temporal velocity-increment probability density functions (PDFs) at decreasing time lags; rare extreme events in pair dispersion, where some particle pairs separate much faster than the median; superdiffusive separation; and non-trivial multi-particle correlations, illustrated by the deformation of a tetrad. Particle trajectories. Representative Lagrangian trajectories coloured by acceleration magnitude comparing Eulerian–Lagrangian direct numerical simulation (DNS) ground truth with autoregressive graph neural network (GNN) rollouts, illustrating the complexity of tracer dynamics. Method. Schematic of the Mori–Zwanzig graph neural network (MZ–GNN). Particles are represented as nodes of a graph, with edges encoding local particle neighborhoods defined by a cutoff radius. The highlighted red edges illustrate, for a given particle, the neighbours contained within the corresponding spherical interaction region. The particle acceleration is modelled through a finite-memory expansion over present and delayed graph states, an i = ∑K k=0 Ω (k) θ ( G n−ks,v n−ks ) i . The k = 0 contribution is Markovian, whereas the k > 0 terms encode non-Markovian memory effects. Each operator Ω (k) θ is parameterized by an equivariant message-passing graph neural network.\", \"page\": 2, \"index\": 1, \"width\": 1059, \"height\": 1061}, {\"url\": \"assets/figures/arxiv/2606.14918v1/fig-002.webp\", \"caption\": \"TABLE I: Main training and architecture hyperparameters.\", \"page\": 12, \"index\": 2, \"width\": 421, \"height\": 338}, {\"url\": \"assets/figures/arxiv/2606.14918v1/fig-003.webp\", \"caption\": \"FIG. 4. Effective spatial receptive field estimated as ℓeff/L0 ∼ αNMP, with α = rc/L0. The dash-dotted and dashed lines indicate ℓeff = L0 and ℓeff = Lbox/2, respectively. The star marks the configuration used in this work, rc = L0/3 and NMP = 5.\", \"page\": 16, \"index\": 3, \"width\": 502, \"height\": 331}]"
motivation: 现有模型难以同时捕捉多粒子系统的对称性、相关性和长时间统计，需要一种物理结构化、可扩展的降阶方法。
method: 基于Mori-Zwanzig投影算子，将粒子加速度分解为马尔可夫项和延迟记忆项，每项由等变图神经网络参数化，并通过时间差分数据训练。
result: 模型在保持置换、伽利略及旋转反射等变性的前提下，成功复现未训练的单粒子弥散、对弥散和四粒子四面体统计，且记忆项是恢复重尾分布的关键。
conclusion: MZ-GNN为数据驱动多粒子湍流模拟提供了物理一致的框架，可作为学习相关对称粒子系统降阶动力学的通用模板。
---

## 摘要
我们提出了一种Mori–Zwanzig图神经网络（MZ–GNN）框架，用于学习各向同性均匀湍流中示踪粒子的降阶拉格朗日动力学。该模型将粒子加速度表示为当前和延迟粒子邻域图上的有限记忆展开，每个记忆贡献由一个等变消息传递图神经网络参数化。通过构造，该架构尊重问题的相关物理对称性，包括置换等变性、伽利略不变性以及旋转和反射下的等变性。在直接数值模拟数据上训练后，模型以自回归方式展开，并在训练中未强加的观测量上进行评估。我们表明，记忆对于恢复间歇性、重尾的加速度统计至关重要，并且学习到的动力学准确地再现了单粒子弥散、对弥散统计和四粒子四面体几何。我们的结果建立了一条物理结构化、可扩展的数据驱动多粒子湍流输运模拟路径，以及学习相关且富含对称性的粒子系统降阶动力学的模板。

## Abstract
We introduce a Mori--Zwanzig graph neural network (MZ--GNN) framework for learning reduced-order Lagrangian dynamics of tracer particles in homogeneous isotropic turbulence. The model represents particle acceleration as a finite-memory expansion over present and delayed particle-neighborhood graphs, with each memory contribution parameterized by an equivariant message-passing graph neural network. By construction, the architecture respects the relevant physical symmetries of the problem, including permutation equivariance, Galilean invariance, and equivariance under rotations and reflections. Trained on direct numerical simulation data, the model is rolled out autoregressively and evaluated on observables that are not imposed during training. We show that memory is essential for recovering the intermittent, heavy-tailed acceleration statistics, and that the learned dynamics accurately reproduce single-particle dispersion, pair-dispersion statistics, and four-particle tetrad geometry. Our results establish a physically structured, scalable route to data-driven multi-particle simulation of turbulent transport, and a template for learning reduced dynamics of correlated, symmetry-rich particle systems.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究动机**：湍流中示踪粒子的多粒子输运是一个典型的降阶建模（ROM）问题。由于湍流本质上是多尺度、混沌且间歇的，直接数值模拟（DNS）的计算成本随雷诺数快速增长（$N_{\text{DOF}} \sim \text{Re}^{9/4}$），亟需一种能够在保持物理对称性和多粒子空间相关性的同时，实现自回归预测的降阶替代模型。现有方法（如生成式模型）只能合成轨迹而非显式动力学，单粒子Mori–Zwanzig模型缺乏空间信息，而传统的随机模型难以捕捉间歇性。
- **整体含义**：本文提出的Mori–Zwanzig图神经网络（MZ–GNN）首次将数据驱动MZ形式与等变图神经网络结合，构建了一个显式的自回归动力学系统，能够同时学习时间记忆和粒子间空间相关性，从而恢复拉格朗日湍流的关键统计特征（如重尾加速度、超扩散对弥散、四面体变形），为多粒子系统降阶模拟提供了一个物理结构化、可扩展的模板。

### 2. 论文提出的方法论
- **核心思想**：将粒子加速度分解为马尔可夫项和有限项非马尔可夫记忆展开，每一项作用于当前或延迟的粒子邻域图，并通过等变消息传递图神经网络（GNN）参数化。通过构造，模型自动满足置换等变、平移不变、伽利略不变和旋转/反射等变（$O(3)$等变）。训练采用残差顺序回归方式：先训练马尔可夫项，然后依次冻结已训练项，只优化当前记忆项，使其学习残差加速度。
- **关键技术细节**：
  - **Mori–Zwanzig离散化**：加速度形式为
    $$
    a_i^n = \sum_{k=0}^{K} \Omega^{(k)}_\theta\left( G^{n-ks}, v^{n-ks} \right)_i,
    $$
    其中 $k=0$ 为马尔可夫项，$k>0$ 为记忆项，$s$ 为记忆步长，$G$ 为基于粒子位置的周期图，$v$ 为速度。
  - **图构建**：每个时间步根据截止半径 $r_c = L_0/3$（$L_0$ 为纵向积分尺度）构建周期邻域图，采用最小镜像约定。
  - **等变消息传递**：节点特征初始为零，经 $N_{\text{MP}}=5$ 层消息传递更新。每层使用相对位移 $r_{ij}$、相对速度 $v_{ij}$ 和相对隐含特征 $h_{ij}^{(\ell)}$ 的标量不变量（如范数、点积）作为MLP输入，输出系数 $\alpha,\beta,\gamma$，进而组合成等变向量消息：
    $$
    m_{ij}^{(\ell)} = \alpha_{ij}^{(\ell)} r_{ij} + \beta_{ij}^{(\ell)} v_{ij} + \gamma_{ij}^{(\ell)} h_{ij}^{(\ell)},
    $$
    然后平均聚合更新节点特征，最终读取出加速度贡献。
  - **训练流程**：在短时间窗口（滚动步长 $R=5$）内进行自回归展开，损失为预测与DNS加速度的均方误差。学习率 $5\times10^{-4}$，优化器Adam，批次粒子数8000。
- **公式与算法**：使用Euler推进粒子状态：
  $$
  v_i^{n+1} = v_i^n + \Delta t\, a_i^n,\quad x_i^{n+1} = x_i^n + \Delta t\, v_i^n.
  $$

### 3. 实验设计
- **数据集**：DNS数据来自256³网格的各向同性均匀湍流（Reλ ≈ 87，Kolmogorov尺度 $\eta$，Kolmogorov时间尺度 $\tau_\eta$），共约3.2×10⁴个示踪粒子，存储步长2.5×10⁴，总时长250 $\tau_\eta$。时间步长 $\Delta t = 0.01 \tau_\eta$。80%轨迹用于训练，20%用于测试。
- **基准（Benchmark）**：DNS地面真值作为唯一对比标准。模型逐项评估不同记忆深度（$K=0,3,5$），未与其他降阶模型（如随机模型、生成式模型）进行数值对比。
- **对比方法**：主要对比了不同记忆深度的MZ–GNN模型（$K=0$马尔可夫、$K=3$、$K=5$），消融实验考察记忆对加速度重尾和对分散布平坦度的影响。
- **评估观测量**：所有测试观测量均未出现在训练损失中，包括：单粒子均方位移、加速度PDF；对粒子均方分离、分离PDF、纵向/横向相对速度PDF；四粒子四面体的回转张量特征值和形状指数。

### 4. 资源与算力
- **未明确说明**：文中给出了主要超参数（见表1），包括隐含维度256、MLP层数10、总参数量约10⁷、批次粒子数8000、滚动步长5、学习率5×10⁻⁴。但未提及GPU型号、数量、分布式策略及总训练时长。仅在致谢中提到使用了荷兰Snellius超级计算机，但未给出具体算力细节。

### 5. 实验数量与充分性
- **实验数量**：针对不同记忆深度（$K=0,3,5$）进行训练和评估，并展示了单粒子、对粒子和四粒子统计结果；此外在附录B中展示了分离平坦度随记忆深度的变化。未见在不同雷诺数、不同粒子密度或不同截止半径下的系统扫描。
- **充分性与客观性**：实验设计合理，评估采用了训练中未使用的统计量（如对弥散PDF、四面体形状），避免了过拟合。但缺乏与现有降阶模型（如随机Langevin模型、生成式扩散模型）的定量对比。结果主要展示与DNS的一致性，但未有显著性检验或置信区间说明（除四面体形状带估计标准差）。消融实验只针对记忆深度，未对图参数（如截止半径、消息传递层数）进行系统扫描。整体而言，实验覆盖了多粒子统计的核心维度，但公平性限于自身对比。

### 6. 论文的主要结论与发现
1. **记忆是关键**：没有记忆（$K=0$）的马尔可夫模型严重低估加速度重尾；增加记忆深度（$K=5$）逐渐恢复间歇性，加速度PDF与DNS一致。
2. **多粒子统计成功复现**：模型准确再现了单粒子均方位移的弹道-扩散过渡、对分离的超扩散幂律（类Richardson $t^3$）、分离PDF的演化（包括极端分离事件）、纵向和横向相对速度PDF的偏斜与尾行为，以及四面体回转张量特征值和形状指数的各向异性变形。
3. **物理对称性是保障**：通过构造置换等变、伽利略不变和$O(3)$等变，模型无需学习这些对称性，避免了方向/坐标系伪影，提升了泛化能力。
4. **空间与时间分工**：图消息传递捕获已解析的粒子空间组织，MZ记忆负责未解析自由度（如小尺度结构），两者互补，形成自然的降阶结构。

### 7. 优点
- **物理结构化**：严格基于Mori–Zwanzig投影算子，赋予模型显式的记忆结构，可解释性强。
- **对称性内建**：通过等变消息传递自动满足置换、平移、伽利略和旋转/反射对称性，减少数据依赖，提升泛化。
- **显式动力学**：不同于生成式模型，MZ–GNN产生自回归动力学，可用于耦合仿真、控制和预测。
- **多粒子一致性**：单一框架同时学习单粒子、对粒子和四粒子统计，展现了跨尺度的空间相关性捕获能力。
- **训练效率**：采用残差顺序训练，每次只优化一个记忆模块，避免了端到端联合优化的稳定性问题。

### 8. 不足与局限
- **计算资源未公开**：未说明GPU型号、数量及训练耗时，不利于复现与可扩展性评估。
- **缺乏基准对比**：未与现有降阶模型（如随机Langevin模型、生成式扩散模型、其他GNN代理）进行定量性能比较，难以评估相对优势。
- **参数选择缺乏系统优化**：图截止半径、消息传递层数、记忆步长等关键超参数仅凭物理直觉选取，未进行网格搜索或敏感性分析。
- **雷诺数单一**：仅在Reλ≈87下测试，未验证在高雷诺数或不同湍流类型（如剪切湍流、旋转湍流）下的外推能力。
- **粒子云密度固定**：训练和评估中粒子数8000，未研究粒子稀疏程度对模型性能的影响。
- **轻微过估计极端事件**：对粒子纵向相对速度和远距离对分离的PDF尾部略有过估计，可能在高阶统计中引入偏差。
- **无严格泛化性证明**：模型在相同流场配置下评估，未测试对初始条件分布变化或不同初始分离尺度的鲁棒性。

（完）

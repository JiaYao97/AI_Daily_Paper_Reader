---
title: "Guided Flow Matching for Forward and Inverse PDE Problems with Sparse Observations: Algorithm and Theory"
title_zh: 面向稀疏观测的正反PDE问题的引导流匹配：算法与理论
authors: "Xifeng Zhang, Jin Zhao"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25509v1"
tags: ["query:flow-field"]
score: 9.0
evidence: 基于流动匹配的稀疏数据PDE解重建
tldr: 针对从稀疏观测重建PDE解的挑战，提出FM4PDE流匹配生成框架，学习系数/初始状态与解/最终状态的联合分布，推理时通过复合损失引导采样，支持确定性、随机和混合采样器，并给出误差保证。实验表明，该框架在静态和时变PDE上具有竞争性精度，且推理速度优于扩散模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25509v1/fig-001.webp\", \"caption\": \"Figure 1: Framework of FM4PDE. The framework consists of two phases: (i) a training phase, where a Flow Matching network is trained on paired PDE coefficients (or initial states) and solutions (or final states); and (ii) a sampling phase, where the pretrained model is frozen and used for inference.\", \"page\": 6, \"index\": 1, \"width\": 877, \"height\": 698}, {\"url\": \"assets/figures/arxiv/2605.25509v1/fig-004.webp\", \"caption\": \"Table 1: Comparative analysis of relative errors between FM4PDE, DeepONet, and FNO for forward and inverse problems across various PDEs under full observations. The number of steps for the FM4PDE algorithm is set to 1000.\", \"page\": 39, \"index\": 4, \"width\": 582, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2605.25509v1/fig-007.webp\", \"caption\": \"Table 2: Comparative analysis of relative errors between FM4PDE and DiffusionPDE for forward and inverse problems across various PDEs under sparse observations. The number of steps for both algorithms is set to 1000.\", \"page\": 40, \"index\": 7, \"width\": 527, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2605.25509v1/fig-010.webp\", \"caption\": \"Figure 3: Reconstruction of the reaction-diffusion equation from 500 sparse observation points using FM4PDE.\", \"page\": 41, \"index\": 10, \"width\": 667, \"height\": 525}, {\"url\": \"assets/figures/arxiv/2605.25509v1/fig-009.webp\", \"caption\": \"Figure 4: Reconstruction of the shallow-water equations from 500 sparse observation points using FM4PDE.\", \"page\": 41, \"index\": 9, \"width\": 720, \"height\": 435}, {\"url\": \"assets/figures/arxiv/2605.25509v1/fig-005.webp\", \"caption\": \"Figure 5: Comparison of sampling times between FM4PDE and DiffusionPDE for solving forward problems of different equations.\", \"page\": 42, \"index\": 5, \"width\": 709, \"height\": 411}, {\"url\": \"assets/figures/arxiv/2605.25509v1/fig-006.webp\", \"caption\": \"Figure 6: Comparison of sampling times between FM4PDE and DiffusionPDE for solving inverse problems of different equations.\", \"page\": 42, \"index\": 6, \"width\": 709, \"height\": 412}, {\"url\": \"assets/figures/arxiv/2605.25509v1/fig-003.webp\", \"caption\": \"Figure 7: Reconstruction results of FM4PDE on the Burgers equation given 10 and 20 time segments.\", \"page\": 43, \"index\": 3, \"width\": 712, \"height\": 251}, {\"url\": \"assets/figures/arxiv/2605.25509v1/fig-002.webp\", \"caption\": \"Figure 8: The sampling results of solving the Poisson equation using FM4PDE at different step sizes.\", \"page\": 43, \"index\": 2, \"width\": 814, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2605.25509v1/fig-008.webp\", \"caption\": \"Table 3: Global relative error (Loss), observation mean squared error (ObsLoss), and PDE mean squared error (PDELoss) for various sampling strategy mixing ratios on the Poisson equation sparse recovery task.\", \"page\": 44, \"index\": 8, \"width\": 794, \"height\": 256}]"
motivation: 解决稀疏观测下PDE前向与逆向重建问题，需要高效、有理论保证的生成方法。
method: 提出FM4PDE，一种流匹配生成框架，学习联合分布，推理时用复合损失引导采样，并提供三种采样器及其误差分析。
result: 在静态和时变基准PDE上，实现了竞争性精度，且推理速度显著快于扩散模型。
conclusion: 该工作为稀疏观测PDE问题提供了高效且理论完备的生成式求解方案。
---

## 摘要
从稀疏观测中重建PDE解是科学计算中的核心挑战。我们提出FM4PDE，一种流匹配生成框架，学习PDE系数（或初始状态）与解（或最终状态）的联合分布，从而在有限配对数据下实现正向模拟和逆向恢复。在推断时，通过复合损失引导采样，该损失强制与稀疏测量一致并降低PDE残差；我们支持确定性、随机性和混合采样器。我们为这些引导过程提供误差保证。对于确定性优化器，强制条件确保轨迹有界，分阶段收缩在目标精度下产生对数复杂度。对于随机采样器，我们引入自适应引导并假设速度场的耗散性，以获得与噪声基底参数无关的均匀矩界。这导致多项式时间误差界，且匹配下界表明恒定引导会引入不可避免的正偏差，从而激发自适应性。还提供了确定性-随机混合分析。在静态和时变基准PDE上的实验表明，与基于扩散的生成模型相比，具有竞争力的准确性和更快的推断速度。

## Abstract
Reconstructing PDE solutions from sparse observations is a core challenge in scientific computing. We present FM4PDE, a flow-matching generative framework that learns the joint distribution of PDE coefficients (or initial states) and solutions (or final states), enabling both forward simulation and inverse recovery with limited paired data. At inference, sampling is guided by a composite loss that enforces agreement with sparse measurements and reduces the PDE residual; we support deterministic, stochastic, and hybrid samplers. We provide error guarantees for these guided procedures. For the deterministic optimizer, a coercivity condition ensures trajectory boundedness and a phase-wise contraction yields logarithmic complexity in the target accuracy. For the stochastic sampler, we introduce adaptive guidance and assume dissipativity of the velocity field to obtain uniform moment bounds independent of the noise-floor parameter. This leads to polynomial-time error bounds, and a matching lower bound shows constant guidance induces an unavoidable positive bias, motivating adaptivity. A hybrid deterministic-stochastic analysis is also provided. Experiments on static and time-dependent benchmark PDEs demonstrate competitive accuracy and faster inference than diffusion-based generative models.

---

## 论文详细总结（自动生成）

# 论文总结：面向稀疏观测的正反PDE问题的引导流匹配：算法与理论

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：从稀疏观测中重建偏微分方程（PDE）的全局解，这既是前向问题（已知系数/初始条件求解）也是逆向问题（从观测解推断参数）中的关键挑战。实际中传感器限制、测量成本或可达性约束使得只能获得稀疏观测，导致问题具有病态性，需要概率化处理。
- **研究动机**：传统数值方法和监督学习方法（如 PINN、FNO、DeepONet）在满场数据或密集观测时表现优异，但并非设计用于稀疏观测场景；生成模型（如扩散模型）虽可处理条件生成，但训练和采样成本高且缺乏严格的理论收敛分析。流匹配（Flow Matching）作为新兴生成范式，具有稳定训练和高效采样的优势，但缺乏针对 PDE 约束的引导采样理论。
- **整体含义**：本文提出 FM4PDE，一种基于流匹配的生成框架，学习 PDE 系数（或初始状态）与解（或最终状态）的联合分布，通过复合损失引导采样实现从稀疏观测重建全局解，并为确定性、随机和混合采样器提供完整的误差分析，平衡计算效率与理论保证。

## 2. 论文提出的方法论

### 2.1 核心思想
- 利用条件最优传输路径（线性插值）构建概率路径，训练一个参数化速度场 $u^\theta_t$ 将高斯噪声分布变换为目标联合分布（系数+解）。
- 在采样阶段，通过复合引导损失 $\mathcal{L} = \zeta_{\text{obs}} \mathcal{L}_{\text{obs}} + \zeta_{\text{pde}} \mathcal{L}_{\text{pde}}$ 修正生成轨迹，其中 $\mathcal{L}_{\text{obs}}$ 衡量稀疏观测一致性，$\mathcal{L}_{\text{pde}}$ 衡量 PDE 残差。
- 支持三种采样策略：确定性（梯度下降型修正）、随机（基于预测端点插值并加噪声）和混合（先确定性后随机），针对不同阶段设计时间网格和引导系数。

### 2.2 关键技术细节
- **训练阶段**：对于静态 PDE（如 Darcy 流、Poisson、Helmholtz）和时变 PDE（如 Navier-Stokes、Burgers、反应-扩散、浅水方程），构建配对数据集 $x_1 = (a, u)$，使用条件流匹配损失：

$$
\mathcal{L}_{\text{CFM}}(\theta) = \mathbb{E}_{t, X_t, X_1} \left[ \| u^\theta_t(X_t) - u_t(X_t \mid X_1) \|^2 \right]
$$

其中 $X_t = t X_1 + (1 - t) X_0$，$X_0 \sim \mathcal{N}(0, I)$。

- **采样阶段**：
  - 确定性优化器：先用欧拉步预测 $\tilde{x}_{k+1} = x_k + \Delta t_k u^\theta_{t_k}(x_k)$，再基于校正速度更新：

  $$
  x_{k+1} = \tilde{x}_{k+1} - \Delta t_k b_{t_k} \left( \zeta_{\text{obs}} \nabla \mathcal{L}_{\text{obs}} + \zeta_{\text{pde}} \nabla \mathcal{L}_{\text{pde}} \right)
  $$

  其中 $b_t = 1/t - 1$，并采用几何时间网格 $\Delta t_k = \eta t_k$ 避免 $t=0$ 奇点，同时使用梯度裁剪。

  - 随机采样器：预测端点 $\hat{x}^{(k)}_1 = x_k + (1 - t_k) u^\theta_{t_k}(x_k)$，采样新噪声 $\xi_k$，插值得到 $\tilde{x}_{k+1}$，再添加自适应引导项：

  $$
  x_{k+1} = \tilde{x}_{k+1} - c_\zeta (1 - t_k) \left( \zeta_{\text{obs}} \nabla \mathcal{L}_{\text{obs}}(\hat{x}^{(k)}_1) + \zeta_{\text{pde}} \nabla \mathcal{L}_{\text{pde}}(\hat{x}^{(k)}_1) \right)
  $$

  其中自适应引导强度 $\zeta_k = c_\zeta \delta_k$，$\delta_k = 1 - t_k$。

  - 混合策略：设置时间阈值 $t^*$，$t < t^*$ 时使用确定性优化器（快速收敛），$t \ge t^*$ 时切换为随机采样器（探索与鲁棒性）。

### 2.3 理论保证要点
- **确定性采样器**：在耗散性、Polyak-Łojasiewicz 条件和速度-损失交互假设下，证明轨迹有界，且 Phase A（$b_t > \beta_1$）具有指数收缩率 $O(\epsilon^{2\mu})$，Phase B 为线性累积，总体复杂度 $O(\log(1/\varepsilon))$。
- **随机采样器**：引入自适应引导后，利用速度场耗散性和稳定性条件得到与噪声基底 $\delta_{\min}$ 无关的均匀二次矩界和四次矩界。在 Phase 2（终端附近）建立一步下降结构，最终误差 $V_N \le C_{\text{final}} \bar{\zeta} + C_{\text{floor}}$，其中 $\bar{\zeta} = c_\zeta \delta_{\min}$，复杂度 $O(\varepsilon^{-2} \log(1/\varepsilon))$。同时证明恒定引导会带来不可避免的正偏置 $V_{ss} \ge \delta_{\min}/40$。
- **混合采样器**：组合两阶段，保持对数初始收敛和多项式最终精度，总复杂度 $O(\varepsilon^{-2} \log(1/\varepsilon))$。

## 3. 实验设计

- **数据集与场景**：涵盖 7 种 PDE：Darcy 流、Poisson 方程、Helmholtz 方程（静态），以及 Navier-Stokes 方程（非有界涡量形式）、Burgers 方程、反应-扩散方程（FitzHugh-Nagumo 模型）、浅水方程（时变）。系数/初始条件从不同平滑度的高斯随机场（GRF）采样，训练集与测试集使用不同 GRF 参数以严格评估泛化性。
- **观测设置**：全观测场景（表1）和稀疏观测场景（表2），稀疏观测点数为 500 个随机点。对于 Burgers 方程还测试了 5 个时间传感器和 10/20 个时间段的模式。
- **对比方法**：全观测下对比 FNO、DeepONet；稀疏观测下对比 DiffusionPDE。使用 PDEBench 框架实现 FNO，DeepXDE 库实现 DeepONet。
- **评估指标**：相对误差 $\varepsilon_{\text{rel}} = \|x_{\text{pred}} - x_{\text{true}}\|_2 / \|x_{\text{true}}\|_2$，独立生成 20 个测试样本取平均值。

## 4. 资源与算力

- 论文明确说明：“所有实验均使用双 NVIDIA A100 GPU（80GB）”。
- 具体训练时长、步骤数或 GPU 小时数未提及。
- 采样步骤数设为 1000 步进行对比，但未给出训练单模型所需时间。

## 5. 实验数量与充分性

- **实验数量**：较为丰富，包括：
  - 表1：4 种 PDE 的全观测对比（前向和逆向各 2 个指标，共 8 组对比）。
  - 表2：4 种 PDE 的稀疏观测对比（前向和逆向，8 组对比）。
  - 图2-4：Burgers、反应-扩散、浅水方程的视觉重建（含不同观测模式）。
  - 图5-6：采样时间随步数变化（4 种 PDE × 两个问题，8 组对比）。
  - 图7：稀疏度影响（Burgers 10/20 传感器）。
  - 图8：步长影响（Poisson 方程，6 种步长）。
  - 表3：采样策略混合比例消融（Poisson 方程，9 组配置）。
- **充分性**：覆盖多种 PDE 类型（扩散、波动、对流、多分量），同时考虑前向/逆向任务、不同观测密度、不同采样策略和超参数。对比方法为当前主流（FNO、DeepONet、DiffusionPDE），评价指标为标准相对误差，实验设计相对公平。但对比数据集的生成细节可能不完全一致（例如 GRF 参数），可能影响绝对公平性。此外，反应-扩散方程的相对误差高达 49.92%，说明方法在某些复杂多分量系统上仍有较大改进空间。

## 6. 论文的主要结论与发现

- FM4PDE 在稀疏观测下能够有效重建全局 PDE 解，其精度在多数静态 PDE（Darcy、Poisson、Helmholtz）上优于 DiffusionPDE，在 Navier-Stokes 上略逊但仍具竞争力。
- 在满观测对比中，FM4PDE 在除 Helmholtz 正向外的多数任务上优于 FNO 和 DeepONet，尤其在前向和逆向联合解耦方面表现出色。
- 推理速度显著快于 DiffusionPDE：在同等步数下，FM4PDE 的采样时间约减少 30%~50%（图5-6）。
- 理论分析验证了：
  - 确定性采样器具有对数收敛复杂度；
  - 自适应引导可避免恒定引导导致的不可消去的正偏差；
  - 混合策略结合快速初始收敛和最终精度，复杂度 $O(\varepsilon^{-2} \log(1/\varepsilon))$。
- 消融实验表明：纯随机采样器效果最佳，而先确定后随机的混合稍次但优于反向顺序；步长越细或观测越密，重建精度越高。

## 7. 优点

- **理论贡献突出**：为引导流匹配在 PDE 约束下的收敛性提供首个严格分析，涵盖确定性、随机和混合三种采样器，并证明自适应引导的必要性。
- **算法灵活全面**：提出三种采样策略，提供统一框架，可根据任务需求（模式寻求 vs 分布覆盖）灵活选择。
- **实验覆盖广泛**：验证了 7 种不同类型 PDE，涵盖静态/时变、线/非线性、单/多分量，并测试稀疏度、步长、混合比例等关键因素。
- **推理效率高**：流匹配天然支持大步长采样，推理速度显著优于扩散模型（节省一半时间）。
- **代码开源**：提供完整源代码和基准，便于复现和拓展。

## 8. 不足与局限

- **时域处理局限**：目前时变系统仅建模初始和最终状态，未引入全时间演化积分，限制了长期预测或跨时间步的处理能力。
- **实验对比不完全公平**：对手方法（FNO、DeepONet、DiffusionPDE）可能未针对稀疏观测进行专门优化，且训练数据生成方式（GRF 参数）可能影响结果绝对公平性。
- **反应-扩散系统误差较大**：在反应-扩散方程的重建相对误差接近 50%，表明方法对复杂多分量耦合系统适应性不足。
- **超参数选择依赖手动**：引导权重 $\zeta_{\text{obs}}, \zeta_{\text{pde}}$ 和自适应系数 $c_\zeta$ 需要人工调参，缺乏自适应或自动调优机制。
- **理论假设较强**：如耗散性、Polyak-Łojasiewicz 条件、速度场 Lipschitz 光滑性等在一般 PDE 问题中未必严格满足，限制了理论结论的普适性。
- **算力资源未充分报告**：未给出训练阶段的实际耗时和 GPU 使用细节，难以评估实际部署成本。

（完）

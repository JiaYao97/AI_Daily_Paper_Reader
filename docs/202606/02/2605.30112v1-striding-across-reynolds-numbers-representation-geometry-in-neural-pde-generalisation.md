---
title: "Striding Across Reynolds Numbers: Representation Geometry in Neural PDE Generalisation"
title_zh: 跨雷诺数之步：神经PDE泛化中的表示几何
authors: Jianing Shi
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.30112v1"
tags: ["query:pclso"]
score: 9.0
evidence: 卷积自编码器潜在空间用于神经PDE泛化
tldr: "神经PDE求解器的跨雷诺数泛化性能尚不明确。在强迫二维Navier-Stokes基准上，FNO在10倍雷诺数变化下误差达46.68%，而零前向模型检索基线为41-42%，表明表示几何是关键变量。本文提出ConvAE-Relay方法，在源训练卷积自编码器潜在空间中匹配状态并借用力学，仅用源域数据库达到38.34%误差。消融实验表明匹配质量优于更新规则。Oracle实验显示源域力学方向可转移（余弦相似度~0.84），自回归漂移为主要瓶颈（~12百分点）。U-Net多尺度跳跃连接实现34.72%误差，进一步支持局部多尺度表示组织迁移。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.30112v1/fig-004.webp\", \"caption\": \"Figure 1. Cross-Reynolds diagnostic summary. (a) Vorticity predictions and absolute error maps at Re=10,000 (rollout step 5, medianerror trajectory). FNO produces visibly smoother fields; ConvAE-Relay and U-Net preserve finer structure. (b) 2×2 representation ablation (seed 42): upgrading matching space from PCA to ConvAE latent improves the best result by 2.37 pp, while the update-rule effect is ≤1.1 pp; matching quality dominates. (c) Oracle bottleneck decomposition: autoregressive drift accounts for ∼12 pp, magnitude mismatch ∼2 pp, and residual ∼1 pp. Oracle variants are target-informed diagnostic probes, not deployable methods.\", \"page\": 3, \"index\": 4, \"width\": 1012, \"height\": 591}, {\"url\": \"assets/figures/arxiv/2605.30112v1/fig-008.webp\", \"caption\": \"Figure 2. Per-step dynamics cosine similarity ct = ⟨∆̂ωt,∆ωt⟩/(∥∆̂ωt∥2∥∆ωt∥2) between borrowed and actual dynamics. Under oracle matching, similarity remains stable at ∼0.84; under standard relay, it collapses by step 10.\", \"page\": 5, \"index\": 8, \"width\": 487, \"height\": 337}, {\"url\": \"assets/figures/arxiv/2605.30112v1/fig-002.webp\", \"caption\": \"Figure 3. Per-step relative L2 error over 10 rollout steps at Re=10,000. Shaded bands denote 95% bootstrap confidence intervals over 500 trajectories (narrow bands indicate low trajectorylevel variance). U-Net maintains the lowest error throughout; all methods show monotonically increasing error consistent with autoregressive drift accumulation.\", \"page\": 6, \"index\": 2, \"width\": 443, \"height\": 334}, {\"url\": \"assets/figures/arxiv/2605.30112v1/fig-001.webp\", \"caption\": \"Figure 4. Wavenumber-resolved spectral relative error at rollout step 10. All methods show higher error at low wavenumbers (largescale dynamics); U-Net achieves the lowest error across most scales. Shaded bands denote 95% bootstrap confidence intervals.\", \"page\": 6, \"index\": 1, \"width\": 487, \"height\": 335}, {\"url\": \"assets/figures/arxiv/2605.30112v1/fig-003.webp\", \"caption\": \"Figure 5. Cross-regime comparison at Re=10,000 and Re=100,000. At 10× shift, ConvAE-Relay outperforms FNO; at 100× shift, this advantage reverses. U-Net retains its advantage at both regimes. Error bars denote 95% bootstrap confidence intervals.\", \"page\": 7, \"index\": 3, \"width\": 483, \"height\": 259}, {\"url\": \"assets/figures/arxiv/2605.30112v1/fig-006.webp\", \"caption\": \"Figure 6. Relay scaling behaviour. (a) Database size ablation: approximately 75% of improvement is achieved by the first 500 source trajectories, with diminishing returns beyond. (b) Long-horizon degradation: by T=20, ConvAE-Relay converges toward persistence, consistent with drift accumulation eroding matching quality over extended rollouts. Error bars denote 95% bootstrap confidence intervals.\", \"page\": 12, \"index\": 6, \"width\": 1012, \"height\": 357}, {\"url\": \"assets/figures/arxiv/2605.30112v1/fig-007.webp\", \"caption\": \"Figure 7. U-Net epoch saturation (seed 42). OOD performance at Re=10,000 saturates by 10 epochs, while in-distribution error continues improving to 50 epochs. Further source-regime training does not reduce OOD error.\", \"page\": 12, \"index\": 7, \"width\": 496, \"height\": 307}, {\"url\": \"assets/figures/arxiv/2605.30112v1/fig-005.webp\", \"caption\": \"Figure 8. Enstrophy spectrum ratio Zpred(k)/Ztrue(k) at rollout step 10. Values above 1 indicate spectral energy amplification; below 1 indicates damping. The dashed line marks perfect reconstruction (ratio=1).\", \"page\": 12, \"index\": 5, \"width\": 503, \"height\": 304}]"
motivation: 跨雷诺数泛化在神经PDE求解器中仍缺乏清晰表征，需要探索表示几何对迁移的影响。
method: 提出ConvAE-Relay，通过源训练卷积自编码器潜在空间状态匹配，从源域数据库借用力学更新。
result: "在强迫2D Navier-Stokes基准中，ConvAE-Relay达到38.34%相对L2误差，优于零前向模型检索基线（41-42%）。"
conclusion: 局部多尺度表示是组织跨雷诺数迁移的关键，自回归漂移是主要性能瓶颈。
---

## 摘要
跨雷诺数泛化在神经PDE求解器中仍未得到充分表征。在经典的受迫二维纳维-斯托克斯基准测试中，经过训练的傅里叶神经算子（Fourier Neural Operator）在雷诺数变化10倍时，相对L2误差达到46.68%，而零前向模型检索基线已提升至41-42%。这表明表示几何是测试方法间的一个主要组织变量。我们通过ConvAE-Relay来检验这一假设，该方法在源域训练的卷积自编码器潜在空间中进行状态匹配，并从源域数据库借用动力学，仅使用源域数据库且无需目标域拟合、标签或数据库条目，即达到38.34±0.07%的误差。2×2消融实验表明，匹配质量优于更新规则的主导因素。Oracle实验证实，当匹配保持在流形上时，源域动力学方向仍可迁移（余弦相似度约0.84）；自回归漂移是主要瓶颈（约12个百分点）。从学习预测的角度看，具有多尺度跳跃连接的U-Net达到了34.72±0.60%的误差，这与检索端的发现一致，即局部多尺度表示在测试方法间组织了跨雷诺数迁移。所有结论均限于该基准测试。

## Abstract
Cross-Reynolds generalisation in neural PDE solvers remains poorly characterised. On the canonical forced 2D Navier-Stokes benchmark, a trained Fourier Neural Operator reaches 46.68% relative L2 error under a 10x Reynolds-number shift, yet zero-forward-model retrieval baselines already improve to 41-42%. This suggests representation geometry as a major organising variable among the tested methods. We test this hypothesis through ConvAE-Relay, which matches states in a source-trained convolutional autoencoder latent space and borrows dynamics from a source-regime database, achieving 38.34+/-0.07% using only a source-regime database and no target-regime fitting, labels, or database entries. A 2x2 ablation isolates matching quality as dominant over the update rule. Oracle experiments confirm that source-regime dynamics directions remain transferable (cosine similarity ~0.84) when matching stays on-manifold; autoregressive drift is the primary bottleneck (~12 percentage points). From the learned-prediction side, a U-Net with multi-scale skip connections achieves 34.72+/-0.60%, consistent with the retrieval-side finding that local, multi-scale representations organise cross-Reynolds transfer among tested methods. All claims are scoped to this benchmark.

---

## 论文详细总结（自动生成）

# 跨雷诺数之步：神经PDE泛化中的表示几何 —— 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：神经PDE求解器在跨参数（尤其是雷诺数Re）的泛化性能尚不明确。雷诺数控制惯性力与粘性力的比例，提高Re会激活更细的空间尺度，使得跨Re预测成为一种本质性的外推问题，而非简单的协变量偏移。
- **动机**：在经典受迫二维纳维-斯托克斯（2D Navier-Stokes）基准上，已训练的傅里叶神经算子（FNO）在Re变化10倍时相对L2误差高达46.68%，而零前向模型检索基线（如PCA relay、kNN-copy）已能达到41–42%，这暗示**表示几何**（representation geometry）可能是组织不同方法表现的关键变量。
- **核心假设**：测试方法之间的性能差异主要由它们使用的状态表示几何（如全局线性 vs. 局部非线性 vs. 多尺度）驱动，而非是否学习或检索动力学映射。

## 2. 方法论：核心思想、关键技术细节

### 2.1 总体框架：ConvAE-Relay

- **核心思想**：利用源域（Re≈1000）训练的卷积自编码器（ConvAE）学到一个局部、非线性的潜在空间，在该空间中对目标域（Re≈10000）的状态进行最近邻匹配，然后直接从源域数据库借用力学更新（delta），无需目标域任何数据或标签。
- **关键技术细节**：
  - 编码器 $\phi$：4个步长为2的卷积层（1→32→64→128→256）+ BatchNorm + ReLU，最后线性投影到64维潜在空间。镜像解码器用于训练。
  - 训练：仅使用源域Re≈1000的帧，MSE重建损失，50 epochs，重建误差约1%。
  - 数据库：存储156,000个源域编码后的状态及其对应的delta（$\Delta\omega = \omega_{t+1} - \omega_t$）。
  - 推理过程（Algorithm 1）：给定目标上下文$\omega_0,\dots,\omega_9$，从第9帧开始。每隔$N_{\text{ride}}=3$步进行一次匹配，其余步沿用所匹配源轨迹的后续帧（riding）。
  - 匹配度量：余弦距离 $d(\phi(\omega_i), \phi(\omega_j)) = 1 - \frac{\langle\phi(\omega_i), \phi(\omega_j)\rangle}{\|\phi(\omega_i)\|_2 \|\phi(\omega_j)\|_2}$。
  - 预测：$\hat{\omega}_{t+1} = \omega_t + \Delta\omega_{s, j^\star_t}$，其中$j^\star_t$是源数据库中的最近邻索引。

- **公式（略）**：匹配公式如上，无需额外公式。

### 2.2 其他方法

- **FNO**：全局谱域卷积，4个傅立叶层，12个模式，宽度20，约466K参数。
- **PCA relay**：使用PCA（36个分量，保留99.77%方差）作为匹配空间，后续delta或copy。
- **kNN-copy**：在原始4096维涡量空间匹配，直接复制源域的下一帧。
- **U-Net**：编码器-解码器+跳跃连接，通道32→64→128→256→512，周期填充，约7.8M参数。输入10帧堆叠，预测下一帧。

## 3. 实验设计

### 3.1 数据集与场景

- **方程**：受迫2D涡量Navier-Stokes方程，64×64周期域。
- **三个雷诺数Regime**（表1）：
  - 源域（Source）：Re≈1000，5000条轨迹（0–3999训练/数据库，4000–4999验证）。
  - 目标域（Target 1）：Re≈10000，500条轨迹，仅用于评价（OOD）。
  - 边界域（Target 2）：Re≈100000，500条轨迹，额外边界评估。
- **任务**：给定前10帧（0–9），预测后10帧（10–19），通过10步自回归滚动预测。评价指标为平均相对L2误差（公式2），对500条轨迹取平均。

### 3.2 对比方法

- **零参数基准**：Persistence（保留最后一帧），PCA relay，kNN-copy。
- **检索方法**：ConvAE-Relay（3种子）。
- **学习预测器**：FNO（3种子），U-Net（3种子），以及消融中的其他变体（局部卷积预测器、U-Net relay等）。
- **Oracle探针**：用于诊断瓶颈（如Oracle matching、Oracle + alpha、Oracle magnitude）。

### 3.3 协议公平性

所有方法仅使用源域（Re≈1000）数据训练或构建数据库；目标域数据仅用于评价。FNO和U-Net训练epochs不同（FNO 300 epochs有早停，U-Net 10 epochs），但论文不声称穷尽架构优化，而是诊断标准协议下的行为。

## 4. 资源与算力

- **文中未明确说明**使用的GPU型号、数量、训练时长。仅报告了各模型的参数规模和训练epochs（FNO最多300，ConvAE 50，U-Net 10）。未提及分布式训练或硬件配置。
- **缺失信息**：无法得知具体算力消耗。只能推断实验规模适中（64×64网格，参数数百万）。

## 5. 实验数量与充分性

- **主要实验**（表2）：6种方法，每个学习预测器3种子，检索方法（除PCA relay外）3种子，统计标准差。包含FNO家族变体（因子化FNO、尺度一致性损失，附录B）。
- **消融实验**（表3）：2×2表示消融（PCA、原始场、ConvAE潜在空间 × delta借用、原始复制），隔离匹配质量与更新规则。
- **Oracle瓶颈分解**（表4）：逐步移除漂移、幅度、残差，量化贡献。
- **机制消融**（第4.4节）：Smart tracking（~0.15 pp改进）、Dynamics-aware AE（2.19 pp更差）、Nride消融（<0.1 pp）、History matching（5 pp更差）。这些消融通过“排除法”尖锐化诊断。
- **边界与扩展**：Re=100000比较、长视界T=20、数据库大小消融（100→4000）、U-Net epoch饱和、U-Net relay变体（3种）、注意力增强（附录B）、局部卷积预测器（负对照）。
- **充分性评价**：实验设计较为全面，覆盖了检索端和学习端，使用多种诊断工具（oracle、分辨率谱、涡度谱比）。但所有实验仅限一个基准（2D NS，64×64），且某些方法的训练预算不对齐（FNO 300 v.s. U-Net 10 epochs），可能引入不公平性。作者承认这一局限，并将结论限定在该基准内。

## 6. 主要结论与发现

1. **表示几何是组织变量**：跨Re性能由表示几何（而非是否学习动力学）主导。检索方法随表示变得更局部/非线性（PCA→kNN→ConvAE）而提升；学习预测器随架构提供更多局部多尺度通路（FNO→U-Net）而提升。
2. **ConvAE-Relay优于零前向模型基线**：达到38.34%，相比PCA relay（41.77%）提升3.4 pp，仅通过改变匹配表示实现。
3. **U-Net最佳**：34.72%，比FNO（46.68%）大幅提升，表明多尺度跳跃连接对跨Re迁移有益。
4. **自回归漂移是主要瓶颈**：Oracle匹配消除漂移后误差降至26.49%（≈12 pp改善），漂移是最大的可工程化瓶颈。
5. **源域动力学方向可迁移**：当匹配保持在流形上时，余弦相似度稳定在~0.84。
6. **表示权衡**：优化预测性能（如dynamics-aware AE）会损害检索指数可索引性（matching error上升），净效果变差。
7. **边界效应**：Re=100000时，检索优势逆转（ConvAE-Relay 65.65% vs FNO 63.08%），表明超出覆盖范围时表示几何不足以补偿。

## 7. 优点

- **新颖的方法学**：ConvAE-Relay将学习表示与检索结合，无需学习前向映射即可实现诊断，结构简单且可控。
- **系统的诊断工具**：Oracle实验、2×2消融、机制消融等提供了清晰的瓶颈量化，揭示了漂移、幅度、表示权衡等因素。
- **从检索与学习双角度提供一致性证据**：两种互补视角均指向表示几何，增强了结论的可靠性。
- **精细的消融设计**：如Smart tracking、Dynamics-aware AE、History matching等失败尝试，通过排除法锐化了诊断。
- **透明地承认局限**：明确限定结论范围，不夸大，有可重复性意识（提供种子数、置信区间等）。

## 8. 不足与局限

- **实验覆盖狭窄**：仅在一个基准（受迫2D NS，64×64）上验证，泛化性未知。作者也声明所有主张限于该基准。
- **训练预算不对齐**：FNO训练至300 epochs（早停），U-Net只训练10 epochs（但验证OOD饱和）。虽然作者强调不追求最优性能，但预算差异可能影响公平性。
- **表示几何的代理性质**：对于U-Net等学习预测器，表示几何是架构偏置的代理，未直接测量隐空间邻域结构（作者建议作为下一步）。
- **缺失算力信息**：无法评估实际计算成本。
- **未探讨更大的模型家族**：如Transformer神经算子等虽提但未深入测试；注意力增强仅在简单条件下测试（降解）。
- **数据库大小衰减**：虽然数据库大小消融表明500条轨迹后收益递减，但未讨论数据库索引的进一步潜力（如结构化索引、记忆压缩）。

（完）

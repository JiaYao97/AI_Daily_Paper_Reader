---
title: Constructing VAE Latent Spaces with Prescribed Topology
title_zh: 构建具有指定拓扑结构的VAE潜空间
authors: "Jilles S. van Hulst, Jakub M. Tomczak, W. P. M. H. Heemels, Duarte J. Antunes"
date: 2026-06-05
pdf: "https://arxiv.org/pdf/2606.07058v1"
tags: ["query:pclso"]
score: 7.0
evidence: 构建具有规定拓扑的VAE潜在空间，促进更好的自编码器降阶模型。
tldr: 标准VAE的高斯先验与数据非欧拓扑不匹配会降低重构质量。本文提出构造性框架，针对乘积覆盖空间流形（如圆环、莫比乌斯带等），使用分解分布得到闭式KL散度，通过重新参数化编码器先验对和群不变解码器来消除拓扑失配。实验证明拓扑匹配先验在所有正则化强度下均优于高斯基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.07058v1/fig-002.webp\", \"caption\": \"TABLE II SYNTHETIC EXPERIMENTS: RECONSTRUCTION AND TOPOLOGY METRICS. LOWER IS BETTER FOR ALL QUANTITIES. MODELS WITHIN EACH GROUP ARE EVALUATED AT MATCHED KL DIVERGENCE.\", \"page\": 10, \"index\": 2, \"width\": 852, \"height\": 213}, {\"url\": \"assets/figures/arxiv/2606.07058v1/fig-001.webp\", \"caption\": \"Fig. 5. KL sweep for the three synthetic topologies. Each subplot varies β from 0.01 to 10.0 and plots reconstruction RMS vs. KL divergence for the topology-aware VAE and the Gaussian baseline: (a) Cylinder S1 × [0, 1], (b) Torus T 2, (c) Möbius strip. Shaded bands show the 25th–75th percentile over 50 seeds. The torus topology-aware VAE Pareto-dominates the Gaussian baseline; for cylinder and Möbius strip, the advantage holds once β exceeds a small topology-dependent threshold.\", \"page\": 11, \"index\": 1, \"width\": 1016, \"height\": 207}, {\"url\": \"assets/figures/arxiv/2606.07058v1/fig-004.webp\", \"caption\": \"Fig. 6. Rotated MNIST experiment (R2 × S1 latent space, digit classes 6 and 7). Columns: ground truth, Gaussian VAE (ℓ = 3), Mixed-Circle VAE, and Mixed-Circle VAE (anchored). First row: latent coordinates colored by the true rotation angle θ. for the Mixed-Circle VAEs, the circular coordinates (cos zθ, sin zθ) are plotted; for the Gaussian, a UMAP projection of the full latent space is shown instead; for the ground-truth column, (cos θ, sin θ) is plotted. Second row: latent coordinates colored by digit class. The Mixed-Circle VAE shows the Euclidean coordinates (z1, z2), while a UMAP projection is shown for the ground-truth and Gaussian columns. Third row: 12-step ring for each digit class (3 and 4), sweeping zθ for the Circle VAEs or the proxy atan2(z1, z2) for the Gaussian, with style coordinates (z1, z2) fixed at the class centroid. Fourth row: decoded geodesics between two endpoints from the same digit class, linearly interpolated in latent space.\", \"page\": 12, \"index\": 4, \"width\": 1051, \"height\": 767}, {\"url\": \"assets/figures/arxiv/2606.07058v1/fig-003.webp\", \"caption\": \"Fig. 7. Shifted MNIST experiment (R2 × T 2 latent space, digit classes 3, 4, and 7). Columns: ground truth, Gaussian VAE (ℓ = 4), Mixed-Torus VAE, and Mixed-Torus VAE (anchored). First row: latent coordinates colored by the true shift angle θ1. The ground-truth column plots (θ1, θ2) directly. The Mixed-Torus VAEs plot the learned circular coordinates (zθ1 , zθ2 ). The Gaussian column shows a UMAP projection of the full latent space. Second row: latent coordinates colored by digit class. The Mixed-Torus VAEs show the Euclidean style coordinates (z1, z2). The ground-truth and Gaussian columns show a UMAP projection. Third row: 10× 10 decoded grid, shown for digit 3. The Mixed-Torus VAEs sweep both circular coordinates (zθ1 , zθ2 ) with style coordinates fixed at the digit-3 centroid. The Gaussian VAE sweeps its first two Euclidean dimensions (z1, z2), with the remaining dimensions fixed at the digit-3 centroid. Fourth row: decoded geodesic paths, one per digit class, with style coordinates fixed at the class centroid.\", \"page\": 13, \"index\": 3, \"width\": 1050, \"height\": 1015}]"
motivation: 标准VAE的高斯先验假设潜空间具有欧几里得拓扑，而数据分布常位于非欧流形上，拓扑不匹配导致重构质量下降和表示不忠实。
method: 将潜空间拓扑显式建模为基本因子的乘积或商，使用分解分布得到解耦的KL散度，并为周期、有界、无界支撑设计重新参数化编码器-先验对；商流形上解码器使用群不变特征。
result: 在合成流形和旋转/循环移位MNIST数据集上，拓扑匹配VAE在所有正则化强度下均优于高斯基线，重构更准确。
conclusion: 通过设计匹配数据拓扑的先验分布，能有效提升VAE的重构质量和表示忠实性，且训练方式保持实用。该框架适用于一大类非欧流形。
---

## 摘要
变分自编码器（VAEs）学习高维数据的低维潜在表示。当数据位于具有非欧几里得拓扑的流形上时，标准高斯先验会引入拓扑不匹配，从而降低重建质量并阻止忠实表示。我们提出了一个构造性数学框架，该框架解决了所有允许乘积覆盖空间的流形的这种不匹配问题。这些流形可以表示为基本因子（圆、区间或直线）的乘积，或通过有限对称群对这类乘积的商。该类包括圆柱面、环面、莫比乌斯带、克莱因瓶和实射影空间。基本因子上的因子化分布产生具有闭式解耦KL散度的乘积拓扑，使得每个潜在因子可以独立成形，同时保持训练的可处理性。我们为周期、有界和无界支撑编录了可参数重参数化的编码器-先验对，并提供了允许标准神经网络输出具有平滑梯度的非欧几里得参数的坐标变换。对于商流形，解码器接收覆盖空间坐标的群不变特征，使得识别点产生相同的输出。锚约束将坐标系相对于数据固定或创建软拓扑孔。在合成流形和真实图像数据集（旋转和循环平移的MNIST）上的实验证实，拓扑匹配的先验使KL正则化与数据流形对齐。由此产生的拓扑感知模型在所有实际相关的正则化强度上都优于高斯基线。代码可在 https://github.com/JvHulst/VAE-Topology 获取。

## Abstract
Variational autoencoders (VAEs) learn low-dimensional latent representations of high-dimensional data. When the data lies on a manifold with non-Euclidean topology, the standard Gaussian prior introduces a topological mismatch that degrades reconstruction quality and prevents faithful representation. We present a constructive mathematical framework that resolves this mismatch for all manifolds that admit a product covering space. These are manifolds expressible as products of elementary factors (circles, intervals, or lines) or as quotients of such products by a finite symmetry group. The class includes cylinders, tori, Möbius strips, Klein bottles, and real projective spaces. Factorized distributions over the elementary factors yield product topologies with closed-form, decoupled KL divergences, so that each latent factor can be shaped independently while keeping training tractable. We catalogue reparametrizable encoder-prior pairs for periodic, bounded, and unbounded supports, and provide coordinate transformations that allow standard neural networks to output non-Euclidean parameters with smooth gradients. For quotient manifolds, the decoder receives group-invariant features of the covering-space coordinates, so that identified points produce identical outputs. Anchor constraints fix the coordinate system relative to the data or create soft topological holes. Experiments on synthetic manifolds and real-image datasets (rotated and cyclically shifted MNIST) confirm that a topology-matched prior aligns KL regularization with the data manifold. The resulting topology-aware models outperform the Gaussian baseline at all practically relevant regularization strengths. The code is available at https://github.com/JvHulst/VAE-Topology.
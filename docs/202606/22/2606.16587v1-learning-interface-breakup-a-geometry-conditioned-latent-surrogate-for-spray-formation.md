---
title: "Learning Interface Breakup: A Geometry-Conditioned Latent Surrogate for Spray Formation"
title_zh: 学习界面破碎：一种用于喷雾形成的几何条件潜在代理模型
authors: "Julius H Ramlau, Friedrich Hastedt, Tolga Birdal, Ehecatl-Antonio del Río Chanona, Nausheen S Basha, Omar K Matar"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16587v1"
tags: ["query:flow-field"]
score: 9.0
evidence: 几何条件潜在代理用于喷雾形成; 降阶建模
tldr: 针对喷嘴设计中的两相破碎瞬态模拟成本高的问题，提出几何条件潜在代理模型，利用AMR细胞密度场作为紧凑表示，在797个模拟上训练，重建密度演化与几何，推理仅0.045秒，加速超6e4倍，准确捕捉界面动力学。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16587v1/fig-003.webp\", \"caption\": \"Figure 1. Geometry-conditioned latent surrogate architecture. (a) The FNO encoder (four spectral-convolution blocks followed by global average pooling) maps a density snapshot to a time-independent latent z ∈ R48. The time-conditioned SIREN decoder reconstructs κ̂(x, y; t) from (z, t, x, y) via FiLM conditioning with multi-scale spatial Fourier features (σs ∈ {1, 2, 5, 10, 20, 50, 100}) and sinusoidal time features. The geometry head is a 1D implicit neural representation (INR) decoding (yupper(x), ylower(x)) from the same µ. (b) The Stage-2 U-Net recovers velocity (u, v) and volume fraction f from the reconstructed density, trained independently.\", \"page\": 2, \"index\": 3, \"width\": 1017, \"height\": 194}, {\"url\": \"assets/figures/arxiv/2606.16587v1/fig-001.webp\", \"caption\": \"Figure 2. Full-pipeline temporal reconstruction on a held-out simulation (sim 474). Each row shows a flow channel (density κ, volume fraction f , horizontal velocity u, vertical velocity v); columns show ground truth (GT) and model prediction (Pred) at four times. The latent z is obtained by encoding a single late-time snapshot (tmax = 3.9) and is held fixed for the entire trajectory. Density is reconstructed by the Stage-1 decoder from this time-independent latent, while volume fraction and velocities are predicted by the Stage-2 U-Net from the reconstructed density κ̂. Predicted nozzle walls (white/gray), decoded from the same latent, are overlaid on every panel, confirming geometric consistency and showing reconstruction of both later and earlier times from a late-time encoding.\", \"page\": 6, \"index\": 1, \"width\": 1012, \"height\": 517}, {\"url\": \"assets/figures/arxiv/2606.16587v1/fig-004.webp\", \"caption\": \"Figure 3. Per-timestep reconstruction error across the 81 test simulations. Absolute per-channel MSE; shaded bands show ± one standard deviation across test simulations.\", \"page\": 7, \"index\": 4, \"width\": 456, \"height\": 326}, {\"url\": \"assets/figures/arxiv/2606.16587v1/fig-005.webp\", \"caption\": \"Figure 4. Effect of early-time encoding on full-pipeline reconstruction (sim 474). Same setup as Fig. 2, but the latent z is obtained from an early-time snapshot (tmin = 0.1) and held fixed for the full trajectory reconstruction. All channels (density κ, volume fraction f , velocities u, v) and geometry are decoded as in the main figure, showing qualitatively similar reconstruction behavior.\", \"page\": 10, \"index\": 5, \"width\": 1012, \"height\": 517}, {\"url\": \"assets/figures/arxiv/2606.16587v1/fig-002.webp\", \"caption\": \"Figure 5. Representative failure case on a held-out test simulation (sim 366). This example ranks among the worst 5% of held-out simulations by composite reconstruction error at t = 3.9. The model smooths thin ligaments and under-resolves sharply localized late-time breakup structures, with visible errors also appearing in the macroscopic jet trajectory.\", \"page\": 11, \"index\": 2, \"width\": 1012, \"height\": 518}]"
motivation: 高保真VOF模拟成本过高，无法用于迭代设计探索，且标准代理模型难以处理界面和自适应网格的瞬态演化。
method: 编码AMR细胞密度场作为紧凑代理，训练几何条件潜在模型重建密度演化与喷嘴几何，再用轻量级第二阶段恢复其余流动变量。
result: 在保留模拟上准确捕捉关键界面动力学，推理时间0.045秒每轨迹，相比Basilisk CFD加速超过6×10^4倍。
conclusion: AMR细化结构可作为几何条件代理建模的可学习紧凑表示，适用于瞬态两相流。
---

## 摘要
设计喷雾喷嘴需要预测几何形状如何影响瞬态两相破碎，但采用自适应网格细化（AMR）的高保真流体体积（VOF）模拟对于迭代设计探索来说成本过高。标准代理模型也面临挑战，因为液-气界面和底层的自适应离散化都随时间与几何形状演变。我们提出了一种几何条件潜在代理模型，该模型在797个两相喷嘴模拟上进行训练，通过编码AMR细胞密度场（作为求解器集中分辨率处的紧凑代理），而非完整的多通道流状态，来应对这一挑战。从该表示中，模型重建瞬态密度演化和喷嘴几何形状，而轻量级的第二阶段恢复剩余的流动变量。在保留的模拟中，该方法准确地捕捉了关键的界面动力学，同时将每个轨迹的推理时间减少到0.045秒，相对于Basilisk CFD加速了6×10⁴倍以上。这些结果表明，AMR细化结构可以作为瞬态两相流几何条件代理建模的紧凑且可学习的表示。

## Abstract
Designing spray nozzles requires predicting how geometry shapes transient two-phase breakup, but high-fidelity volume-of-fluid (VOF) simulations with adaptive mesh refinement (AMR) are too expensive for iterative design exploration. Standard surrogate models are also challenged by this setting because both the liquid--gas interface and the underlying adaptive discretization evolve across time and geometries. We introduce a geometry-conditioned latent surrogate trained on 797 two-phase nozzle simulations that addresses this by encoding the AMR cell-density field, rather than the full multi-channel flow state, as a compact proxy for where the solver concentrates resolution. From this representation, the model reconstructs transient density evolution and nozzle geometry, and a lightweight second stage recovers the remaining flow variables. On held-out simulations, the method accurately captures key interface dynamics while reducing inference time to 0.045 seconds per trajectory, corresponding to a speed-up of more than $6\times10^4$ relative to Basilisk CFD. These results suggest that AMR refinement structure can serve as a compact and learnable representation for geometry-conditioned surrogate modeling of transient two-phase flows.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：在喷雾喷嘴设计中，几何形状对瞬态两相破碎过程有决定性影响。然而，高保真度的 VOF 模拟（结合 AMR）计算成本极高，无法满足迭代设计探索的需求。标准代理模型也面临挑战，因为液‑气界面和自适应离散化都随时间与几何变化而演变。
- **核心问题**：如何在保持关键界面动力学精度的同时，大幅降低瞬态两相流仿真的计算成本，从而为几何驱动的迭代设计提供可行的替代方案。
- **整体含义**：论文提出一种**几何条件潜在代理模型**，通过编码 AMR 细胞密度场（而非完整的多通道流状态）作为紧凑表示，实现从几何到瞬态流演化的联合建模，在推理速度上相比 CFD 获得超过 6×10⁴ 倍的加速，同时保持了合理的重建精度。

## 2. 论文提出的方法论

- **核心思想**：将 AMR 生成的细胞密度场 $\kappa$ 作为求解器信息源，利用其集中分辨率的特性，作为紧凑的可学习代理。通过一个两阶段管道实现：
    - **Stage‑1**：一个变分自编码器（VAE），学习共享潜在表示 $z \in \mathbb{R}^{48}$，同时解码密度演化 $\hat{\kappa}$ 和喷嘴几何边界 $(y_{\text{upper}}(x), y_{\text{lower}}(x))$。
    - **Stage‑2**：一个轻量级 U‑Net，从重建的密度 $\hat{\kappa}$ 中恢复体积分数 $f$ 和速度场 $(u, v)$。
- **关键技术细节**：
    - **FNO 编码器**：从密度快照 $\kappa \in \mathbb{R}^{1\times 128\times 128}$ 中提取时间无关的潜在码 $(\mu, \log\sigma^2) \in \mathbb{R}^{d_z}$。结构：提升卷积 + 4 个傅里叶神经算子块 + 全局平均池化 + 线性头。
    - **时间条件 INR 解码器**：使用 SIREN 网络与 FiLM 条件调制，输入包括潜在码 $z$、时间 $t$、空间坐标 $(x,y)$。空间坐标经多尺度随机傅里叶特征编码，时间经正弦特征编码。
    - **几何头部**：一个 1D INR 从 $\mu$ 解码出喷嘴上下壁曲线，支持任意分辨率查询。
    - **Stage‑2 U‑Net**：标准卷积编码‑解码结构，输入为重建密度 $\hat{\kappa}$，输出三通道 $[\hat{f}, \hat{u}, \hat{v}]$，体积分数用 sigmoid 激活，速度用线性激活。
    - **推理方式**：
        - 若有密度快照，直接编码得到 $z$。
        - 若仅有目标几何 $g^*$，则通过梯度优化在潜在空间求解等式 (9) 得到 $z^*$（K=8 次重启）。
    - **损失函数**：Stage‑1 联合优化密度重建误差、几何重建误差（权重 $\lambda_g=5$）和 KL 散度（采用 free‑bits 技巧，$\lambda_{fb}=0.5$，KL 权重 $\beta$ 经线性预热后由 PID 控制器调整至目标 18 nats）。
- **公式**：
    $$L = \|\hat{\kappa}_t - \kappa_t\|_2^2 + \lambda_g \|G_\omega(\mu) - g_\theta\|_2^2 + \beta \cdot \widetilde{D}_{KL}\big(q_{\phi}(z|\kappa_t)\| \mathcal{N}(0,I)\big)$$
    其中 $\widetilde{D}_{KL}$ 为带 free‑bits 下界 $ \lambda_{fb} $ 的 KL 项。

## 3. 实验设计

- **数据集**：
    - 生成 1000 个喷嘴几何样本（NURBS 曲线参数化：上壁曲率 $p_1 \in [0.05, 0.2]$，下壁曲率 $p_2 \in [-0.2, -0.05]$，宽度缩放 $w \in [0.5, 1.5]$）。
    - 使用 Basilisk 执行两相流 DNS，自适应八叉树网格细化至最大级别 9。在无量纲化下，液相雷诺数 $\mathrm{Re}=10^3$，韦伯数 $\mathrm{We}\approx5.56\times10^3$，密度比 $\rho_l/\rho_g=27.84$。对每个几何模拟 40 个时间步（$\Delta t=0.1$）。
    - 保留 797 个完整轨迹，映射到固定 128×128 欧拉网格（通过 Nadaraya‑Watson 核回归），得到四通道表示：密度代理 $\kappa$、体积分数 $f$、速度 $u$ 和 $v$。
    - 按模拟级划分：训练/验证/测试 = 637/79/81 个模拟（约 31,000 个快照）。
- **基准与对比**：
    - 对比**局部几何最近邻基线**：将测试模拟的预测替换为训练集中几何距离最近（L2 边界曲线距离）的模拟的对应快照。
    - 对于几何目标潜在反演，还对比了**编码器 oracle**（使用真实密度快照编码）和**最近邻几何基线**。
- **评估指标**：密度 MSE/PSNR、几何 MAE（物理坐标）、Stage‑2 U‑Net 各通道 MSE、集成密度和动量守恒偏差的比较。

## 4. 资源与算力

- **训练硬件**：
    - Stage‑1 VAE：在单块 NVIDIA A100 GPU 上训练 300 个 epoch，批量大小 16，优化器 AdamW（学习率 $10^{-4}$，权重衰减 $10^{-4}$），梯度裁剪 1.0。
    - Stage‑2 U‑Net：在 Apple M4 设备上（PyTorch MPS 后端）训练 20 个 epoch，批量大小 32，优化器 AdamW（学习率 $10^{-3}$）。
- **模型参数**：VAE 约 8.7M，几何头部约 30K，总计约 8.8M；Stage‑2 U‑Net 约 7M。
- **推理速度**：每个轨迹（39 时间步）平均 0.045 秒（Apple M4 GPU），相对 Basilisk CFD（12 核 CPU，45–90 分钟）加速 >6×10⁴ 倍。
- **注**：未明确说明完整训练的总 GPU 小时数，但可从 300 epoch、batch 16、单个 A100 推断训练时间在数十小时量级。

## 5. 实验数量与充分性

- **实验数量**：
    - 主要结果基于 81 个保留测试模拟（≈3,155 个快照）。
    - 包含全管道重建（Stage‑1 + Stage‑2）、几何目标潜在反演、基于最近邻的基线对比。
    - 附录中提供早期时间编码重建和代表性失败案例。
- **充分性评估**：
    - **优点**：数据集规模（797 个模拟）在同类两相喷嘴仿真研究中较大；模拟级划分防止快照级泄漏；在密度、几何、速度、体积分数上均报告了量化指标，且与最近邻基线对比，证明代理学习到非平凡的结构。
    - **不足**：仅包含单一雷诺‑韦伯数操作条件（$\mathrm{Re}=10^3, \mathrm{We}=5.56\times10^3$）；测试集仅 81 个模拟，统计显著性有限；未进行消融实验（如移除几何正则项、改变潜在维度等）；未将代理预测与独立 CFD 重跑或实验数据对比；未评估对下游粒径分布等可观测量的影响。

## 6. 论文的主要结论与发现

- **核心结论**：AMR 细胞密度场可以作为瞬态两相流几何条件代理建模的**紧凑、可学习的表示**，它捕捉了求解器自适应细化的位置信息，从而能联合编码喷嘴几何与瞬态流演化。
- **具体性能**：
    - 密度重建：测试 MSE 2.74×10⁻³，PSNR 30.2 dB。
    - 几何重建：壁面 MAE 1.13×10⁻²（物理坐标）。
    - Stage‑2 场预测：$f$ 的 MSE 1.33×10⁻³，$u$ 的 MSE 2.50×10⁻³，$v$ 的 MSE 1.60×10⁻³；对应 $R^2$ 分别为 0.69、0.78、0.61。
    - 代理显著优于几何最近邻基线（各通道 MSE 约优 2 倍）。
    - 从几何单独恢复潜在（梯度反演）可达到接近编码器 oracle 的性能，优于最近邻基线。
- **推理效率**：每个轨迹 0.045 秒，加速 >6×10⁴ 倍。

## 7. 优点

1. **创新性表示**：利用 AMR 细胞密度场作为代理，将自适应网格从计算副产品转化为学习信号，避免了直接建模高维多通道流场。
2. **联合建模**：潜在空间同时编码几何与瞬态流演化，支持从几何单独恢复潜在，为逆设计奠定基础。
3. **高计算效率**：推理速度极快，且一次性前向而非自回归，无累积误差。
4. **良好的泛化**：在保留模拟上优于几何最近邻基线，证明学到几何‑流之间的非线性映射。
5. **可查询的几何解码**：几何头部为 1D INR，支持任意分辨率查询，无需重训。
6. **清晰的分阶段验证**：Stage‑1 与 Stage‑2 独立评估，显示密度代理包含足够信息恢复其他变量。

## 8. 不足与局限

1. **参数空间有限**：仅变动三个几何参数（$p_1, p_2, w$），工作条件固定（单一 Re–We）。无法评估对更广泛几何与流动状态的泛化能力。
2. **测试集规模小**：81 个测试模拟可能不足以充分揭示模型在高几何变异性下的行为。
3. **缺乏消融实验**：没有系统评估不同潜在维度、不同编码器/解码器结构、不同损失权重的影响，也未分析时间条件对重建质量的作用。
4. **误差传播**：Stage‑2 独立训练，密度重建误差直接传递给场预测（如薄韧带被过度平滑导致速度/体积分数偏差）。
5. **求解器绑定**：表示依赖 Basilisk 的 AMR 策略，迁移至其他求解器或细化准则未验证。
6. **验证循环不完整**：代理未与独立 CFD 重跑或实验数据对比，也未在逆设计后闭环验证。
7. **高误差案例**：最差 5% 案例显示模型对薄韧带和后期破碎结构解析不足，在高度非线性区域可能不可靠。

（完）

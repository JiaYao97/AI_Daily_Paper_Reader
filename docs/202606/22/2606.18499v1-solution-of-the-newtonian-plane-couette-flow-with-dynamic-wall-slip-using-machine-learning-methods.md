---
title: Solution of the Newtonian plane Couette flow with dynamic wall slip using machine-learning methods
title_zh: 使用机器学习方法求解带有动态壁面滑移的牛顿平面Couette流
authors: "Georgia Foutsitzi, Nikolaos Antoniadis, Georgios C. Georgiou"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.18499v1"
tags: ["query:flow-field"]
score: 9.0
evidence: 使用物理信息神经网络和深度算子网络预测平面库埃特流
tldr: "本文比较了物理信息神经网络（PINN）和数据驱动的深度算子网络（DeepONet）在预测带有动态壁面滑移的牛顿平面Couette流演化中的表现。PINN达到了0.083%的相对L2误差，但需针对每个实例重新训练；DeepONet在未见和分布外信号上分别达到0.36%和0.88%的平均相对误差，推理速度比数值求解器快540倍，比PINN快30.5%，展示了作为高效替代模型的潜力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.18499v1/fig-001.webp\", \"caption\": \"Figure 1. Schematic of the Physics-Informed Neural Network (PINN) architecture used to approximate the velocity field 𝑣(𝑦, 𝑡). The network takes the spatial coordinate (𝑦) and time (𝑡) as inputs and outputs the predicted velocity. Automatic differentiation is employed to compute the required derivatives, enabling enforcement of the governing diffusion equation, initial condition, and boundary conditions directly through the loss function.\", \"page\": 6, \"index\": 1, \"width\": 869, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2606.18499v1/fig-006.webp\", \"caption\": \"Figure 2. Schematic representation of the Deep Operator Network (DeepONet) architecture.\", \"page\": 8, \"index\": 6, \"width\": 708, \"height\": 409}, {\"url\": \"assets/figures/arxiv/2606.18499v1/fig-004.webp\", \"caption\": \"Figure 3. Training convergence of the PINN model showing the evolution of the total loss function. The initial phase (Adam optimizer) achieves rapid reduction of the loss, followed by refinement using the L-BFGS optimizer, which further decreases the residual by approximately one order of magnitude. The transition between optimizers is marked by a temporary increase in loss, after which stable convergence is achieved.\", \"page\": 10, \"index\": 4, \"width\": 669, \"height\": 448}, {\"url\": \"assets/figures/arxiv/2606.18499v1/fig-002.webp\", \"caption\": \"Figure 4. Results for oscillatory Couette flow with 𝐵 = 1, 𝛬𝑠 = 0.5 and 𝑓(𝑡) = sin(4𝜋𝑡): (a) Numerical (CN) velocity; (b) PINN velocity prediction; (c) Absolute 𝐿2 error.\", \"page\": 11, \"index\": 2, \"width\": 500, \"height\": 1217}, {\"url\": \"assets/figures/arxiv/2606.18499v1/fig-009.webp\", \"caption\": \"Figure 5. Comparison of the PINN predictions (dashed) with the reference solution (solid) with 𝐵 = 1, 𝛬𝑠 = 0.5 and 𝑓(𝑡) = sin(4𝜋𝑡): (a) Temporal slices of the velocity field at fixed spatial positions 𝑦; (b) Spatial velocity profiles across the channel at selected time instances.\", \"page\": 12, \"index\": 9, \"width\": 527, \"height\": 852}, {\"url\": \"assets/figures/arxiv/2606.18499v1/fig-011.webp\", \"caption\": \"Figure 6. Training history of the DeepONet model showing the evolution of the mean squared error (MSE) and the relative operator 𝐿2 error over epochs. Both training and validation losses exhibit rapid convergence and remain closely aligned, indicating stable learning and strong generalization.\", \"page\": 16, \"index\": 11, \"width\": 677, \"height\": 406}, {\"url\": \"assets/figures/arxiv/2606.18499v1/fig-012.webp\", \"caption\": \"Figure 7. Distribution of the relative 𝐿2 error for DeepONet predictions on the unseen test set.\", \"page\": 16, \"index\": 12, \"width\": 696, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2606.18499v1/fig-008.webp\", \"caption\": \"Figure 8. Results for a representative sample from the unseen test dataset with 𝐵 = 11.16 and 𝛬𝑠 = 0.12: (a) reference solution; (b) DeepONet prediction; (c) absolute 𝐿2 error.\", \"page\": 17, \"index\": 8, \"width\": 475, \"height\": 1169}, {\"url\": \"assets/figures/arxiv/2606.18499v1/fig-005.webp\", \"caption\": \"Figure 9. Comparison of the DeepONet predictions (dashed lines) with reference solutions (solid lines) with 𝐵 = 11.16 and 𝛬𝑠 = 0.12 : (a) Temporal slices 𝑣(𝑦, 𝑡) at fixed spatial locations; (b) Spatial slices at fixed time instances.\", \"page\": 18, \"index\": 5, \"width\": 548, \"height\": 918}, {\"url\": \"assets/figures/arxiv/2606.18499v1/fig-007.webp\", \"caption\": \"Figure 10. Generalization capability of DeepONet on unseen harmonic signals (𝑓(𝑡) = sin (4𝜋𝑡)). Comparative heatmaps for (a) 𝐵 = 0.1, (b) 𝐵 = 1, (c) 𝐵 = 10 with 𝛬𝑠 = 0.5.\", \"page\": 19, \"index\": 7, \"width\": 859, \"height\": 901}, {\"url\": \"assets/figures/arxiv/2606.18499v1/fig-010.webp\", \"caption\": \"Figure 11. Results for 𝐵 = 10 𝑎𝑛𝑑 𝛬𝑠 = 0.5 demonstrating near-perfect alignment between the reference solution (solid lines) and DeepONet predictions (dashed lines): (a) temporal slices; (b) spatial slices.\", \"page\": 20, \"index\": 10, \"width\": 558, \"height\": 928}, {\"url\": \"assets/figures/arxiv/2606.18499v1/fig-003.webp\", \"caption\": \"Table 3. Performance comparisons between Numerical Solver, PINN, and DeepONet.\", \"page\": 21, \"index\": 3, \"width\": 871, \"height\": 256}]"
motivation: 传统数值方法计算量大，难以用于实时应用，因此需要高效的机器学习方法替代。
method: 采用PINN求解特定参数下的控制方程，并利用高保真数值数据训练数据驱动DeepONet以学习连续解算子。
result: "PINN点对点精度高（相对L2误差0.083%），但需重训练；DeepONet泛化能力强（未见信号误差0.36%，分布外误差0.88%），推理速度提升540倍。"
conclusion: DeepONet可作为高效替代模型，实现快速参数探索和实时流体动力学预测，结合了物理和数据的优势。
---

## 摘要
本研究对物理信息神经网络（PINNs）和数据驱动的深度算子网络（DeepONets）在预测带有动态壁面滑移的平面牛顿Couette流演化方面进行了比较研究。虽然传统的数值方法（如克兰克-尼科尔森格式）具有高精度，但其计算需求在实时应用中带来挑战。为解决这一问题，我们首先实现了一个PINN框架来求解特定物理参数下的控制方程。随后，我们开发了一个数据驱动的DeepONet，基于高保真数值数据进行训练，以学习在广泛的滑移边界条件和上壁速度范围内的连续解算子。我们的结果表明，尽管PINN实现了卓越的点态精度，相对L2误差为0.083%，但它仍受限于需要对每个具体实例重新训练的要求。相比之下，DeepONet在未见和分布外信号上展现出鲁棒的泛化能力，平均相对误差分别为0.36%和0.88%。最值得注意的是，它提供了近乎即时的推理，相对于数值求解器实现了约540倍的加速，相对于PINN实现了30.5%的加速。这项工作展示了基于物理和数据驱动架构之间的协同作用，并将DeepONet确立为用于快速参数探索和实时流体动力学预测的高效代理模型。

## Abstract
This study presents a comparative investigation of Physics-Informed Neural Networks (PINNs) and data-driven Deep Operator Networks (DeepONets) for predicting the evolution of plane Newtonian Couette flow with dynamic wall slip. While traditional numerical methods, such as the Crank-Nicolson scheme, offer high accuracy, their computational demand poses challenges in real-time applications. To address this, we first implement a PINN framework to solve the governing equations for specific physical parameters. Subsequently, we develop a data-driven DeepONet, trained on high-fidelity numerical data, to learn the continuous solution operator across a broad range of slip boundary conditions and upper wall velocities. Our results indicate that while the PINN achieved superior point-wise precision with a relative L_2 error of 0.083%, it remains constrained by the requirement for instance-specific retraining. In contrast, the DeepONet demonstrates robust generalization on unseen and out-of-distribution signals with a mean relative error of 0.36% and 0.88%, respectively. Most notably, it provides near-instantaneous inference, achieving a speedup factor of approximately 540X over the numerical solver and 30.5% over the PINN. This work demonstrates the synergy between physics-based and data-driven architectures and establishes DeepONet as a highly efficient surrogate model for rapid parametric exploration and real-time fluid dynamics forecasting.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：传统数值方法（如Crank-Nicolson格式）求解带有动态壁面滑移的牛顿平面Couette流时精度高，但计算成本高，难以满足实时应用、参数化研究和多查询场景的需求。本研究旨在探索机器学习方法（PINN与DeepONet）作为高效替代方案的可行性。
- **背景**：壁面滑移在聚合物挤出、微流控等领域普遍存在，动态滑移模型（引入滑移松弛参数 $\Lambda_s$）能捕捉瞬态记忆效应。平面Couette流是经典的粘性剪切驱动流动，其控制方程为扩散方程，但动态滑移边界条件增加了求解难度。
- **研究意义**：通过比较PINN（基于物理约束的单实例求解器）与DeepONet（数据驱动的算子学习框架），揭示二者在精度与效率之间的权衡，并验证神经网络算子作为快速代理模型的潜力。

### 2. 方法论

#### 核心思想
- **PINN**：将物理定律（扩散方程、初始/边界条件）嵌入神经网络训练损失函数，通过自动微分计算导数，无需标签数据即可求解指定参数的单个实例。
- **DeepONet**：学习从输入参数空间（上板速度函数 $f(t)$、滑移数 $B$、滑移松弛数 $\Lambda_s$）到速度场 $v(y,t)$ 的连续算子映射。训练后可在毫秒级泛化到未见参数组合。

#### 关键技术细节
1. **PINN架构**
   - 输入：空间坐标 $y$ 和时间 $t$；输出：速度 $v_\theta(y,t)$。
   - 网络：4个隐藏层，每层90个神经元，激活函数为 $\tanh$。
   - 损失函数：$$\mathcal{L} = w_{pde}\mathcal{L}_{pde} + w_{bc0}\mathcal{L}_{bc0} + w_{bc1}\mathcal{L}_{bc1} + w_{ic}\mathcal{L}_{ic}$$
     其中各损失项分别对应PDE残差、下边界动态滑移条件（含 $\Lambda_s$ 和 $B$）、上边界速度条件、初始条件。权重设为 $w_{pde}=w_{bc0}=1$，$w_{ic}=w_{bc1}=10$。
   - 训练策略：先Adam优化器（10,000次迭代，学习率$10^{-4}$），再L-BFGS优化（最多4,000次）。

2. **DeepONet架构**
   - 分支网络：输入为离散化的上板速度函数 $f(t)$（200个时间点）加上标量参数 $\Lambda_s$ 和 $B$，共202维。4个隐藏层，每层192个神经元，激活函数为GELU。
   - 主干网络：输入 $(y,t)$，同样4层×192神经元，GELU激活。
   - 潜在维度 $q=256$，输出为分支与主干输出的点积加偏置：$$\hat{\mathcal{G}}(p)(y,t) = \sum_{k=1}^{q} \beta_k(p) \cdot \tau_k(y,t) + b_0$$
   - 训练：MSE损失，学习率常数$10^{-4}$，蒙特卡洛采样每步4096点，500步/epoch，总共1000 epochs。

### 3. 实验设计

- **数据集/场景**
  - **PINN**：仅针对一个特定参数实例（$B=1$，$\Lambda_s=0.5$，$f(t)=\sin(4\pi t)$），在域 $[0,1]\times[0,1]$ 上随机采样：$N_{pde}=10^4$ 内点，$N_{ic}=3000$ 初始点，$N_{bc}=3000$ 边界点。
  - **DeepONet**：使用Crank-Nicolson数值求解器生成10,000组独特仿真数据。参数范围：$B\in[0.1, 100]$（对数采样），$\Lambda_s\in[0, 0.5]$（线性采样，20%设为0），$f(t)$ 由高斯随机场（RBF核，长度尺度0.2）生成。网格：199空间点×199时间点（共200×200输出）。80%训练，10%验证，10%测试。
- **基准方法**：Crank-Nicolson有限差分方案（CN），作为参考解和训练/测试真值。
- **对比方法**：PINN vs DeepONet vs CN（数值求解器），主要对比预测精度（相对 $L_2$ 误差）和推理速度（ms/解）。

### 4. 资源与算力

- **硬件**：所有计时实验在Google Colab环境下运行，使用NVIDIA T4 GPU。
- **训练时长**（均值±标准差，5次独立运行）：
  - PINN：平均46.83 min（标准差7.33 min）。
  - DeepONet：平均256.65 min（标准差14.91 min）。
- **推理时间**（均值±标准差，基于100次独立运行×20个测试用例）：
  - CN（CPU）：109.4±1.85 ms
  - PINN（GPU）：0.61±0.076 ms
  - DeepONet（GPU）：0.02±0.084 ms
- **备注**：文中未明确说明CPU型号或CN使用何种CPU，也未提及训练DeepONet时是否使用了多GPU。

### 5. 实验数量与充分性

- **实验数量**：
  - PINN：5次独立随机种子运行，统计平均误差和训练时间。
  - DeepONet：5次独立随机种子运行；额外在验证集和测试集（含分布外信号）评估；测试了不同 $B$ 和 $\Lambda_s$ 组合下的表现。
- **充分性评判**：
  - **优点**：使用了多次独立运行以评估统计稳定性；DeepONet测试包含未见信号（保留的测试集）和分布外信号（$\sin(4\pi t)$，未在训练中出现），泛化评估较为严格；参数空间覆盖了三个数量级的滑移数。
  - **局限**：PINN仅测试了一个参数组合，未探讨不同 $\Lambda_s$、$B$ 或 $f(t)$ 对PINN精度的影响；DeepONet的分布外测试仅使用一种正弦信号，可能不足以全面反映模型外推能力；所有数据均来自同一数值求解器，未引入实验噪声或扰动。

### 6. 主要结论与发现

- **PINN**：在单一参数实例上达到**0.083%**的相对 $L_2$ 误差，精度极高，但需**46.83 min**训练，且每次更换参数必须重新训练。
- **DeepONet**：在未见测试集上平均相对误差**0.36%**（最佳0.297%），在分布外正弦信号上达到**0.88%**误差。推理时间仅**0.02 ms**，比CN快**540倍**，比PINN快**30.5倍**。
- **核心权衡**：PINN适合高精度的单实例验证，DeepONet适合实时/多查询场景，作为快速代理模型具有显著效率优势。
- **协同价值**：物理约束（PINN）与数据驱动（DeepONet）可以互补，未来可结合为物理信息DeepONet进一步提升精度与泛化。

### 7. 优点

- **方法创新**：首次将DeepONet应用于具有动态壁面滑移的Couette流，展示了算子学习在非标准边界条件问题中的有效性。
- **实验设计严谨**：多次独立运行统计性能；训练/验证/测试分离；包含分布外泛化测试。
- **架构选择合理**：PINN使用Adam+L-BFGS两阶段优化，DeepONet选用GELU激活函数（优于ReLU），并设计平衡的分支/主干宽度以匹配表达力。
- **效率对比充分**：统一硬件环境下测量推理时间，加速比可信。
- **可视化充分**：提供热图、时间/空间切片、误差分布、收敛曲线等多个角度验证。

### 8. 不足与局限

- **实验覆盖面窄**：PINN仅测试一个参数组合，未检验不同 $B$ 或 $\Lambda_s$ 下PINN的精度变化；DeepONet仅使用GRF生成的 $f(t)$ 训练，分布外测试仅一种正弦信号，泛化范围有限。
- **数据来源单一**：所有参考解来自同一数值求解器，未考虑数值误差或测量噪声的影响，可能高估模型在真实物理数据上的表现。
- **计算资源未充分描述**：CN求解器运行在CPU上，但未说明CPU型号；无法确认CPU基准是否最优（如可改用GPU加速的CN）。
- **消融实验缺失**：未系统分析网络深度、宽度、激活函数、损失权重等超参数对性能的影响；未评估不同采样策略或数据量的敏感性。
- **应用限制**：仅针对一维瞬态扩散问题，未扩展到二维/三维或非线性情况；动态滑移模型仅考虑线性Navier型，未探讨其他非线性滑移规律。

（完）

---
title: Surrogate modeling for convection-dominated parametric problems based on error learning
title_zh: 基于误差学习的对流主导参数问题代理建模
authors: "Lihong Feng, Ali Hassan, Shuwen Sun"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.29769v1"
tags: ["query:flow-field"]
score: 9.0
evidence: 针对对流主导流动问题的降阶建模
tldr: 针对对流主导问题线性模型降阶精度不足的挑战，提出一种混合代理建模方法，结合投影基降阶模型与深度神经网络误差修正，及其非侵入变体。在1D和2D无粘Burgers方程上测试，该方法在线预测精度高于现有方法，且预测时间大幅降低。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.29769v1/fig-002.webp\", \"caption\": \"Figure 3.2: The NN structure of the hybrid ROM\", \"page\": 7, \"index\": 2, \"width\": 596, \"height\": 365}, {\"url\": \"assets/figures/arxiv/2605.29769v1/fig-009.webp\", \"caption\": \"Figure 4.1: The NN structure of the hybrid ROM\", \"page\": 8, \"index\": 9, \"width\": 596, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2605.29769v1/fig-001.webp\", \"caption\": \"Figure 5.1: The FOM solution at µ∗ = 1.08 (left) and the hybrid ROM at µ∗ (right).\", \"page\": 11, \"index\": 1, \"width\": 596, \"height\": 445}, {\"url\": \"assets/figures/arxiv/2605.29769v1/fig-013.webp\", \"caption\": \"Figure 5.2: Left: the FOM solutions (solid lines), the hybrid ROM solutions (dash lines), and the POD-FFNN-e-decoder predictions (dotted lines) at time instances 0.2s, 0.4s, 1s and 2s and at at µ∗ = 1.08. Right: the FOM solutions (solid lines) and the CAE-FFNN predictions (dash-dot lines) at time instances 0.2s, 0.4s, 1s and 2s and at at µ∗ = 1.08\", \"page\": 12, \"index\": 13, \"width\": 979, \"height\": 358}, {\"url\": \"assets/figures/arxiv/2605.29769v1/fig-008.webp\", \"caption\": \"Table 5.2: Runtime: the RBM ROM, the CAE-FFNN, the POD-FFNN-e-decoder, and the hybrid ROM (GROM+1dCNNResi).\", \"page\": 13, \"index\": 8, \"width\": 792, \"height\": 206}, {\"url\": \"assets/figures/arxiv/2605.29769v1/fig-012.webp\", \"caption\": \"Table 5.3: Errors and speedups of different ROMs at testing samples of µ.\", \"page\": 17, \"index\": 12, \"width\": 975, \"height\": 238}, {\"url\": \"assets/figures/arxiv/2605.29769v1/fig-011.webp\", \"caption\": \"Table 5.4: Runtime of the proposed methods\", \"page\": 17, \"index\": 11, \"width\": 844, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2605.29769v1/fig-004.webp\", \"caption\": \"Figure 5.3: The FOM solutions and the surrogate predictions at time instances 0, 5, 10, 15, 20, and 25.\", \"page\": 18, \"index\": 4, \"width\": 829, \"height\": 656}, {\"url\": \"assets/figures/arxiv/2605.29769v1/fig-005.webp\", \"caption\": \"Figure 5.4: The FOM solutions and the surrogate predictions at time instances 0, 5, 10, 15, 20, and 25.\", \"page\": 18, \"index\": 5, \"width\": 829, \"height\": 554}, {\"url\": \"assets/figures/arxiv/2605.29769v1/fig-010.webp\", \"caption\": \"Figure 5.5: The FOM solutions and the surrogate predictions at time instances 0, 5, 10, 15, 20, and 25.\", \"page\": 19, \"index\": 10, \"width\": 829, \"height\": 516}, {\"url\": \"assets/figures/arxiv/2605.29769v1/fig-003.webp\", \"caption\": \"Figure A.4: The 1dCNNResi for the 1D inviscid Burgers’ equation\", \"page\": 22, \"index\": 3, \"width\": 596, \"height\": 320}, {\"url\": \"assets/figures/arxiv/2605.29769v1/fig-006.webp\", \"caption\": \"Figure A.6: Upsampling block used in the upsampling block for the 2D inviscid Burgers’ equation\", \"page\": 23, \"index\": 6, \"width\": 596, \"height\": 211}, {\"url\": \"assets/figures/arxiv/2605.29769v1/fig-007.webp\", \"caption\": \"Figure A.7: The 2dCNNResi for the 2D inviscid Burgers’ equation\", \"page\": 23, \"index\": 7, \"width\": 596, \"height\": 240}]"
motivation: 对流主导问题的Kolmogorov n-width衰减慢，线性降阶模型精度不足，需要更有效的非线性建模方法。
method: 构建投影基降阶模型，并用深度神经网络学习模型误差进行修正；非侵入变体适用于黑箱求解器。
result: 在1D和2D无粘Burgers方程上，混合代理模型的在线预测精度优于现有方法，且预测时间显著减少。
conclusion: 所提混合代理方法易于构建和在线组装，神经网络结构轻量，有效克服了对流主导问题降阶的难点。
---

## 摘要
对流主导问题以柯尔莫哥洛夫$n$宽度衰减缓慢而闻名，并且对模型降阶具有挑战性。在这项工作中，我们提出了一种混合代理建模方法及其非侵入式变体，克服了线性模型降阶方法的一些缺点。所提出的混合代理模型是一种基于投影的降阶模型，并通过深度神经网络学习的误差进行修正。借助深度学习，代理模型的模型组件可以保持在较小的降维尺度上。神经网络组件和模型组件在离线阶段顺序但独立地构建。在线阶段，它们易于耦合以输出解预测。由于混合降阶模型的侵入性，必须获得原始模型的数值离散化算子。对于使用黑盒求解器求解的问题，其中无法获取数值离散化的细节，我们进一步提出了混合代理的非侵入式变体。与现有的具有非线性流形的模型降阶方法相比，所提出的混合降阶模型更容易构建，并且也易于组装用于在线预测。与纯粹基于深度学习的代理建模方法相比，所提出的非侵入式变体具有更轻的神经网络结构，需要学习的参数更少。我们在两个非线性对流参数问题上测试了所提出的方法。第一个是带有一个参数的1D无粘 Burgers 方程，第二个是带有两个参数的2D无粘 Burgers 方程。由于两种方法均基于误差校正，与最先进的方法相比，它们的在线预测具有更高的准确性，同时预测时间大幅减少。

## Abstract
Convection-dominated problems are known for their slow Kolmogorov $n$-width decays and are challenging for model order reduction (MOR). In this work, we propose a hybrid surrogate modeling approach and a non-intrusive variant that overcome some drawbacks of linear MOR methods. The proposed hybrid surrogate model is a projection-based reduced-order model (ROM), corrected by the error learned from a deep neural network. With the aid of deep learning, the model component of the surrogate model can be kept in a small reduced dimension. The neural network component and the model component are sequentially but separately built during the offline stage. At the online stage, they are easily coupled to output the solution predictions. Due to the intrusive nature of the hybrid-ROM, the numerically discretized operators of the original model must be available. For problems solved using black-box solvers, where the details of the numerical discretization are not accessible, we further propose a non-intrusive variant of the hybrid surrogate. Compared to the existing MOR methods with nonlinear manifolds, the proposed hybrid ROM is more easily built and is also easily assembled for online prediction. In contrast to the surrogate modeling approaches purely based on deep-learning, the proposed non-intrusive variant has a lighter neural network structure with much fewer parameters to be learned. We test the proposed methods on two nonlinear convection parametric problems. The first is the 1D inviscid Burgers' equation with one parameter, and the second is the 2D inviscid Burgers' equation with two parameters. Since both methods are based on error correction, their online predictions exhibit higher accuracy yet with largely reduced prediction time, compared to state-of-the-art methods.

---

## 论文详细总结（自动生成）

# 基于误差学习的对流主导参数问题代理建模——论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题背景**：对流主导参数化偏微分方程（如无粘Burgers方程）的解流形通常具有缓慢的Kolmogorov $n$ 宽度衰减，导致基于线性子空间的投影降阶方法（如POD-Galerkin）精度不足，尤其是当降阶维度保持在较小时。
- **现有方法局限**：
  - 空间/快照变换方法多限于一维或稳态问题，或实现复杂。
  - 使用卷积自编码器的非线性压缩方法可能计算开销大。
  - 非线性流形增广的投影ROM（如[5,3]）虽然提高了精度，但离线和在线实现复杂，且其最终精度受限于增广的线性子空间维度。
  - 数据驱动闭合模型有时贡献微弱，且易出现病态最小二乘问题。
- **本文目标**：提出一种简单、易实现的混合代理模型，结合投影基ROM和深度神经网络误差修正，突破线性子空间的精度瓶颈，并给出非侵入变体以适应黑箱求解器。

## 2. 方法论

### 2.1 核心思想
- 混合ROM形式为：
  $$ \tilde{u}(t,\mu) = V z(t,\mu) + \tilde{e}(z(t,\mu)) $$
  其中 $V \in \mathbb{R}^{n \times r}$（$r \ll n$）为降阶投影矩阵（通过POD‑greedy算法获得，$r$ 取较小值），$z(t,\mu)$ 是标准Galerkin‑ROM或LSPG‑ROM的降阶状态，$\tilde{e}(z(t,\mu))$ 是由深度神经网络学习的误差（近似 $u - Vz$）。NN的输入为物理信息驱动的降阶状态，而非直接的时间‑参数对。

### 2.2 关键技术细节
- **模型组件构建**：
  - 采用POD‑greedy算法选择训练参数样本并得到投影矩阵 $\tilde{V}$，然后取前 $r$ 列组成 $V$（或从SVD of $X_s$ 得到）。
  - 模型组件为带有EIM超缩减的Galerkin ROM（或LSPG‑ROM），在线求解低成本。
- **神经网络组件**：
  - **一维问题**：1dCNNResi，含残差块（ResiBlock）和多个上采样块（含嵌入的残差块 ProjResiBlock），构成从 $z$ 到误差 $\tilde{e}$ 的映射。
  - **二维问题**：2dCNNResi，使用ConvTranspose2d上采样层和两个残差块。对超大规模FOM，先对误差进行POD压缩得到 $r_0$ 个系数向量（$r \leq r_0 \ll n$），再用2dCNNResi学习这些系数（称为POD‑2dCNNResi），从而大幅减少NN参数。
- **非侵入变体（POD‑FFNN‑e‑decoder）**：
  - 首先对解快照做SVD，得到POD系数 $z(t,\mu)$。
  - 用前馈神经网络（FFNN）学习参数域和时间域到潜在空间 $z$ 的映射。
  - 同时，用1dCNNResi或2dCNNResi作为误差解码器，输入为FFNN输出的 $\tilde{z}$，输出为预测误差 $\tilde{e}$。
  - 总损失为FFNN损失与解码器损失的加权和（$\alpha_1 \text{Loss}_1 + \alpha_2 \text{Loss}_2$）。
- **离线训练**：模型组件和NN组件顺序但独立构建；各组件所需数据来自FOM在训练参数样本和时间点的解快照（EIM过程副产品）。

## 3. 实验设计

- **使用场景/数据集**：
  - **1D 无粘 Burgers 方程**：空间离散化为 $n=1000$ 点，时间步 $N_t=500$，单参数 $\mu\in[0.9,1.1]$ 控制初始条件。
  - **2D 无粘 Burgers 方程**：$250\times250$ 网格（除去边界后 $n=123,008$），两个参数：$\mu_1\in[4.25,5.50]$（边界条件）、$\mu_2\in[0.015,0.03]$（源项），时间步 $N_t=500$。
- **训练与测试划分**：
  - 1D：训练参数 $\{0.9, 0.95, 1, 1.05, 1.1\}$（5个）；测试参数 $\{0.92, 0.98, 1.02, 1.08\}$（4个）。
  - 2D：训练参数为 $\mu_1$ 和 $\mu_2$ 各取3个等距值构成的网格（共9个）；测试参数3个 $(5.19,0.026)$、$(4.75,0.02)$、$(4.56,0.019)$。
- **基准方法**：
  - RBM G‑ROM（$\tilde{r}=44$ 或 $227$ 等）、标准LSPG‑ROM（$r=10,150,287$）、CAE‑FFNN（卷积自编码器+前馈网络，与[16]类似）。
  - 在2D实验中，间接与使用非线性流形增广的ROM（文献[5,3]）比较（引用其公布的结果）。
- **评估指标**：相对误差 $\epsilon(\mu) = \frac{\sum_i \|u(t_i,\mu)-\tilde{u}(t_i,\mu)\|_2}{\sum_i \|u(t_i,\mu)\|_2}$ 及最大误差 $\epsilon_{\max}$；记录离线训练时间和在线预测时间。

## 4. 资源与算力

- **硬件**：
  - 模型组件（POD‑greedy、EIM、ROM求解）在单核心服务器上运行：2×AMD EPYC 7763 64核，1TB内存。
  - 神经网络训练在桌面工作站上：Ubuntu，31GiB内存，12th Gen Intel i5‑12600K（16核），配备GPU（具体型号未指明）。
- **训练时长**（典型值）：
  - 1D：混合ROM（G‑ROM+1dCNNResi）NN训练约19分钟；CAE‑FFNN 39分钟；POD‑FFNN‑e‑decoder约26分钟。
  - 2D：G‑ROM+2dCNNResi约68小时；G‑ROM+POD‑2dCNNResi约8.6小时；POD‑FFNN‑e‑decoder约12.6小时。
- **离线其他时间**：POD‑greedy和EIM在2D问题中约3.6小时。
- **说明**：论文未给出GPU型号、数量及批大小等详细配置。

## 5. 实验数量与充分性

- **实验数量**：
  - 1D：一组测试（4个参数），展示了精度对比（表5.1）和运行时间（表5.2），并附有可视化图。
  - 2D：一组测试（3个参数），对比了多种ROM（表5.3、表5.4）和可视化。
  - 消融/变体：对比了两个混合ROM变体（G‑ROM+2dCNNResi vs G‑ROM+POD‑2dCNNResi）、非侵入变体，以及经典LSPG‑ROM不同维度。
- **充分性评价**：
  - **优点**：涵盖了不同维度问题（1D和2D），对比了多种最先进方法，并量化了精度和速度。2D实验直接给出了与文献[5,3]的对比（尽管非直接复现）。
  - **不足**：1D测试参数仅4个，2D仅3个，不足以全面验证泛化性；缺少超参数敏感性分析；未进行严格的误差上界分析或理论证明；与非线性流形方法（如[5,3]）的对比仅基于其发表结果，而非在统一代码框架下复现，可能存在实施差异导致的不公平。

## 6. 论文的主要结论与发现

1. **混合ROM显著提升精度**：在1D和2D Burgers方程上，混合ROM（G‑ROM+NN）优于相同降维潜变量的纯数据驱动方法（CAE‑FFNN、POD‑FFNN‑e‑decoder），也优于高维线性ROM（如LSPG‑ROM $r=150$ 或 $r=227$）。
2. **突破线性子空间精度限制**：混合ROM通过直接学习误差，可达比LSPG‑ROM（$r=150$）更好的精度，而后者是文献[5,3]非线性流形方法的上限，因此混合ROM超过了这些非线性流形方法。
3. **在线预测极快**：2D问题中G‑ROM+POD‑2dCNNResi的在线预测比FOM快约898倍，POD‑FFNN‑e‑decoder快约1945倍，远快于标准LSPG‑ROM（$r=10$ 仅10倍）。
4. **非侵入变体适用性强**：POD‑FFNN‑e‑decoder仅需数据，无需接触数值算子，精度接近侵入式混合ROM，适合黑箱求解器。
5. **神经组件轻量**：由于输入为物理信息的潜在状态，NN训练快速且参数较少（1D下19分钟 vs CAE 39分钟）。

## 7. 优点

- **结构简单易实现**：仅需标准MOR方法和一个解码器NN，无需复杂的非线性流形或耦合机制。
- **物理信息驱动**：NN输入为ROM降阶状态，而非原始时间‑参数，使训练更高效且泛化更好。
- **便于超缩减**：模型组件可方便地通过EIM/DEIM超缩减，整体在线效率高。
- **非侵入变体**：提供灵活性，适用于商业或封闭源码求解器。
- **多参数扩展性**：2D算例展示了双参数下的有效性。

## 8. 不足与局限

- **离线训练开销仍大**：2D问题中POD‑2dCNNResi训练需8.6小时，2dCNNResi需68小时，对于更大规模问题可能不可接受。
- **非侵入变体精度较低**：在1D和2D中，POD‑FFNN‑e‑decoder的误差比混合ROM高一个数量级，且2D末时间步出现明显振荡。
- **实验覆盖有限**：测试参数仅有3–4个，未在更广泛的参数域上评估泛化能力；未进行统计性重复实验。
- **理论分析缺失**：未提供严格的误差界或收敛性分析；神经网络训练依赖启发式。
- **对比公平性存疑**：与非线性流形方法的对比基于引用文献中的数值，而非在同一代码库中复现；可能因实现细节（如超参数、时间积分方案）不同导致偏差。
- **应用限制**：目前仅测试了无粘Burgers方程（双曲型守恒律），在更复杂系统（如纳维‑斯托克斯、湍流）上的表现未知。
- **超参数敏感性**：$r$、$r_0$、损失权重 $\alpha_1,\alpha_2$ 等选择未提供调优指南。

（完）

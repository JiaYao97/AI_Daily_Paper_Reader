---
title: "Guided Flow Matching for Forward and Inverse PDE Problems with Sparse Observations: Algorithm and Theory"
title_zh: 引导流匹配用于稀疏观测的前向和逆PDE问题：算法与理论
authors: "Xifeng Zhang, Jin Zhao"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25509v1"
tags: ["query:flow-field"]
score: 9.0
evidence: 从稀疏观测重建PDE解
tldr: 针对稀疏观测下PDE正向模拟和逆推问题，提出FM4PDE框架，通过流匹配学习系数与解的联合分布，推理时用复合损失引导采样（确定性/随机/混合）。理论证明确定性优化器在强制条件下对数复杂度，随机采样器自适应引导下多项式时间误差界，并指出常数引导的不可消除偏差。实验比扩散模型更快更准。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25509v1/fig-001.webp\", \"caption\": \"Figure 1: Framework of FM4PDE. The framework consists of two phases: (i) a training phase, where a Flow Matching network is trained on paired PDE coefficients (or initial states) and solutions (or final states); and (ii) a sampling phase, where the pretrained model is frozen and used for inference.\", \"page\": 6, \"index\": 1, \"width\": 877, \"height\": 698}, {\"url\": \"assets/figures/arxiv/2605.25509v1/fig-004.webp\", \"caption\": \"Table 1: Comparative analysis of relative errors between FM4PDE, DeepONet, and FNO for forward and inverse problems across various PDEs under full observations. The number of steps for the FM4PDE algorithm is set to 1000.\", \"page\": 39, \"index\": 4, \"width\": 582, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2605.25509v1/fig-007.webp\", \"caption\": \"Table 2: Comparative analysis of relative errors between FM4PDE and DiffusionPDE for forward and inverse problems across various PDEs under sparse observations. The number of steps for both algorithms is set to 1000.\", \"page\": 40, \"index\": 7, \"width\": 527, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2605.25509v1/fig-010.webp\", \"caption\": \"Figure 3: Reconstruction of the reaction-diffusion equation from 500 sparse observation points using FM4PDE.\", \"page\": 41, \"index\": 10, \"width\": 667, \"height\": 525}, {\"url\": \"assets/figures/arxiv/2605.25509v1/fig-009.webp\", \"caption\": \"Figure 4: Reconstruction of the shallow-water equations from 500 sparse observation points using FM4PDE.\", \"page\": 41, \"index\": 9, \"width\": 720, \"height\": 435}, {\"url\": \"assets/figures/arxiv/2605.25509v1/fig-005.webp\", \"caption\": \"Figure 5: Comparison of sampling times between FM4PDE and DiffusionPDE for solving forward problems of different equations.\", \"page\": 42, \"index\": 5, \"width\": 709, \"height\": 411}, {\"url\": \"assets/figures/arxiv/2605.25509v1/fig-006.webp\", \"caption\": \"Figure 6: Comparison of sampling times between FM4PDE and DiffusionPDE for solving inverse problems of different equations.\", \"page\": 42, \"index\": 6, \"width\": 709, \"height\": 412}, {\"url\": \"assets/figures/arxiv/2605.25509v1/fig-003.webp\", \"caption\": \"Figure 7: Reconstruction results of FM4PDE on the Burgers equation given 10 and 20 time segments.\", \"page\": 43, \"index\": 3, \"width\": 712, \"height\": 251}, {\"url\": \"assets/figures/arxiv/2605.25509v1/fig-002.webp\", \"caption\": \"Figure 8: The sampling results of solving the Poisson equation using FM4PDE at different step sizes.\", \"page\": 43, \"index\": 2, \"width\": 814, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2605.25509v1/fig-008.webp\", \"caption\": \"Table 3: Global relative error (Loss), observation mean squared error (ObsLoss), and PDE mean squared error (PDELoss) for various sampling strategy mixing ratios on the Poisson equation sparse recovery task.\", \"page\": 44, \"index\": 8, \"width\": 794, \"height\": 256}]"
motivation: 从稀疏观测数据中高效且准确地重构PDE解是核心挑战，现有生成模型缺乏理论保证且推理慢。
method: 基于流匹配生成模型学习PDE系数与解的联合分布，推理时通过复合损失（稀疏观测一致性和PDE残差）引导采样，支持确定性、随机及混合采样器。
result: 确定性采样器在强制条件下达到对数复杂度误差界；随机采样器自适应引导下获得多项式时间误差界，且常数引导存在正偏差；混合分析提供额外理论保证。实验中比扩散模型推理更快且精度更高。
conclusion: 所提引导流匹配框架为稀疏观测PDE问题提供了高效算法和严格理论保障，优于现有扩散模型。
---

## 摘要
从稀疏观测中重建PDE解是科学计算中的核心挑战。我们提出了FM4PDE，一个流匹配生成框架，学习PDE系数（或初始状态）与解（或最终状态）的联合分布，使得在有限的配对数据下既能进行前向模拟也能进行逆向恢复。在推理时，通过一个复合损失引导采样，该损失强制与稀疏测量一致并减少PDE残差；我们支持确定性、随机性和混合采样器。我们为这些引导过程提供了误差保证。对于确定性优化器，一个强制有界性条件确保轨迹有界，并且相位收缩导致目标精度中的对数复杂度。对于随机采样器，我们引入自适应引导并假设速度场的耗散性以获得与噪声基底参数无关的均匀矩有界性。这导致了多项式时间误差界，一个匹配的下界表明恒定引导会导致不可避免的正偏差，从而激发自适应性。还提供了确定性-随机混合分析。在静态和时变基准PDE上的实验表明，与基于扩散的生成模型相比，具有竞争力的准确性和更快的推理速度。

## Abstract
Reconstructing PDE solutions from sparse observations is a core challenge in scientific computing. We present FM4PDE, a flow-matching generative framework that learns the joint distribution of PDE coefficients (or initial states) and solutions (or final states), enabling both forward simulation and inverse recovery with limited paired data. At inference, sampling is guided by a composite loss that enforces agreement with sparse measurements and reduces the PDE residual; we support deterministic, stochastic, and hybrid samplers. We provide error guarantees for these guided procedures. For the deterministic optimizer, a coercivity condition ensures trajectory boundedness and a phase-wise contraction yields logarithmic complexity in the target accuracy. For the stochastic sampler, we introduce adaptive guidance and assume dissipativity of the velocity field to obtain uniform moment bounds independent of the noise-floor parameter. This leads to polynomial-time error bounds, and a matching lower bound shows constant guidance induces an unavoidable positive bias, motivating adaptivity. A hybrid deterministic-stochastic analysis is also provided. Experiments on static and time-dependent benchmark PDEs demonstrate competitive accuracy and faster inference than diffusion-based generative models.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

从稀疏观测中重建偏微分方程（PDE）的完整解场是科学计算中的核心挑战。实际应用中，由于传感器限制或测量成本，系统状态往往只能在少量时空位置被观测。传统数值方法和监督学习方法（如物理信息神经网络PINNs、神经算子FNO、DeepONet等）在全场或密集观测下表现良好，但直接处理稀疏观测时面临病态问题，需要概率性处理。生成模型为这类问题提供了合理框架，而流匹配（Flow Matching）相比扩散模型具有训练稳定、采样高效的优点。本文提出FM4PDE，通过流匹配学习PDE系数/初始条件与解/终态的联合分布，并在采样时利用稀疏观测和PDE残差引导生成，实现前向模拟和逆推，同时提供了严格的收敛性理论保证。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

核心思想是：预训练一个流匹配网络 $u^\theta_t$，学习从高斯噪声到PDE系数/初始条件和解/终态的联合分布的映射。推理时，通过一个复合损失 $L = \zeta_{\text{obs}} L_{\text{obs}} + \zeta_{\text{pde}} L_{\text{pde}}$ 引导采样过程，其中 $L_{\text{obs}}$ 为观测损失（强制生成结果与稀疏测量一致），$L_{\text{pde}}$ 为PDE残差损失（强制物理一致性）。基于此，文章设计了三种采样策略：

- **确定性采样器**：先执行标准Euler步得到预测状态 $\tilde{x}_{k+1} = x_k + \Delta t_k u^\theta_{t_k}(x_k)$，然后利用引导系数 $b_t = 1/t - 1$ 和梯度修正：$x_{k+1} = \tilde{x}_{k+1} - \Delta t_k b_{t_k} (\zeta_{\text{obs}} \nabla L_{\text{obs}} + \zeta_{\text{pde}} \nabla L_{\text{pde}})$。为处理 $t=0$ 处的奇异性，采用 $\varepsilon$ 稳定化或跳过初始步。
- **随机采样器**：先预测端点 $\hat{x}^{(k)}_1 = x_k + (1 - t_k) u^\theta_{t_k}(x_k)$，采样新噪声 $\xi_k \sim N(0, I)$，插值得到 $\tilde{x}_{k+1} = (1 - t_{k+1}) \xi_k + t_{k+1} \hat{x}^{(k)}_1$，然后施加自适应引导 $\zeta_k = c_\zeta (1 - t_k)$ 进行修正：$x_{k+1} = \tilde{x}_{k+1} - \zeta_k (\zeta_{\text{obs}} \nabla L_{\text{obs}}(\hat{x}^{(k)}_1) + \zeta_{\text{pde}} \nabla L_{\text{pde}}(\hat{x}^{(k)}_1))$。
- **混合采样器**：设置时间阈值 $t^*$，在 $t < t^*$ 阶段使用确定性采样器（利用几何时间网格以满足稳定性和快速收缩），之后切换到随机采样器（均匀网格）进行探索和鲁棒性。

算法流程包括训练阶段（在配对数据上最小化条件流匹配损失 $L_{\text{CFM}}(\theta)$）和采样阶段（冻结网络参数，根据观测和PDE引导生成）。论文还提供了时间网格策略（确定性阶段用 $\Delta t_k = \eta t_k$，随机阶段用等步长）和梯度裁剪以保证数值稳定性。

### 3. 实验设计：数据集/场景、基准、对比方法

- **数据集/场景**：涉及多种静态和时变PDE：
  - 静态：Darcy流动、Poisson方程、Helmholtz方程。
  - 时变：Navier-Stokes方程（涡度形式，$Re=1000$）、浅水方程（SWE）、反应-扩散方程（FitzHugh-Nagumo）、Burgers方程。
  系数场和初始条件从高斯随机场（GRF）采样，训练和测试采用不同GRF设置以评估泛化能力。每个PDE生成50000训练样本。
- **基准**：在全观测设置下对比FNO（傅里叶神经算子）和DeepONet；在稀疏观测设置下对比DiffusionPDE（基于扩散的生成模型）。还进行了不同步长、不同观测稀疏度、不同采样策略的消融实验。
- **对比方法**：FNO和DeepONet通过PDEBench和DeepXDE实现；DiffusionPDE为直接比较对象。评估指标采用相对误差 $\epsilon_{\text{rel}} = \|x_{\text{pred}} - x_{\text{true}}\|_2 / \|x_{\text{true}}\|_2$，在每个测试集上取20次独立运行的平均值。

### 4. 资源与算力

论文明确说明所有实验在**双NVIDIA A100 GPU（80GB）** 上进行。训练数据生成使用了标准数值方法（有限差分等），但未给出具体训练时长。实验中的采样时间比较（FM4PDE vs DiffusionPDE）显示FM4PDE在多数情况下更快（例如Darcy正向问题，2000步下FM4PDE用时392秒 vs DiffusionPDE 659秒）。

### 5. 实验数量与充分性

实验覆盖了7种不同PDE（Darcy、Poisson、Helmholtz、Navier-Stokes、反应-扩散、浅水、Burgers），每个都包含前向和逆问题（部分）。在全观测对比中，与FNO和DeepONet进行了4个PDE的对比；在稀疏观测对比中，与DiffusionPDE进行了4个PDE的对比（表1、表2）。此外，还有Burgers方程的两种稀疏模式重建（图2），以及反应-扩散和浅水方程的多通道重建（图3、图4）。消融实验包括不同观测稀疏度（图7）、不同步长（图8）和不同采样策略组合（表3）。总体实验数量较多，对比全面，涵盖了多种PDE类型和任务场景。测试集使用独立生成的数据，并取20次平均，结果具有统计意义。客观性方面，训练和测试采用了不同GRF分布，避免了过拟合。没有发现明显的不公平对比，但未对理论假设进行数值验证（如PL条件、耗散性等）。

### 6. 论文的主要结论与发现

- FM4PDE在稀疏观测下能够有效重建全局PDE解，并且在全观测下也优于或媲美FNO和DeepONet。
- 相比于扩散模型（DiffusionPDE），FM4PDE在多数PDE上实现了更低或相当的相对误差，且采样速度更快（图5、图6）。
- 理论分析表明：确定性采样器在强制条件下具有对数复杂度 $O(\log(1/\varepsilon))$；随机采样器在自适应引导下达到 $O(\varepsilon^{-2} \log(1/\varepsilon))$ 复杂度，且证明了常数引导会导致不可消除的正偏差（下界 $\delta_{\min}/40$），从而说明自适应引导的必要性；混合分析结合了两者优势，同样达到 $O(\varepsilon^{-2} \log(1/\varepsilon))$。
- 实验中发现纯随机采样器性能最优，其次为确定性→随机混合；纯确定性或随机→确定性较差，这与理论一致。

### 7. 优点：方法或实验设计上的亮点

- **理论贡献**：首次为引导流匹配解决PDE问题提供了严格的收敛性分析，包括对数复杂度、多项式时间误差界以及引导强度导致的偏差下界，证明了自适应引导的必要性。理论推导完整，假设合理（耗散性、PL条件等）。
- **方法创新**：结合稀疏观测损失和PDE残差损失，设计三种采样策略（确定性、随机、混合），并针对各策略特点采用不同时间网格（几何网格处理奇异性）。自适应引导避免了常数引导的正偏差。
- **实验全面**：覆盖多种经典PDE，包括静态和时变、单通道和多通道；与多个基线对比，性能指标清晰；消融实验分析了观测稀疏度、步长和采样策略的影响。可视化结果直观展示了重建效果。
- **效率优势**：相比扩散模型，流匹配采样更快（尤其是少量步数时），且理论保证更充分。

### 8. 不足与局限

- **时间演化处理有限**：本框架目前仅处理初始状态和最终状态之间的映射，对于需要完整时间演化轨迹的高维PDE，未集成连续时间动力学，限制了其在更精细时间依赖问题上的应用。
- **超参数依赖**：引导权重 $\zeta_{\text{obs}}, \zeta_{\text{pde}}$ 以及自适应系数 $c_\zeta$ 需要手动调节，虽然理论给出了稳定性条件，但实际选择仍依赖于问题，缺乏自动推断方法。
- **未验证所有理论假设**：论文假设速度场满足耗散性、损失满足PL条件等，但实验中没有检查这些条件在具体PDE上是否成立，对于不满足假设的问题，理论保证可能失效。
- **泛化边界**：训练和测试采用不同GRF分布，但均来自同类随机场；对于完全未知的PDE结构或观测模式，模型泛化能力有待进一步验证。
- **计算资源报告不完整**：未给出训练时长、内存消耗等详细信息，仅提供了采样时间对比。

（完）

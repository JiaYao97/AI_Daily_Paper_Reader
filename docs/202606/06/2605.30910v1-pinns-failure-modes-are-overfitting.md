---
title: PINNs Failure Modes are Overfitting
title_zh: PINNs失效模式是过拟合
authors: "Nigel T. Andersen, Takashi Matsubara"
date: 2026-05-29
pdf: "https://arxiv.org/pdf/2605.30910v1"
tags: ["query:flow-field"]
score: 8.0
evidence: PINN失效模式过拟合研究，适用于流体流动偏微分方程
tldr: 物理信息神经网络（PINNs）在求解偏微分方程时有时会因过拟合而失败，即使损失函数在配置点上很低，其他位置却误差较大。本文通过可视化残差揭示了这一失败模式，并证明正则化可消除问题。此外，作者扩展了双重反向传播，在全残差集上应用，在标准失败模式方程上以更少的配置点和普通架构实现了最先进性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.30910v1/fig-002.webp\", \"caption\": \"Figure 1: Exact solution to the wave equaiton (a), compared to the PINNs failure mode (b). Unregularized PINNs (b)-(d) minimize the residual only at the collocation points, resulting in failure to learn the solution even though the objective loss has been minimized. Regularizing with double backpropagation eliminates this, resulting in successful training of vanilla PINNs (e)-(g).\", \"page\": 2, \"index\": 2, \"width\": 824, \"height\": 307}, {\"url\": \"assets/figures/arxiv/2605.30910v1/fig-006.webp\", \"caption\": \"Figure 3: Failure and success for the convection equation at FP32. Exact solution is shown in (a), while the unregularized version is shown in (b), and the regularized double PINN in (d). The domain residual over the highlighted segment in (b) and (d) is plotted for vanilla PINNs in (c), and double PINN in (e). Each curve is the domain residual as a function of t for a given x value. The collocation points are indicated by the vertical line. Unregularized PINN show a clear preference for reducing the residual at the collocation point, while double PINN remains smooth and shows no such preference.\", \"page\": 5, \"index\": 6, \"width\": 816, \"height\": 345}, {\"url\": \"assets/figures/arxiv/2605.30910v1/fig-001.webp\", \"caption\": \"Figure 4: Comparison of the solution to the Allen-Cahn equation (a), with PINNs (b)-(d). Some training issues are caused by stiff training dynamics, rather than failure. This is indicated in (b), where the test and train loss track each other closely, yet optimization stalls due to the stiffness of the equation. An upgrade to FP64 (c) fixes this, but overfitting during later optimization reduces the accuracy. Combining double PINNs with FP64 in (d) achieves the best performance.\", \"page\": 6, \"index\": 1, \"width\": 825, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2605.30910v1/fig-003.webp\", \"caption\": \"Figure 5: Regularization removes PINNs failure modes, while FP64 improves optimization. Combining both allows for a simple PINN architecture that achieves state of the art with few collocation points. Equations (a)-(c) trained for the full number of iterations, while (d) existed early after the train loss plateaued (final iteration indicated by the virtual line).\", \"page\": 7, \"index\": 3, \"width\": 825, \"height\": 616}, {\"url\": \"assets/figures/arxiv/2605.30910v1/fig-004.webp\", \"caption\": \"Table 1: Comparison of double PINN at FP64 with the current top performing methods\", \"page\": 8, \"index\": 4, \"width\": 660, \"height\": 610}, {\"url\": \"assets/figures/arxiv/2605.30910v1/fig-005.webp\", \"caption\": \"Figure 6: Comparison of vanilla PINNs on the β = 50 convection equation at FP32. (a) unregularized PINNs fails due to overfitting of the residual. (b) Double PINNs succeed after a little over 5000 iterations of L-BFGS optimization. (c) L2 regularized PINNs also succeeds, but requires more iterations, and maintains a higher test loss. (d) L1 PINNs avoid failure from overfitting, but are difficult to optimize, resulting in large error. Final optimization steps are indiciated by the virtical line in the loss curves, and the final loss is indicated by the faded lines to enable easier visual comparison.\", \"page\": 9, \"index\": 5, \"width\": 818, \"height\": 616}]"
motivation: 揭示PINNs失败的根本原因是过拟合，而非普遍认为的优化或架构问题。
method: 通过直接可视化残差证明过拟合，并采用正则化及扩展双重反向传播在全残差集上训练。
result: 在四个标准失败模式方程上，以最多23倍更少的配置点和原始架构实现了最先进性能。
conclusion: PINNs的失败模式本质是过拟合，正则化和全残差双重反向传播可有效解决。
---

## 摘要
物理信息神经网络（PINNs）是一类常见的基于机器学习的偏微分方程（PDE）求解器，通过最小化编码PDE的残差损失来训练网络表示解。尽管取得了成功，但已知它们在某些简单方程上会失败，尽管损失很低却收敛到错误的解。这些失效模式在过去几年中引起了文献中的广泛关注，激发了基于架构和优化的解决方案。通过直接可视化残差，我们展示了失效模式是过拟合的结果：损失在配置点上被最小化，但在其他地方并非如此。应用正则化会导致失效模式消失。最后，我们将双重反向传播扩展到整个残差集，并使用它在四个标准失效模式方程上实现了最先进的性能，同时使用了最多减少$23\times$的配置点和普通架构。

## Abstract
Physics-Informed Neural Networks (PINNs) are a common class of machine learning-based partial differential equation (PDE) solvers which train a network to represent a solution by minimizing a residual loss that encodes the PDE. Despite their successes, they are known to fail on certain simple equations, converging to an incorrect solution despite low loss. These failure modes have garnered significant attention in the literature over the past several years, motivating both architectural and optimization based solutions. By directly visualizing the residual, we show that failure modes are the result of overfitting: the loss is minimized on the collocation points, but not elsewhere. Applying regularization causes the failure modes to vanish. Finally, we extend double backpropagation over the full set of residuals, and use it to achieve state-of-the-art performance on four standard failure mode equations with up to $23\times$ fewer collocation points and a vanilla architecture.
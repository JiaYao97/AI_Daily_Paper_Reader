---
title: "GraphGP: Scalable Gaussian Processes with Vecchia's Approximation"
title_zh: GraphGP：基于Vecchia近似的可扩展高斯过程
authors: "Benjamin Dodge, Philipp Frank, Susan E. Clark"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.11402v1"
tags: ["query:flow-field"]
score: 6.0
evidence: 可扩展高斯过程用于连续场建模
tldr: 高斯过程因二次计算和存储成本而受限。本文提出GraphGP，基于Vecchia近似的GPU算法，通过位反转k-d树排序实现高效邻居搜索和批并行，结合CUDA实现，达到近十亿参数规模的线性时间和内存需求，支持任意点分布。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11402v1/fig-001.webp\", \"caption\": \"Figure 1: Evaluation of GraphGP order. Left: Illustration of our bit-reversed order and the standard binary k-d tree order. Consecutive points in our order are far away from each other in space and likely to be conditionally independent. Center: Batch sizes for N = 104 points drawn from a unit Gaussian in 3d with k = 16 and n0 = 50. We choose this test distribution as it is irregular and offers three orders of magnitude in dynamic range while being small enough for dense matrix operations. We compare the GraphGP order, standard tree order, random order, x-coordinate sorted order, and the “max-min” order proposed by Guinness [2018]. Our order offers the largest parallel batches while simultaneously enabling scalable preceding neighbor searches. Right: Accuracy as measured by the Kullback–Leibler divergence from truth for a Matérn covariance with unit variance, cutoff ρ = 10, and smoothing parameter ν. While the quantitative results will depend on the point distribution and covariance assumed, the impact of order on accuracy is minor compared to the number of neighbors used, justifying our focus on favorable scaling properties.\", \"page\": 3, \"index\": 1, \"width\": 797, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2606.11402v1/fig-002.webp\", \"caption\": \"Figure 2: Performance of GraphGP as a function of N and k. Points are drawn from a unit Gaussian in 3d with n0 = 1000. All experiments were performed on an H100 GPU (80 GB) with 32-bit floats, increasing N until memory was exhausted. The large improvements that CUDA offers for the forward pass are due to on-the-fly matrix operations described in Section 2.3.\", \"page\": 4, \"index\": 2, \"width\": 825, \"height\": 273}]"
motivation: 高斯过程朴素计算复杂度高，需高效近似方法以处理大规模数据。
method: 提出GraphGP算法，使用位反转k-d树排序优化邻居搜索，并基于CUDA实现可微分的Vecchia近似。
result: 算法可扩展至近十亿参数，具有线性时间和内存需求，大幅优于纯JAX基线。
conclusion: GraphGP为大规模高斯过程推理提供了高效的可微分构建块。
---

## 摘要
高斯过程是建模连续场的强大工具，但其朴素的$\mathcal{O}(N^3)$计算成本和$\mathcal{O}(N^2)$内存需求常常限制其实用性。Vecchia近似是一种针对平稳衰减核的稀疏精度矩阵近似，它将每个点仅依赖于其$k$个最近邻。我们提出了GraphGP，一种用于Vecchia近似的GPU算法，能以线性的时间和内存需求扩展到近十亿参数，并处理大动态范围内的任意点分布。我们的关键贡献包括：(1) 一种比特反转的k-d树排序方法，既能实现高效的邻居搜索，又能最大化批量并行性；(2) 一个可微的CUDA实现，其速度和内存效率显著优于纯JAX基线。GraphGP提供了推理所需的构建模块，包括前向生成、逆应用、对数行列式和核参数导数。

## Abstract
Gaussian processes are a powerful tool for modeling continuous fields, but their naive $\mathcal{O}(N^3)$ computational cost and $\mathcal{O}(N^2)$ memory requirement often limit their practical use. Vecchia's approximation is a sparse precision matrix approximation for stationary, decaying kernels that conditions each point only on its $k$ nearest neighbors. We present GraphGP, a GPU algorithm for Vecchia's approximation that scales to nearly a billion parameters with linear time and memory requirements, handling arbitrary point distributions over a large dynamic range. Our key contributions are (1) a bit-reversed k-d tree ordering that allows efficient neighbor searches while also maximizing batch parallelism, and (2) a differentiable CUDA implementation, which is substantially faster and more memory efficient than our pure JAX baseline. GraphGP provides the building blocks for inference, including forward generation, inverse application, log-determinant, and kernel parameter derivatives.
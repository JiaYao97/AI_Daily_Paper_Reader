---
title: Reducing Experimental Testing in Space Propulsion Film Cooling Analyses by Pixelwise Generative Image Interpolation
title_zh: 在空间推进薄膜冷却分析中通过逐像素生成图像插值减少实验测试
authors: "Adam T. Müller, Philipp J. Teuffel, Konstantin Manassis, Nicolaj C. Stache"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.29911v1"
tags: ["query:q2"]
score: 9.0
evidence: 航天推进中气膜冷却分析的机器学习方法
tldr: "提出一种基于轻量级前馈神经网络和位置编码的机器学习方法，用于从稀疏实验测量中进行图像回归，应用于空间推进薄膜冷却分析。通过实际与合成数据验证，该方法在减少30%测量数据的情况下仍能达到高图像相似度（RMSE<8%，SSIM>93%），并支持知识引导的局部自适应，有效减少实验测试需求，优化冷却剂喷射器配置。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.29911v1/fig-003.webp\", \"caption\": \"Figure 1: Neural network architecture, where (a) shows the full architecture from input parameters to output and (b) depicts a residual block as implemented in (a).\", \"page\": 3, \"index\": 3, \"width\": 983, \"height\": 189}, {\"url\": \"assets/figures/arxiv/2605.29911v1/fig-004.webp\", \"caption\": \"Figure 2: Schematic diagram of the proposed neural network, illustrating (left) the training phase, where the network learns on sampled pixels from ground truth images, and (right) the inference phase, where the trained model generates images conditioned on input parameters of a new operating point (OP).\", \"page\": 3, \"index\": 4, \"width\": 983, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2605.29911v1/fig-005.webp\", \"caption\": \"Figure 3: Experimental setup for conducting cold flow tests of a film cooling setup with a single coolant injector on a semi-opened combustion chamber. (a) shows the full setup whilst (b) provides a close-up view of the combustion chamber, injector and lighting setup.\", \"page\": 4, \"index\": 5, \"width\": 773, \"height\": 297}, {\"url\": \"assets/figures/arxiv/2605.29911v1/fig-007.webp\", \"caption\": \"Figure 7: PixCOIN regression results on real images showing the variation of the injection angle φ. Line (1.) shows extrapolation, where no operating point with φ of 0 was given as training data. Line (2.) shows extrapolation where one single operating point with φ of 0 was given as training data. All red-framed images are training images, whilst all other images were generated using PixCOIN.\", \"page\": 9, \"index\": 7, \"width\": 983, \"height\": 212}, {\"url\": \"assets/figures/arxiv/2605.29911v1/fig-002.webp\", \"caption\": \"Figure 9: Exemplary comparison of real (1.) and generated (2.) image data for two operating points. Column (3.) shows the difference of the foreground of images in (1.) and (2.) in red, with overlays of (3.) with (1.) shown in (4.) and (3.) and (2.) shown in (5.).\", \"page\": 10, \"index\": 2, \"width\": 713, \"height\": 218}, {\"url\": \"assets/figures/arxiv/2605.29911v1/fig-001.webp\", \"caption\": \"Figure 10: Histogram of pixel values in bins of intensity values of multiples of 10 and their contribution to the RMSEValue of the operating point shown in figure 9(a)\", \"page\": 11, \"index\": 1, \"width\": 693, \"height\": 387}, {\"url\": \"assets/figures/arxiv/2605.29911v1/fig-006.webp\", \"caption\": \"Figure 11: Schematic flowchart of the extended training workflow to enable an expert informed adaptation.\", \"page\": 12, \"index\": 6, \"width\": 983, \"height\": 239}]"
motivation: 减少推进系统薄膜冷却研究中大量物理实验的需求，提高优化效率。
method: 采用带位置编码的轻量级前馈神经网络，实现从输入参数到图像的回归，并扩展知识引导的局部自适应。
result: "在减少30%测量数据时，图像RMSE低于8%，SSIM高于93%。"
conclusion: 该方法显著减少所需实验次数，同时保持高质量数据，可推广至其他航空航天应用。
---

## 摘要
我们提出了一种从稀疏实验测量中进行图像回归的机器学习方法。我们展示了该方法在推进系统开发中薄膜冷却研究中的应用，旨在减少对大量物理测试的需求。我们的方法采用带有位置编码的轻量级前馈神经网络，生成由输入参数条件化的图像。在真实和合成数据上验证，它实现了高图像相似度（RMSE < 8%，SSIM > 93%），同时保持准确性，减少了30%的测量。我们进一步提出了一种知识驱动的扩展，用于生成图像的局部适应性。这种方法显著减少了所需测试，同时保留了高质量数据，能够高效优化冷却剂喷射器配置，应用领域超越航空航天。

## Abstract
We propose a machine learning approach for image regression from sparse experimental measurements. We show the application of the proposed method on film cooling studies in propulsion system development, aiming to reduce the need for extensive physical testing. Our method employs a lightweight feed-forward neural network with positional encoding to generate images conditioned by input parameters. Validated on real and synthetic data, it achieves high image similarity (RMSE < 8 %, SSIM > 93 %) while maintaining accuracy with a 30 \% reduction of measurements. We further propose a knowledge-informed extension for local adaptability of the generated images. This approach significantly reduces required tests while preserving high-quality data, enabling efficient optimization of coolant injector configurations with applications beyond aerospace.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在航天推进系统的薄膜冷却研究中，实验测试成本高昂且耗时长。传统方法依赖大量物理实验来探索设计参数空间（如喷射器几何、冷却剂性质、工况），难以高效优化。因此，需要一种数据驱动方法，能够从少量稀疏测量数据中生成高质量、全工况的图像表征，从而减少实验次数，加速热管理系统的设计迭代。
- **整体含义**：本文提出一种基于逐像素生成图像插值的机器学习框架（PixCOIN），旨在通过少量实验数据，预测未见工况下的冷却膜分布图像，并在保持高精度的前提下将实验数据需求降低约30%，为推进系统及其他领域的参数化图像回归提供高效、可解释的解决方案。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将图像生成视为一个像素级的回归任务，输入为**物理参数**（如注射角度、压力等）和**归一化像素坐标**（$x, y$），输出为对应像素的强度（0–255）。通过轻量级前馈神经网络学习从参数空间到图像空间的连续映射，实现条件性图像插值。
- **关键技术细节**：
  - **网络架构**：采用全连接前馈神经网络（FFNN），包含3个密集层和2个残差块，约350万可训练参数（中等模型）。使用ReLU激活函数，输出层不施加sigmoid，仅通过截断确保像素值范围。
  - **位置编码**：显式将归一化像素坐标作为输入特征，使模型直接学习空间依赖模式，避免卷积架构的复杂性和空间连续性假设。
  - **训练方式**：将每张图像分解为**像素级样本**（每个样本包含像素坐标、物理参数、对应像素强度）。训练时随机采样这些样本，防止过拟合。
  - **推理过程**：对指定的物理参数，逐像素枚举全图坐标，生成完整图像。
  - **损失函数**：均方误差（MSE）：
    $$\text{MSE Loss} = \frac{1}{n}\sum_{i=1}^{n}(y_i - \hat{y}_i)^2$$
  - **知识驱动扩展（IML）**：在训练后期（第5个epoch后），利用轻量级CNN分类器检测生成图像中的分割缺陷，通过Grad-CAM定位缺陷区域，对该区域像素使用修正损失：
    $$\text{Loss}_{\text{Ref}} = \frac{1}{n}\sum_{i=1}^{n}(y_i - r)^2$$
    其中 $r$ 为参考前景值，其他像素仍用标准MSE。
- **算法流程**：训练阶段：输入参数+坐标 → 网络 → 预测像素值 → 计算MSE；推理阶段：固定参数，遍历所有坐标 → 组装图像。扩展阶段：生成图像 → 分类器检测 → Grad-CAM定位 → 修正损失。

### 3. 实验设计：数据集、场景、基准及对比方法

- **数据集**：
  - **合成数据**：基于参数函数 $f(x) = u_1 \cdot e^{-(u_2 \cdot (x-u_3))^2} \cdot \cos(u_4 \cdot x) + u_5$ 生成三种变体：(1) 二进制函数绘图，(2) 填充积分表示，(3) 积分灰度梯度。图像尺寸 50×50，外加±5%噪声。仅变化 $u_1, u_2, u_3$ 三个参数。
  - **真实数据**：来自DLR冷流实验装置，使用去离子水+墨水模拟HTP。参数包括：制造方法（2种）、燃烧室直径（4种）、注射器直径（2种）、注射角 $\phi$（6种）、压力（5种），共480个操作点，每点10帧图像，总计4800帧。图像经几何校正、时间平均、二值化分割后缩放到120×84。
- **基准**：未直接对比其他生成模型（如GAN、扩散模型），因为核心目标是**减少实验数据**而非提升绝对生成质量。主要对比自身在不同稀疏程度下的性能。
- **对比方法**：
  - 不同参数设置缩减：质量流量从5档减至3档、2档；注射角从6档减至3档；所有参数截断至最多3档（从480个点减至162个点）；随机减少30%操作点。
  - 插值与外推对比：在外推场景（缺失0°注射角）下评估性能，并与包含一个近邻点的情况对比。
  - 网络规模消融：小（~925K）、中（~3.5M）、大（~6.7M）模型对比。

### 4. 资源与算力

- **计算资源**：使用 **NVIDIA RTX 3500 Ada Generation Laptop GPU**（笔记本级）。
- **训练时长**：
  - 中型模型：每个epoch平均 **7分02秒**，训练50个epoch共计约 **5.9小时**。
  - 大型模型：每个epoch **8分54秒**，总计约 **7.4小时**。
  - 30%缩减数据集训练时间：每个epoch **4分52秒**。
- **推理时间**：
  - 中型模型：每张图像 **5.98秒**。
  - 大型模型：每张图像 **8.77秒**。
- **说明**：文中仅给出了单块GPU的信息，未提及是否使用多卡加速；训练总epoch数固定为50。整体算力要求较低，适合学术研究。

### 5. 实验数量与充分性

- **实验数量**：共计进行了多组实验：
  1. 合成数据上的消融（插值与外推对比，见表3）。
  2. 真实数据上的完整训练（基线）及四种缩减方案（表4、表5）：质量流量3档、2档；注射角3档；所有因素最多3档；随机减少30%操作点。
  3. 外推实验（图7）：比较0°注射角完全缺失与仅含一个近邻点的情况。
  4. 灰度数据定性验证（图8）。
  5. 知识驱动扩展定性展示（图13）。
- **充分性与公平性**：
  - 实验覆盖了多种稀疏程度，从20%到66.25%的数据减少，且报告了RMSE、SSIM、PSNR、余弦相似度、感知哈希**五个指标**，结果具有多维度参考价值。
  - 消融实验对比了网络规模，并选择了最优的中型模型。
  - **潜在不足**：未与其它生成模型（如GAN、扩散模型）进行定量对比；未进行超参数搜索（仅固定学习率0.0001、batch size64等）；真实数据的分割质量有限，可能导致语义偏差。
  - 总体实验设计较为充分，但跨模型对比的缺失使其在“生成质量”上的竞争性论证稍弱。

### 6. 论文的主要结论与发现

- **PixCOIN方法有效**：在完整数据集上，模型生成的图像与真实图像高度相似（RMSE < 7%，SSIM > 94%）。
- **显著减少实验数据**：即使仅使用3个质量流量设置（减少40%参数），RMSE仍低于8%，SSIM高于92%；随机减少30%操作点后，RMSE < 8%，SSIM > 93%。表明模型能高效利用有限测量数据学习物理规律。
- **外推能力有限**：当完全缺失某个参数区域的训练数据时（如0°注射角），生成质量明显下降；增加一个近邻点可小幅改善。
- **知识驱动扩展有效**：在分割缺陷区域（如径向流动区）通过Grad-CAM引导修正损失，能显著改善该区域的生成一致性。
- **误差来源分析**：误差主要出现在前景/背景交界处和动态液滴区域，且多为单像素级偏差，对整体视觉效果影响较小。

### 7. 优点

- **方法轻量、可解释**：使用简单的前馈网络，避免复杂生成模型（GAN/扩散）难以训练、需要大量数据和低可解释性的问题。
- **逐像素回归方案**：将图像生成拆解为像素级任务，无需卷积模块，便于处理不规则或非连续空间结构；可利用专家知识进行像素级修正。
- **数据效率高**：在减少30%–60%实验数据的情况下仍保持高精度，大幅降低测试成本，对资源受限的研发场景价值突出。
- **知识驱动集成**：通过Grad-CAM定位缺陷、修改损失，实现了“人类可介入”的模型修正，增强了工程实用性。
- **通用性**：方法适用于任何参数化的图像回归任务，不仅限于空间推进，可推广至流体可视化、医学成像等其他领域。

### 8. 不足与局限

- **实验对比局限**：未与主流的生成模型（如条件GAN、扩散模型）进行定量比较，难以评估PixCOIN在生成质量上的相对优势。
- **数据质量影响**：真实数据的分割精度有限（尤其是径向流动区），导致部分训练标签存在系统性偏差；使用二值化掩膜生成灰度图会丢失细节信息。作者承认未来需引入深度学习分割。
- **外推能力弱**：当目标参数完全超出训练范围时，性能显著下降；且文中的外推实验仅考察单一参数，未评估多维参数同时外推的情况。
- **图像分辨率限制**：当前模型仅验证了50×50和120×84的分辨率。由于计算复杂度随像素数量线性增长，更高分辨率（如1000×1000）的推理时间可能过长，需要架构优化。
- **仅支持单色输出**：当前只处理灰度图，未扩展到RGB或视频帧序列；虽然理论上可通过修改输出层实现，但缺少实证。
- **资源开销**：尽管训练时间可接受，但推理时每张图像需要逐像素前馈约1万次（对120×84=10,080像素），时间约6秒，对实时应用可能不足。
- **未评估不确定性**：方法本质为确定性回归，未提供预测的不确定性度量，不利于风险敏感应用。

（完）

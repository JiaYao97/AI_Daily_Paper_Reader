---
title: Surrogate modeling for convection-dominated parametric problems based on error learning
title_zh: 基于误差学习的对流主导参数问题代理建模
authors: "Lihong Feng, Ali Hassan, Shuwen Sun"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.29769v1"
tags: ["query:flow-field"]
score: 9.0
evidence: 基于降阶模型和神经网络的输运主导流场代理建模
tldr: 针对对流主导问题Kolmogorov n-width衰减慢导致模型降阶困难，提出基于误差学习的混合代理模型，结合投影降阶模型和深度神经网络误差校正，并发展非侵入变体以兼容黑箱求解器。在1D和2D无粘Burgers方程上测试，相比最新方法，在线预测精度更高且时间大幅减少。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.29769v1/fig-002.webp\", \"caption\": \"Figure 3.2: The NN structure of the hybrid ROM\", \"page\": 7, \"index\": 2, \"width\": 596, \"height\": 365}, {\"url\": \"assets/figures/arxiv/2605.29769v1/fig-009.webp\", \"caption\": \"Figure 4.1: The NN structure of the hybrid ROM\", \"page\": 8, \"index\": 9, \"width\": 596, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2605.29769v1/fig-001.webp\", \"caption\": \"Figure 5.1: The FOM solution at µ∗ = 1.08 (left) and the hybrid ROM at µ∗ (right).\", \"page\": 11, \"index\": 1, \"width\": 596, \"height\": 445}, {\"url\": \"assets/figures/arxiv/2605.29769v1/fig-013.webp\", \"caption\": \"Figure 5.2: Left: the FOM solutions (solid lines), the hybrid ROM solutions (dash lines), and the POD-FFNN-e-decoder predictions (dotted lines) at time instances 0.2s, 0.4s, 1s and 2s and at at µ∗ = 1.08. Right: the FOM solutions (solid lines) and the CAE-FFNN predictions (dash-dot lines) at time instances 0.2s, 0.4s, 1s and 2s and at at µ∗ = 1.08\", \"page\": 12, \"index\": 13, \"width\": 979, \"height\": 358}, {\"url\": \"assets/figures/arxiv/2605.29769v1/fig-008.webp\", \"caption\": \"Table 5.2: Runtime: the RBM ROM, the CAE-FFNN, the POD-FFNN-e-decoder, and the hybrid ROM (GROM+1dCNNResi).\", \"page\": 13, \"index\": 8, \"width\": 792, \"height\": 206}, {\"url\": \"assets/figures/arxiv/2605.29769v1/fig-012.webp\", \"caption\": \"Table 5.3: Errors and speedups of different ROMs at testing samples of µ.\", \"page\": 17, \"index\": 12, \"width\": 975, \"height\": 238}, {\"url\": \"assets/figures/arxiv/2605.29769v1/fig-011.webp\", \"caption\": \"Table 5.4: Runtime of the proposed methods\", \"page\": 17, \"index\": 11, \"width\": 844, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2605.29769v1/fig-004.webp\", \"caption\": \"Figure 5.3: The FOM solutions and the surrogate predictions at time instances 0, 5, 10, 15, 20, and 25.\", \"page\": 18, \"index\": 4, \"width\": 829, \"height\": 656}, {\"url\": \"assets/figures/arxiv/2605.29769v1/fig-005.webp\", \"caption\": \"Figure 5.4: The FOM solutions and the surrogate predictions at time instances 0, 5, 10, 15, 20, and 25.\", \"page\": 18, \"index\": 5, \"width\": 829, \"height\": 554}, {\"url\": \"assets/figures/arxiv/2605.29769v1/fig-010.webp\", \"caption\": \"Figure 5.5: The FOM solutions and the surrogate predictions at time instances 0, 5, 10, 15, 20, and 25.\", \"page\": 19, \"index\": 10, \"width\": 829, \"height\": 516}, {\"url\": \"assets/figures/arxiv/2605.29769v1/fig-003.webp\", \"caption\": \"Figure A.4: The 1dCNNResi for the 1D inviscid Burgers’ equation\", \"page\": 22, \"index\": 3, \"width\": 596, \"height\": 320}, {\"url\": \"assets/figures/arxiv/2605.29769v1/fig-006.webp\", \"caption\": \"Figure A.6: Upsampling block used in the upsampling block for the 2D inviscid Burgers’ equation\", \"page\": 23, \"index\": 6, \"width\": 596, \"height\": 211}, {\"url\": \"assets/figures/arxiv/2605.29769v1/fig-007.webp\", \"caption\": \"Figure A.7: The 2dCNNResi for the 2D inviscid Burgers’ equation\", \"page\": 23, \"index\": 7, \"width\": 596, \"height\": 240}]"
motivation: 对流主导问题中线性模型降阶方法因Kolmogorov n-width衰减慢而效果不佳，需发展更有效的非线性代理模型。
method: 构建混合代理模型，由投影降阶模型与深度神经网络学习的误差校正项组成，离线分别构建，在线耦合输出；非侵入变体不依赖数值离散细节。
result: 在1D和2D无粘Burgers方程上，混合模型在线预测精度高于最新方法，同时预测时间显著降低。
conclusion: 基于误差学习的混合代理模型有效克服了线性降阶的局限，非侵入变体适用于黑箱求解器，为对流主导问题提供高效准确的代理建模方案。
---

## 摘要
对流主导问题以其缓慢的Kolmogorov n-宽度衰减而闻名，对模型降阶（MOR）构成挑战。本文提出了一种混合代理建模方法及其非侵入式变体，克服了线性MOR方法的一些缺点。所提出的混合代理模型是一种基于投影的降阶模型（ROM），通过从深度神经网络学习的误差进行校正。借助深度学习，代理模型的模型组件可以保持在较小的降维维度上。神经网络组件和模型组件在离线阶段按顺序但分别构建。在线阶段，它们易于耦合以输出解预测。由于混合ROM的侵入性，必须获取原始模型的数值离散算子。对于使用黑盒求解器求解的问题（无法获取数值离散细节），我们进一步提出了混合代理的非侵入式变体。与现有采用非线性流形的MOR方法相比，所提出的混合ROM更容易构建，也更容易组装用于在线预测。与纯基于深度学习的代理建模方法相比，所提出的非侵入式变体具有更轻量的神经网络结构，需要学习的参数少得多。我们在两个非线性对流参数问题上测试了所提出的方法。第一个是单参数的一维无粘Burgers方程，第二个是双参数的二维无粘Burgers方程。由于两种方法均基于误差校正，与最先进的方法相比，它们的在线预测具有更高的精度，同时预测时间大幅减少。

## Abstract
Convection-dominated problems are known for their slow Kolmogorov $n$-width decays and are challenging for model order reduction (MOR). In this work, we propose a hybrid surrogate modeling approach and a non-intrusive variant that overcome some drawbacks of linear MOR methods. The proposed hybrid surrogate model is a projection-based reduced-order model (ROM), corrected by the error learned from a deep neural network. With the aid of deep learning, the model component of the surrogate model can be kept in a small reduced dimension. The neural network component and the model component are sequentially but separately built during the offline stage. At the online stage, they are easily coupled to output the solution predictions. Due to the intrusive nature of the hybrid-ROM, the numerically discretized operators of the original model must be available. For problems solved using black-box solvers, where the details of the numerical discretization are not accessible, we further propose a non-intrusive variant of the hybrid surrogate. Compared to the existing MOR methods with nonlinear manifolds, the proposed hybrid ROM is more easily built and is also easily assembled for online prediction. In contrast to the surrogate modeling approaches purely based on deep-learning, the proposed non-intrusive variant has a lighter neural network structure with much fewer parameters to be learned. We test the proposed methods on two nonlinear convection parametric problems. The first is the 1D inviscid Burgers' equation with one parameter, and the second is the 2D inviscid Burgers' equation with two parameters. Since both methods are based on error correction, their online predictions exhibit higher accuracy yet with largely reduced prediction time, compared to state-of-the-art methods.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：对流主导的参数时间依赖问题（如无粘 Burgers 方程）的解流形通常具有缓慢的 Kolmogorov $n$-宽度衰减，导致传统的基于线性子空间的投影模型降阶（MOR）方法（如 POD‑Greedy、Galerkin‑ROM）需要很大的降阶维度才能达到足够精度，而高维 ROM 的计算优势会明显下降。
- **研究动机**：现有非线性流形增强的 ROM 方法（如使用自编码器或二次流形）虽然有望提升精度，但往往需要复杂的离线和在线实现，且精度仍受限于线性子空间的增强维度。同时，纯数据驱动的深度学习代理模型（如 CAE‑FFNN）需要的网络参数多、训练时间长，且无法充分利用问题的物理结构。
- **整体含义**：本文旨在提出一种结构简单、易于实现的混合代理建模框架，通过“标准投影 ROM + 深度学习误差校正”来打破线性降阶的精度上限，并提供适用于黑箱求解器的非侵入式变体。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将代理模型分解为两个部分：
  - **模型组件**：一个低维的投影 ROM（Galerkin‑ROM 或 Least‑Squares Petrov‑Galerkin ROM，通常维数 $r$ 很小，如 $r=5$ 或 $10$），其解为 $\hat{\boldsymbol{u}}(t,\mu) = \boldsymbol{V} \boldsymbol{z}(t,\mu)$。
  - **误差学习组件**：一个深度神经网络（主要为带残差连接的卷积解码器，1dCNNResi 或 2dCNNResi），输入为模型组件产生的降阶状态 $\boldsymbol{z}(t,\mu)$，输出为近似误差 $\tilde{\boldsymbol{e}}(\boldsymbol{z}(t,\mu))$ 的估计。
  - 最终代理解为：
    $$\tilde{\boldsymbol{u}}(t,\mu) = \boldsymbol{V} \boldsymbol{z}(t,\mu) + \tilde{\boldsymbol{e}}(\boldsymbol{z}(t,\mu)).$$
- **关键技术细节**：
  - 模型组件中的投影矩阵 $\boldsymbol{V}$ 可通过 POD‑Greedy 算法或从全快照 SVD 获得，只取前 $r$ 个主导左奇异向量。
  - 误差快照由 FOM 解与 ROM 解的差值构成，用于训练神经网络。
  - 神经网络结构：1D 问题使用 1dCNNResi（Conv1d + 上采样块 + 残差连接）；2D 问题使用 2dCNNResi（ConvTranspose2d + 残差块）。对于高维 FOM，采用 POD‑2dCNNResi：先将高维误差投影到 $r_0$ 个主导 POD 模式上（$r \le r_0 \ll n$），再学习低维系数向量，从而大幅减少网络参数。
  - **非侵入式变体（POD‑FFNN‑e‑decoder）**：不依赖投影 ROM，而是直接从 POD 降维得到潜变量 $\boldsymbol{z}(t,\mu)$，用前馈神经网络（FFNN）学习参数与时间到潜空间的映射，再用 e‑decoder（与混合 ROM 相同的 CNN）校正误差。整体通过加权损失联合训练。
- **算法流程**：离线阶段分别训练模型组件与神经网络组件；在线阶段只需将给定 $\mu$ 和 $t$ 代入模型组件得到 $\boldsymbol{z}(t,\mu)$，再经神经网络得到误差，相加即可得到解。

### 3. 实验设计：数据集 / 场景、基准、对比方法

- **数据集 / 场景**：
  1. **1D 无粘 Burgers 方程**：空间离散 $n=1000$，参数 $\mu \in [0.9,1.1]$ 控制初始条件，时间 $T=0.5$，时间步 $500$。
  2. **2D 无粘 Burgers 方程**：空间离散 $n=123\,008$，参数 $\mu = (\mu_1,\mu_2)$ 分别控制边界条件与源项，$\mu_1 \in [4.25,5.50]$, $\mu_2 \in [0.015,0.03]$，时间 $T=25$，时间步 $500$。
- **基准**：FOM 解作为真值；对比方法包括：
  - 标准 RBM Galerkin‑ROM（G‑ROM）与 LSPG‑ROM（不同维度 $r$）。
  - 文献 [5, 3] 中的非线性流形增强 ROM（精度与速度参考论文数据）。
  - 纯数据驱动 CAE‑FFNN 模型（1D 问题）。
- **测试参数**：1D 问题取 4 个未参与训练的 $\mu$ 值；2D 问题 3 个测试参数点与 [3, 5] 相同，以便直接对比。
- **评价指标**：相对误差 $\epsilon(\mu)$ 及最大误差 $\epsilon_{\text{max}}$；离线训练时间和在线预测时间（壁钟时间）。

### 4. 资源与算力

论文中明确提到的硬件与训练时长：
- **硬件**：
  - 模型组件（ROM 构建）在单核计算服务器上运行（2× AMD EPYC 7763 64‑核处理器，1TB 内存）。
  - NN 训练与测试在 Ubuntu 桌面（31GiB 内存，12th Gen Intel Core i5‑12600K × 16 处理器，并使用了 GPU）上进行。
- **GPU 型号**：未具体说明。
- **训练时长**：
  - 1D 问题：1dCNNResi 训练约 19 分钟（混合 ROM），POD‑FFNN‑e‑decoder 训练约 26 分钟，CAE‑FFNN 训练约 39 分钟。
  - 2D 问题：
    - G‑ROM + 2dCNNResi 训练总计约 68 小时（EIM 3.6h + NN 68h）。
    - G‑ROM + POD‑2dCNNResi 训练总计约 12.2h（EIM 3.6h + NN 8.6h）。
    - POD‑FFNN‑e‑decoder 训练约 12.6h。
- **数据集规模**：1D 训练参数 5 个，2D 训练参数由 3×3 网格共 9 个点，每个参数 500 时间步，快照总数不大。

### 5. 实验数量与充分性

- **实验数量**：
  - 两个完全不同的问题（1D 与 2D），覆盖不同空间维数、参数个数与问题复杂度。
  - 每个问题均对比了 4 种以上方法（包括不同维度的 ROM、CAE‑FFNN、非侵入变体等）。
  - 2D 问题还提供了与文献 [3,5] 中非线性流形 ROM 的间接对比（通过相同测试参数与误差范围）。
- **充分性评价**：实验设计较为充分，验证了混合 ROM 及其变体在精度和速度上的优势，并与多个基准进行对比。**缺点**：未进行消融实验（如改变 $r$ 的影响、NN 结构变体等）；2D 问题只给出了总误差与在线速度，未详细展示所有测试参数下的结果表格（仅有 3 个测试点）；未系统分析训练数据量对性能的影响。

### 6. 论文的主要结论与发现

- **混合 ROM（G‑ROM + NN）** 在 1D 和 2D 问题上均达到与高维线性 ROM（如 $r=44$ 或 $r=227$）相当的精度，但降阶维度仅 $r=5$ 或 $10$，在线预测时间大幅减少（如 2D 问题中加速比达 898×）。
- **非侵入式变体（POD‑FFNN‑e‑decoder）** 精度略低于混合 ROM，但优于纯数据驱动 CAE‑FFNN，且适用于黑箱求解器，提供了更大的灵活性。
- **对比非线性流形增强 ROM**：混合 ROM 精度超越了后者（2D 问题中 $\epsilon_{\text{max}}$ 为 $7.6\times10^{-3}$ vs. 文献 [3,5] 中的 $1.83\times10^{-2}$ 等），同时在线速度更快（加速比 898× vs. 文献中报告的约 10×～100×）。
- **NN 训练效率**：由于输入是物理降阶状态，NN 收敛快（几百至几千 epoch），网络参数量远少于直接学习解的纯 NN 模型。

### 7. 优点

1. **结构简洁**：混合 ROM 仅为线性组合 $Vz + \tilde{e}(z)$，无需复杂非线性流形或超缩减操作。
2. **物理信息注入**：NN 的输入为 ROM 降阶状态，保留了问题的物理拓扑，使得训练快速且精度高。
3. **非侵入式扩展**：不依赖 FOM 算子细节，可适用于商业黑箱软件。
4. **对比同类方法效率高**：POD‑2dCNNResi 借助 POD 预压缩误差，大幅减少 NN 参数，训练时间从 68h 降至 8.6h。
5. **实验对比全面**：不仅与标准线性 ROM 对比，还间接与代表性非线性方法（[3,5]）的发表结果对比，展示了显著优势。

### 8. 不足与局限

1. **局限性**：
   - 混合 ROM 仍需要数值离散算子（侵入式），用于构建模型组件，限制了其在无代码修改的求解器中的应用。
   - 非侵入式变体的精度低于混合 ROM，尤其在长时间预测时（如 2D 问题最后时刻 $t=25$ 出现较大振荡）。
   - 2D 问题的 G‑ROM+2dCNNResi 需要 68h 训练，成本较高，虽有 POD‑2dCNNResi 改进，但仍需数小时。
2. **实验覆盖不足**：
   - 仅测试了 Burgers 方程（一维和二维），未在更复杂三维或真实湍流问题上验证。
   - 未进行参数敏感性分析（如改变 $r$、$r_0$、NN 层数等）和统计显著性分析。
   - 2D 问题只展示了 $u_x$ 分量的结果，未同时验证 $u_y$ 分量精度。
3. **偏差风险**：部分对比（如 [3,5]）依赖论文公开数据，未直接运行其代码，可能因实现细节差异导致比较不够绝对公平。
4. **资源报告不完整**：未说明 GPU 型号和内存使用量，不利于复现和能效评估。

（完）

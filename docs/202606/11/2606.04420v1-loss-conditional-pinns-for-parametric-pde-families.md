---
title: Loss-Conditional PINNs for Parametric PDE Families
title_zh: 面向参数化PDE族的损失条件物理信息神经网络
authors: "Anna Lazareva, Alexander Tarakanov"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.04420v1"
tags: ["query:flow-field"]
score: 7.0
evidence: 物理信息神经网络用于参数化PDE
tldr: 针对PINN依赖损失权重选择的问题，本文提出LC-PINN，将损失权重或物理系数作为条件输入，训练时随机采样，学习连续解族。通过串联或FiLM架构及固定求积L-BFGS，统一处理权重和系数。在多个PDE上，单模型匹配或优于逐个训练，且摊销成本更低。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04420v1/fig-002.webp\", \"caption\": \"Figure 1: Wall-time/accuracy frontier as the number K of evaluated λ values increases. LC-PINN trains once and produces predictions for any K, while per-λ baselines move rightward by a factor of K. On Helmholtz, LC-PINN Pareto-dominates from K = 1. On Burgers, Causal-PINN is more accurate for a single configuration, but becomes more expensive than LC-PINN once K ≳ 4.\", \"page\": 8, \"index\": 2, \"width\": 952, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2606.04420v1/fig-003.webp\", \"caption\": \"Table 2: Per-k rel-L2 on 1D parametric Helmholtz (mean ± std over four seeds). Bold indicates the best method per row. LC-PINN is one trained network evaluated at each k; baselines are retrained separately for each k.\", \"page\": 8, \"index\": 3, \"width\": 682, \"height\": 190}, {\"url\": \"assets/figures/arxiv/2606.04420v1/fig-004.webp\", \"caption\": \"Figure 2: Per-k rel-L2 on 1D parametric Helmholtz. Error bars show ± standard deviation over four seeds. Retrained baselines vary strongly across the parameter grid, whereas LC-PINN remains comparatively stable.\", \"page\": 9, \"index\": 4, \"width\": 565, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2606.04420v1/fig-001.webp\", \"caption\": \"Figure 3: Per-α rel-L2 on 1D parametric Schrödinger. LC-PINN (blue, 4 seeds) and PI-DeepONet (purple, 4 seeds) cover the 20-point grid from one trained network each; SA-PINN (orange, 2 seeds) is three separate retrainings at α ∈ {0.5, 5.0, 10.0}. Error bars are ±std on a log axis.\", \"page\": 18, \"index\": 1, \"width\": 804, \"height\": 378}]"
motivation: 现有PINN性能受限于损失权重的选择，单一权重难以兼顾所有物理约束。
method: 提出LC-PINN，将损失权重或物理系数作为网络条件输入，训练时从先验分布采样，学习由该向量索引的连续解族。
result: 在参数化Helmholtz、Schrodinger、Burgers和Buckley-Leverett方程上，单模型匹配或优于每个权重的PINN基线，且摊销训练成本更低。
conclusion: 通过探索整个权重空间而非调优单一权重，LC-PINN实现了高效、灵活的物理信息学习，并统一了损失权重和参数系数场景。
---

## 摘要
物理信息神经网络（PINNs）通过最小化残差、边界条件、初始条件和数据损失的加权组合来近似常微分方程和偏微分方程的解。其性能通常受限于损失权重的选择：权重选择不当可能导致训练陷入退化解，即满足一个物理约束而忽略另一个。现有方法会选择或调整一组良好的权重。我们采取不同的视角：不调整单个权重向量，而是在训练过程中探索整个权重空间。我们引入LC-PINN，将Dosovitskiy和Djolonga（2020）的损失条件训练方法扩展到PDE残差场景：将条件向量（损失权重或标量物理系数）作为网络输入，并在每个优化步骤中从简单先验中采样。这将PINN训练转变为学习由该向量索引的连续解族，无需求解器生成的配对数据。因此，LC-PINN介于经典PINN和算子学习之间：它完全基于物理信息，但通过参数化族摊销训练。我们的贡献不在于损失条件构造本身，而是将其扩展到PINN，将损失权重和参数系数机制统一在单一架构下（损失权重使用拼接，系数使用FiLM），以及一个固定求积的L-BFGS完成协议，使参数系数机制可训练。我们给出了条件最优的λ不变性结果，并在参数化亥姆霍兹、薛定谔、粘性伯格斯和巴克-莱弗里特方程上研究了LC-PINN。单个LC-PINN在参数化完整族的同时，匹配或改进了每个权重重新训练的PINN基线，总成本相对于每个实例的重新训练具有有利的摊销效果。

## Abstract
Physics-informed neural networks (PINNs) approximate solutions of ODEs and PDEs by minimising a weighted combination of residual, boundary, initial, and data losses. Their performance is often dominated by the choice of loss weights: a poor weighting can drive training to a degenerate solution in which one physical constraint is satisfied while another is ignored. Existing methods select or adapt a single good set of weights. We take a different view: instead of tuning one weight vector, we explore the entire weight space during training.   We introduce LC-PINN, which adapts the loss-conditional training of Dosovitskiy and Djolonga (2020) to the PDE-residual setting: the conditioning vector (either the loss weights or a scalar physical coefficient) is treated as a network input and sampled from a simple prior at every optimisation step. This turns PINN training into learning a continuous family of solutions indexed by that vector, with no solver-generated paired data. LC-PINN thus lies between classical PINNs and operator learning: it stays fully physics-informed but amortises training over a parametric family. Our contribution is not the loss-conditional construction itself, but its extension to PINNs, the unification of the loss-weight and parametric-coefficient regimes under one architecture (concatenation for loss weights, FiLM for coefficients), and a fixed-quadrature L-BFGS finishing protocol that makes the parametric-coefficient regime trainable.   We give a lambda-invariance result for the conditional optimum and study LC-PINN on parametric Helmholtz, Schrodinger, viscous Burgers, and Buckley-Leverett equations. A single LC-PINN matches or improves retrained per-weight PINN baselines while parameterising the full family in one model, at a total cost that amortises favourably against per-instance retraining.
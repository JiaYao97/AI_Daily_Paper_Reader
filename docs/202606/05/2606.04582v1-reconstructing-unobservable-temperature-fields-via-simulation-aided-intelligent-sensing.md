---
title: Reconstructing Unobservable Temperature Fields via Simulation-Aided Intelligent Sensing
title_zh: 通过仿真辅助智能感知重建不可观测温度场
authors: "Monika Stipsitz, Hèlios Sanchis-Alepuz, Jacob Reynvaan, Silvester Sabathiel"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.04582v1"
tags: ["query:flow-field"]
score: 9.0
evidence: 使用合成数据训练的神经网络从稀疏传感器重建温度场
tldr: 针对组件内部温度场实时监测因传感器布设受限而困难的问题，本文提出基于随机物理仿真生成合成数据集的方法，训练神经网络从稀疏传感器重建温度场。实验表明，该方法在鲁棒性和实时性上优于克里金法，适用于在线监测。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04582v1/fig-002.webp\", \"caption\": \"Fig. 1. (a) Hardware setup: For clarity the metal plate is shown without the anti-reflective coating applied for the application of the thermal camera. (b) The sensors are embedded in the metal plate at the holes shown in the view from bottom. To be able to study various sensor configurations, the plate contains more than the Ns sensors that were actually placed.\", \"page\": 2, \"index\": 2, \"width\": 434, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2606.04582v1/fig-003.webp\", \"caption\": \"Fig. 2. Example for one simulation sequence consisting of multiple different heating simulations at locations (1-5) and final cooling period (point 6). The location and movement of the nozzle over the surface is shown in the left figure, the shape of ellipse is an approximation of the parameterized Gaussian at the given simulation instance.\", \"page\": 3, \"index\": 3, \"width\": 529, \"height\": 239}, {\"url\": \"assets/figures/arxiv/2606.04582v1/fig-004.webp\", \"caption\": \"TABLE II RANDOMIZATION PARAMETER AND RANGES FOR THE ML DATASET\", \"page\": 3, \"index\": 4, \"width\": 545, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2606.04582v1/fig-005.webp\", \"caption\": \"Fig. 3. Internal temperature reconstruction for selected samples from unseen FEM test cases. The NN reconstructs temperature maps (row 4) from masked input images Tt and ∆T (rows 2–3) that closely match the ground truth (row 1).\", \"page\": 4, \"index\": 5, \"width\": 537, \"height\": 511}, {\"url\": \"assets/figures/arxiv/2606.04582v1/fig-006.webp\", \"caption\": \"TABLE III ABLATION STUDY EVALUATED ON HELD-OUT FEM TEST SYSTEMS\", \"page\": 5, \"index\": 6, \"width\": 734, \"height\": 224}, {\"url\": \"assets/figures/arxiv/2606.04582v1/fig-001.webp\", \"caption\": \"Fig. 5. Mean reconstructed temperature map TNN and corresponding uncertainty distribution σNN for the sample with the near-median zi.\", \"page\": 6, \"index\": 1, \"width\": 526, \"height\": 230}]"
motivation: 传统方法难以实现高分辨率热监测，且机器学习因缺乏高质量训练数据集而受限。
method: 采用随机物理仿真生成合成数据集，仅用该数据集训练神经网络，从硬件中的稀疏传感器重建内部温度场。
result: 神经网络重建在鲁棒性上优于克里金法，且支持实时推理。
conclusion: 该方法适用于在线监测原本不可观测的热状态。
---

## 摘要
在许多系统中，由于可行传感器位置受限，对组件和子结构内部温度分布的实时监测是一个具有挑战性的课题。尽管机器学习（ML）在许多应用中是一种通用工具，但其在高分辨率热监测中的应用受到高质量训练数据集可用性的阻碍。在这项工作中，我们提出了一种基于随机物理仿真生成工业应用数据集的新方法。我们在一个概念验证硬件装置中演示了该方法：仅通过此类合成数据集训练的神经网络（NN），用于从嵌入硬件中的稀疏传感器重建内部温度场。基于NN的重建不仅在鲁棒性上优于克里金法，而且能够实现实时推理，使得该方法适用于对原本不可观测的热状态进行在线监测。

## Abstract
Real-time monitoring of the temperature distribution within components and sub-structures is a challenging topic in many systems due to restrictions on feasible sensor locations. While machine learning (ML) proves a versatile tool in many applications, its adoption for high-resolution thermal monitoring is hindered by the availability of high-quality datasets for training. In this work, we propose a novel approach for generating datasets for industrial applications based on randomized physics-based simulations. We demonstrate the approach in a proof-of-concept hardware setup: A neural network (NN) trained only on such a synthetic dataset, is used to reconstruct the internal temperature field from sparse sensors embedded in the hardware. The NN-based reconstructions do not only outperform Kriging in robustness but also enable real-time inference, making the method suitable for online monitoring of otherwise unobservable thermal states.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在许多工业系统中（如电子设备、燃料电池、电池组等），对组件或子结构内部的温度分布进行高分辨率实时监测至关重要，但实际传感器布设受到物理可达性、数量限制和成本等因素制约，导致部分区域（内部或难以安装传感器处）的温度状态不可直接观测。
- **研究背景**：传统方法（如直接物理仿真、降阶模型）计算开销大，难以实时；经典插值方法（如Kriging）对稀疏测量敏感，鲁棒性不足；近期机器学习方法多依赖实际测量数据，但高质量、覆盖广泛工况的训练数据获取成本高昂、耗时，且往往存在偏差。
- **本文目标**：提出一种基于**随机物理仿真**生成合成训练数据的策略，仅利用合成数据训练神经网络，实现从稀疏传感器读数到完整内部温度场的高精度、鲁棒且实时的重建。

## 2. 论文提出的方法论

### 核心思想
利用简化的有限元仿真（FEM）模型，通过对关键物理参数进行随机化（覆盖不确定性范围），生成大量物理一致的合成温度场数据。以此训练一个端到端的神经网络，该网络以当前时刻及前一时刻的稀疏传感器测量值（含掩码和噪声增强）为输入，输出全分辨率的内部温度分布。

### 关键技术细节

- **仿真数据集生成（RSD – Randomized Simulation Dataset）**：
  - 使用简化热传导FEM模型（ElmerFEM求解器），将热风枪近似为Robin边界条件，热流参数呈椭圆高斯分布（公式2‑5）。
  - **随机化参数**（表II）：包括喷嘴位置 $(x_0, y_0)$，空气换热系数 $\alpha_{air}$，环境温度 $T_{air}$，喷嘴系数 $\alpha_{nozzle}$，喷嘴温度 $T_{nozzle}$，高斯宽度 $r_{0x}$，椭圆度 $\epsilon$，喷嘴移动速度 $v_{nozzle}$ 等，均从给定均匀范围内采样。
  - **模拟序列**：每个序列包含若干段（静态加热、喷嘴移动、冷却），共240个序列，从每段随机抽取两个时间步，最终得到10464张 $64 \times 64$ 温度图像作为标签（同时提取模拟传感器位置的值作为输入）。
  - 整个流程自动化：FreeCAD（CAD）、gmsh（网格）、ElmerFEM（仿真）、Python后处理。

- **重建神经网络架构**（基于[12]）：
  - **输入**：三个 $64 \times 64$ 通道：① 当前时刻传感器温度掩码图像；② 当前与上一时刻的传感器温差掩码图像；③ 传感器位置的二进制掩码。
  - **结构**：先通过多个线性重建步骤（Learned Spatial Propagator）从稀疏值填充初步温度场；后接U‑Net结构生成完整温度场；最后再将实际传感器值替换回对应位置。
  - **训练损失**包含三项：
    - 温度像素MSE $L_T$；
    - 温度梯度MSE $L_G$（离散有限差分计算）；
    - 基于瞬态热方程的物理正则项 $L_H$（公式6‑7），仅作用于热导率恒定区域，用于提升泛化与收敛速度，不作为主驱动。
  - 总损失：$L = L_T + \alpha_G L_G + \alpha_H L_H$，其中 $\alpha_G = 0.1,\ \alpha_H = 10^{-5}$。
  - 训练时对输入施加乘性高斯噪声（$\mu=1,\sigma=0.005$）并随机移除6‑8个传感器，增强鲁棒性。

### 公式说明（文字流程）
- Robin边界条件：$f = -k \frac{\partial T}{\partial n} = \alpha (T - T_{ext})$。
- 换热系数与外部温度随距离呈椭圆高斯分布（公式2‑3）。
- 物理残差：$C_H(T_t, T_{t-1}) = \frac{\partial T}{\partial t} - \frac{1}{\rho c_p} \nabla \cdot (k \nabla T)$，用于构造 $L_H$。

## 3. 实验设计

- **数据集**：
  - **训练/测试**：240个FEM仿真序列，按80%/20%划分训练与测试集（测试集仅用于定量评估）。
  - **硬件实验**：一块 $192 \times 192 \times 10\ mm$ 钢板，嵌入17个NTC热敏电阻（深度2.6 mm），由ESP32读取。利用热风枪施加三种加热模式：
    1. 静态加热（从均匀初温开始，约30 s）；
    2. 对角线移动（从非均匀初温开始）；
    3. 逆时针圆形移动（从非均匀初温开始）。
  - 对比方法：Kriging（`UniversalKriging` from `pykrige`，标准参数）。
  - 定性验证：利用红外热像仪拍摄钢板表面温度，与NN重建结果进行视觉对比。

- **Benchmark**：Kriging作为经典插值基线。
- **消融实验**：在留出的FEM测试集上评估四个配置：
  1. 完整模型（含所有损失项）；
  2. 无物理损失（$\alpha_H = 0$）；
  3. 随机1‑4个传感器失效；
  4. 不同等级加性高斯噪声（$\sigma = 0.5, 1.14, 2.0\ K$）。
  评价指标：传感器一致性RMSE（SCE RMSE）、空间重建平均/最大RMSE（$\sqrt{L_T}$）、物理残差均值/最大值（$L_H$均值/最大值）。

## 4. 资源与算力

论文未明确说明训练所使用的GPU型号、数量或总训练时间。仅提及训练了7000个epoch，批量大小20，初始学习率0.001，使用阶梯学习率调度（每100步乘以0.9）。硬件实测部分使用了ESP32微控制器进行传感器数据采集。总体而言，算力需求未具体披露。

## 5. 实验数量与充分性

- **实验组数**：仿真数据生成240个序列（约10k幅图像）；硬件实验3种加热情景，每种取多个时间点展示；消融实验包含4种主要配置（各含多个子情况如不同失效数/噪声等级）。
- **充分性评估**：
  - 仿真测试集上的定量消融比较了有无物理损失、传感器失效、噪声的影响，结果清晰表明物理损失提升物理一致性，模型对失效和噪声具有一定鲁棒性。
  - 硬件实验提供了与热像仪的视觉对比以及兼容性分析（基于GUM框架，计算归一化兼容指数 $z_i$，并给出兼容率71‑94%），客观性较好。
  - 不足：仅在2D钢板这一单一几何与材料场景上验证，未扩展到3D、异质材料、多热源等复杂情形；未与其他近期ML重建方法（如CNN‑based、图神经网络等）进行对比，仅与经典的Kriging比较。
  - 总体而言，实验设计合理，覆盖了主要影响因素，但场景多样性有限，公平性方面与Kriging的对比可接受，未与更先进的端到端方法对比是主要缺失。

## 6. 论文的主要结论与发现

- **核心结论**：利用随机物理仿真生成的合成数据集，可以仅通过模拟数据训练神经网络，有效从工业硬件中的稀疏传感器重建内部温度场。
- **性能优势**：
  - NN重建在定性上准确捕捉局部加热、非均匀分布，优于Kriging的平滑/模糊结果。
  - NN对传感器失效和噪声更具鲁棒性（消融显示物理残差较低）。
  - 推理速度快，适合在线监测。
- **物理一致性**：引入瞬态热方程损失项能提升重建结果的物理合理性（降低物理残差）。
- **兼容性**：与传感器测量值的归一化偏差多在1个标准差以内，兼容率71‑94%，说明预测与实测在不确定性范围内一致。

## 7. 优点

- **数据生成创新**：通过随机化简化仿真参数，无需精确标定真实系统，即可生成大量覆盖工况不确定性的物理一致训练数据，大幅降低数据获取成本和时间。
- **模型无关性**：提出的框架可适应不同神经网络架构（如U‑Net、GNN、Transformer），论文明确指出架构对比留作未来工作，体现了方法的通用潜力。
- **鲁棒性设计**：训练时加入传感器噪声和随机移除，使模型能应对实际测量中的传感器漂移和故障。
- **不确定性评估**：遵循国际计量规范（GUM/VIM），对NN输出进行蒙特卡洛不确定性传播，并计算与传感器读数的兼容指数，提升了方法的可信度。
- **轻量化验证**：在真实硬件装置上结合红外图像进行定性检验，并与经典方法对比，结论可信。

## 8. 不足与局限

- **场景局限性**：仅验证了2D规则钢板（单一材料、简单热源），未对3D几何、多材料、多热源、复杂边界等更实际的工业场景进行实验。
- **对比方法不足**：仅与Kriging对比，未与更先进的ML重建方法（如PINN、图神经网络、Transformer）进行比较，削弱了方法先进性的说服力。
- **物理损失计算限制**：物理正则项 $L_H$ 仅在热导率恒定区域计算，因为网格粗糙导致变物性时差分不准确，这限制了其在异质材料上的直接应用。
- **仿真假设简化**：使用纯热传导FEM替代实际CFD，且参数范围基于有限校准，若实际工况超出预设随机范围，模型性能可能下降。
- **实验重复性**：未提供随机种子、硬件实验详细重复次数，难以完全复现结果。
- **算力未说明**：未披露训练所需GPU资源，可能影响其他研究者评估实际部署成本。

（完）

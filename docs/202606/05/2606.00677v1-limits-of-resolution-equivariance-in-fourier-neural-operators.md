---
title: Limits of Resolution Equivariance in Fourier Neural Operators
title_zh: 傅里叶神经算子中分辨率等变性的极限
authors: "Alex Colagrande, Paul Caillon, Eva Feillet, Alexandre Allauzen"
date: 2026-05-30
pdf: "https://arxiv.org/pdf/2606.00677v1"
tags: ["query:flow-field"]
score: 9.0
evidence: 用于流体动力学的神经算子
tldr: Fourier Neural Operators (FNO) 常被认为能跨分辨率泛化，但实验表明在达西流问题中，直接在高分辨率上运行FNO并不总是优于在低分辨率运行后通过傅里叶零填充上采样。分析发现，由于傅里叶截断，中间层能量集中于低频，高频主要来自后期非线性/解码器，导致分辨率变换时性能不稳定。研究强调了低网格加线性上采样作为简单强基线，并指出非线性混叠是零样本分辨率等变性的关键障碍。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00677v1/fig-001.webp\", \"caption\": \"Figure 1: Cross-resolution inference vs. a band-limited upsampling baseline (s = 85 → S = 211). Top: ground-truth at s = 85, Direct-S prediction at S = 211, and Low+pad prediction (run at s then upsample the output to S via Fourier zero-padding), shown with no spatial padding and spatial padding = 10. Bottom: corresponding radially averaged power spectra. Direct evaluation at S produces a spurious high-frequency tail beyond the training Nyquist frequency, whereas Low+pad preserves the spectral decay of the band-limited target. Spatial padding reduces boundary artifacts but does not recover missing high-frequency content.\", \"page\": 3, \"index\": 1, \"width\": 822, \"height\": 293}]"
motivation: 检验FNO在训练和测试分辨率不同时是否真的能零样本泛化，并探究其机理。
method: 对比两种推理方式：直接在高分辨率运行FNO，以及在低分辨率运行后通过傅里叶零填充上采样；并分析各层频谱能量分布。
result: 直接在高分辨率推理并非总是有益，甚至可能更差；低分辨率加线性上采样是更强的基线；中间层能量向低频集中，高频输出依赖后期模块。
conclusion: 非线性混叠是限制FNO零样本分辨率等变性的主要因素，简单的低网格加线性上采样为跨分辨率评估提供了可靠基准。
---

## 摘要
傅里叶神经算子通常被认为能够跨空间分辨率泛化，使得在粗网格上训练并在细网格上部署成为可能。我们通过对比从训练分辨率 $s$ 到测试分辨率 $S > s$ 时的两种推理选择来检验这一假设：直接在 $S$ 上运行 FNO，或者在 $s$ 上运行并通过傅里叶零填充将预测上采样到 $S$。在达西流上，我们观察到直接的细网格推理并不总是有益的，甚至可能比低网格加零填充基线更差。我们进一步分析了逐层频谱，发现在傅里叶截断下，中间表示的能量越来越集中在低频，高频输出主要由后续的非线性/解码器阶段产生。这为 FNO 为什么能够在保留少数模式的同时表现良好，但在分辨率变化下仍然敏感提供了机制性解释。我们的发现突出了跨分辨率评估的一个简单而强大的基线，并将非线性混叠视为零样本分辨率等变性的关键障碍。

## Abstract
Fourier Neural Operators are often assumed to generalize across spatial resolutions, enabling training on a coarse grid and deployment on a finer grid. We test this assumption by contrasting two inference-time choices when moving from training resolution $s$ to test resolution $S>s$: running FNO directly at $S$, or running at $s$ and upsampling the prediction to $S$ via Fourier zero-padding. On Darcy flow, we observe that direct fine-grid inference is not reliably beneficial and can be worse than the low-grid-plus-upsampling baseline. We further analyze layerwise spectra and find that, under Fourier truncation, intermediate representations increasingly concentrate energy in low frequencies, with high-frequency output produced mainly by late nonlinear/decoder stages. This offers a mechanistic explanation for why FNO can perform well while retaining few modes, yet remain sensitive under resolution shifts. Our findings highlight a simple but strong baseline for cross-resolution evaluation and point to nonlinear aliasing as a key obstacle to zero-shot resolution equivariance.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：Fourier Neural Operator（FNO）因其在傅里叶空间做全局卷积、且仅保留固定数量傅里叶模式的特性，常被假设能在粗网格训练后直接零样本部署到细网格（“训练粗网格、部署细网格”）。但这种“分辨率等变性”是否真的成立？近期工作质疑其鲁棒性，指出离散化失配和非线性混叠可能导致性能下降。
- **整体含义**：论文旨在严格检验 FNO 的零样本跨分辨率泛化能力，并通过频谱分析揭示其内在机理。结论是：**非线性混叠是限制等变性的主要障碍**，且一个简单的“低网格预测加线性上采样”基线往往强于直接在高分辨率上运行网络。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：通过对比两种推理协议，分离“执行网络于未见网格”的影响；结合逐层频谱诊断，定位频率内容的产生与抑制。
- **关键技术细节**：
    - **零样本跨分辨率设定**：训练分辨率 $s$，测试分辨率 $S > s$，模型不进行任何微调。
    - **两种推理协议**：
        - **Direct‑S**：直接在 $S\times S$ 网格上执行训练好的 FNO。
        - **Low+pad 基线**：首先通过傅里叶截断将输入降采样到 $s$，在训练分辨率上运行 FNO，再通过傅里叶零填充将输出上采样回 $S$。该基线避免执行网络于未见网格。
    - **逐层频谱诊断**：固定分辨率下记录中间激活，计算径向平均功率谱，追踪能量在不同层间的重分布。特别关注频带截断（retained modes）对谱的影响。
    - **反混叠分析**：指出点状非线性是跨模式交互与频谱泄露的主要来源；高维隐空间可能通过编码‑处理‑解码模式将高频内容编码到低频表示中。

### 3. 实验设计：数据集、基准、对比方法

- **数据集**：Darcy Flow 基准（Li et al., 2021），模拟多孔介质流动。输入为渗透率系数 $a$，输出为压力解 $u$。数据原始分辨率 $421\times 421$，训练样本 1000，测试样本 200。
- **基准**：使用官方 FNO 实现，训练分辨率固定为 $s = 85$，测试分辨率 $S > 85$（如 $S=211$）。对输入进行傅里叶带限重采样（截断到训练奈奎斯特频率）以控制高频信息的存在。
- **对比方法**：
    1. Direct‑S 推理
    2. Low+pad 基线（低网格运行后零填充上采样）
    3. 针对是否使用空间填充（padding）的比较（padding=0 与 padding=10）
- **其他实验**：分析不同傅里叶模式保留数量（$4\times 4$、$8\times 8$、$64\times 64$）下的逐层频谱演化；比较初始化和训练后的谱变化。

### 4. 资源与算力

- **文中提及**：使用了 IDRIS 的 HPC 资源（分配号 A0191016927），并在致谢部分确认。**未明确说明** GPU 型号、数量、训练总时长等具体算力开销。

### 5. 实验数量与充分性

- **实验数量**：主要包含两组核心实验：
    1. 跨分辨率推理性能对比（图 1），展示 Direct‑S 与 Low+pad 视觉效果及频谱对比。
    2. 逐层频谱分析（图 2），展示不同截断级别下中间层与输出的谱分布。
- **补充实验**：对空间填充（padding）的影响做了消融控制（有/无）。
- **充分性评估**：实验设计清晰地分离了“执行于新网格”的影响，聚焦于受控的带限重设场景（无未见过的高频），避免了混淆。但仅使用一个数据集（Darcy flow）和一组训练分辨率（85→211），**泛化性验证不够充分**；未在更多 PDE（如 Navier-Stokes）上验证结论。消融仅针对 padding，未对非线性类型、网络深度等做系统消融。总体实验客观、公平，但覆盖范围有限。

### 6. 论文的主要结论与发现

1. **直接细网格推理并非总是有益**，在零样本跨分辨率场景下甚至可能比“低网格运行+线性上采样”基线更差（图 1）。原因：直接执行引入非线性混叠，产生虚假高频尾，干扰低频带。
2. **逐层频谱呈现“编码‑处理‑解码”模式**：在强截断（如 $4\times 4$ 模式）下，中间层能量向低频集中，高频输出主要来自最后的非线性/解码器阶段（图 2）。这解释了 FNO 为何能仅用少量模式仍表现良好——高维隐空间通过非线性混叠重组信息。
3. **非线性混叠是一把双刃剑**：在分布内训练时，它提供跨模式通信途径，促进低频编码高频；但在分辨率变化时，它会破坏已学频带，导致性能下降。
4. **简单的 Low+pad 基线为跨分辨率评估提供了可靠基准**，比直接高分辨率执行更稳定。

### 7. 优点：方法或实验设计上的亮点

- **清晰的因果区分**：通过对比 Direct‑S 与 Low+pad，有效分离了“执行网络于未见网格”与“线性带限上采样”的影响，避免了传统上仅比较不同分辨率预测的混淆。
- **逐层频谱诊断**：直接测量中间表示的频率分布，揭示了截断下的模式重组过程，提供了机制性解释。
- **受控实验条件**：使用傅里叶带限重采样保证高频信息为零，最小化了数据本身带来的混淆，严格测试了模型对网格离散化的敏感性。
- **提出了强基准**：Low+pad 作为一种简单、无参数的方法，为未来跨分辨率评估树立了标准。

### 8. 不足与局限

- **数据与任务单一**：仅使用 Darcy flow 一个 PDE 数据集，未在更复杂的 Navier-Stokes、弹性力学等任务上验证，结论的一般性存疑。
- **分辨率跨度有限**：仅测试了从 $85$ 到 $211$ 的一个具体跨度，未探索不同训练/测试分辨率比例的影响。
- **消融不全面**：仅对比了空间 padding 的有无，未系统分析网络深度、非线性函数类型（如 ReLU vs GELU）、层宽、通道数等对分辨率等变性的影响。
- **未提供缓解方案**：论文指出非线性混叠是障碍，但未提出具体的抗混叠架构或训练策略（如 anti-aliasing filter、spectral regularization），使得实用性受限于诊断层面。
- **资源信息不充分**：未报告训练时长或 GPU 规格，可复现性略打折扣。

（完）

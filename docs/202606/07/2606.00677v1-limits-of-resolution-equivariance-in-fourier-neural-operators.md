---
title: Limits of Resolution Equivariance in Fourier Neural Operators
title_zh: 傅里叶神经算子的分辨率等变极限
authors: "Alex Colagrande, Paul Caillon, Eva Feillet, Alexandre Allauzen"
date: 2026-05-30
pdf: "https://arxiv.org/pdf/2606.00677v1"
tags: ["query:flow-field"]
score: 7.0
evidence: 傅里叶神经算子用于流体动力学分辨率分析
tldr: 本文质疑傅里叶神经算子（FNO）在分辨率间的泛化能力，对比了直接细网格推断与粗网格加傅里叶零填充上采样两种策略，发现前者不一定更优。通过谱分析，揭示中间表示低频集中，高频由后期非线性层产生，指出非线性混叠是限制零样本分辨率等方差的关键障碍，并提出了一个简单的跨分辨率评估基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00677v1/fig-001.webp\", \"caption\": \"Figure 1: Cross-resolution inference vs. a band-limited upsampling baseline (s = 85 → S = 211). Top: ground-truth at s = 85, Direct-S prediction at S = 211, and Low+pad prediction (run at s then upsample the output to S via Fourier zero-padding), shown with no spatial padding and spatial padding = 10. Bottom: corresponding radially averaged power spectra. Direct evaluation at S produces a spurious high-frequency tail beyond the training Nyquist frequency, whereas Low+pad preserves the spectral decay of the band-limited target. Spatial padding reduces boundary artifacts but does not recover missing high-frequency content.\", \"page\": 3, \"index\": 1, \"width\": 822, \"height\": 293}]"
motivation: 验证FNO在训练粗网格、测试细网格时是否真的具备零样本分辨率等方差能力。
method: 对比两种推断策略：直接在细网格运行FNO，或在粗网格运行后通过傅里叶零填充上采样；并分析中间层的频谱能量分布。
result: 直接细网格推断并非总是优于粗网格加采样基线，且FNO中间表示低频主导，高频输出依赖后期非线性/解码器阶段。
conclusion: 非线性混叠是FNO零样本分辨率等方差的主要障碍，粗网格加采样是简单但有竞争力的跨分辨率基线。
---

## 摘要
傅里叶神经算子通常被假设能够跨空间分辨率泛化，从而可以在粗网格上训练并在细网格上部署。我们通过对比从训练分辨率$s$转移到测试分辨率$S>s$时的两种推理时选择来检验这一假设：直接在$S$上运行FNO，或者在$s$上运行并通过傅里叶零填充将预测上采样到$S$。在达西流问题上，我们观察到直接细网格推理并不总是有益的，甚至可能比低网格加填充上采样的基线更差。我们进一步分析了逐层频谱，发现在傅里叶截断下，中间表示的能量越来越集中在低频部分，而高频输出主要由后期的非线性/解码器阶段产生。这为解释FNO为何能在保留少量模式的情况下表现良好，但在分辨率变化下仍保持敏感提供了一种机制性解释。我们的发现强调了跨分辨率评估的一个简单但强有力的基线，并指出非线性混叠是实现零样本分辨率等变性的关键障碍。

## Abstract
Fourier Neural Operators are often assumed to generalize across spatial resolutions, enabling training on a coarse grid and deployment on a finer grid. We test this assumption by contrasting two inference-time choices when moving from training resolution $s$ to test resolution $S>s$: running FNO directly at $S$, or running at $s$ and upsampling the prediction to $S$ via Fourier zero-padding. On Darcy flow, we observe that direct fine-grid inference is not reliably beneficial and can be worse than the low-grid-plus-upsampling baseline. We further analyze layerwise spectra and find that, under Fourier truncation, intermediate representations increasingly concentrate energy in low frequencies, with high-frequency output produced mainly by late nonlinear/decoder stages. This offers a mechanistic explanation for why FNO can perform well while retaining few modes, yet remain sensitive under resolution shifts. Our findings highlight a simple but strong baseline for cross-resolution evaluation and point to nonlinear aliasing as a key obstacle to zero-shot resolution equivariance.
---
title: "Silent Failures in Physics-Informed Neural Networks: Parameter Poisoning and the Limits of Loss-Based Validation"
title_zh: 物理信息神经网络中的静默失败：参数中毒与基于损失的验证的局限性
authors: "David McShannon, Nicholas Dietrich"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.25151v1"
tags: ["query:flow-field"]
score: 7.0
evidence: 物理信息神经网络的鲁棒性分析
tldr: "物理信息神经网络(PINNs)将控制方程嵌入损失函数，但本文发现当编码的物理参数错误时，模型仍能训练到低损失却给出错误解，称为参数投毒。通过Burgers、Navier-Stokes和对流扩散三种PDE系统验证，中毒模型损失低于或等于干净基线但解误差高达128%。提出后验防御：扫描参数空间的残差损失最小值即可恢复真实参数，无需外部数据，且跨多种架构和随机种子有效。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25151v1/fig-003.webp\", \"caption\": \"Table 1: Physics parameter poisoning results (mean ± std, n = 3 seeds for cavity and Burgers; Conv-Diff single-seed except δ = 200%). R = detection difficulty ratio. Training losses are highly variable across seeds (in some cases σ ≥ µ), reflecting the stochasticity of PINN optimization rather than Gaussian sampling; L2 errors are more stable.\", \"page\": 4, \"index\": 3, \"width\": 694, \"height\": 673}, {\"url\": \"assets/figures/arxiv/2606.25151v1/fig-001.webp\", \"caption\": \"Figure 1: The Danger Zone: physics parameter poisoning across PDEs. Left: Training loss remains low regardless of perturbation magnitude. Center: Solution error grows silently; squares mark silent failures. Right: Detection difficulty ratio varies by orders of magnitude across PDE types.\", \"page\": 5, \"index\": 1, \"width\": 1038, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2606.25151v1/fig-002.webp\", \"caption\": \"Figure 2: Post-hoc loss landscape defense (n = 3 seeds). The PDE residual loss is evaluated at a range of Re values without retraining. Thin lines show individual seeds; thick lines show the mean. The clean model’s mean minimum falls at Re=100 (the stated value); the poisoned model’s mean minimum falls at Re=150 (the true training value). Individual seed minima range Re=100–105 (clean) and Re=145–155 (poisoned), with zero overlap.\", \"page\": 5, \"index\": 2, \"width\": 492, \"height\": 307}]"
motivation: PINNs低损失假设物理正确性被打破，参数错误时低损失无法保证解准确。
method: 通过扰动PDE参数进行敏感度分析（参数投毒），并提出后验防御：扫描参数值的残差损失，最小值对应真实参数。
result: "中毒模型损失匹配或低于干净基线，解误差高达71%-128%；所提防御在所有PDE系统和架构中均恢复真实参数。"
conclusion: 仅凭低损失不能验证PINNs物理准确性，所提参数扫描方法可有效检测参数错误。
---

## 摘要
物理信息神经网络（PINNs）将控制方程嵌入其损失函数中，从而实现对偏微分方程的无网格求解。低训练损失被视为学习到的解在物理上正确的证据。本文表明，当编码的物理不正确时，这一假设会失效。通过在训练前扰动PDE参数（我们将此情境描述为物理参数中毒或参数错误设定），我们得到了训练到低损失但给出错误答案的模型；我们将扰动策略视为敏感性分析而非仅作为安全威胁，且我们的结论无需假设存在对抗者。实现低残差损失并不能区分准确与不准确的解：中毒模型的损失达到或低于干净基线，但结果差异巨大，因此降低残差并非物理准确性的证据。在三个PDE系统（Burgers方程、Navier-Stokes空腔流、对流扩散方程）中，中毒模型匹配或超越了干净模型的训练损失，而其解在固定扫描中差异高达71%，在对抗搜索下高达128%；在空腔流Re=400时，中毒损失低于干净基线。我们定义了一个检测困难比R（求解误差除以训练损失）来总结污染的隐蔽程度，尽管跨PDE的比较因损失尺度差异而复杂化。我们测试了六种候选防御方法，没有一种能在所有情况下可靠检测污染。我们提出了一种事后防御：在不重新训练的情况下，扫描PDE残差损失随参数值的变化。损失最小值恢复了真实的训练参数，无需外部数据，并在所有三个PDE系统中通用。该效果在五种网络架构（8.7K至133K参数）中成立，具有双向性，并通过多个随机种子得到确认。

## Abstract
Physics-informed neural networks (PINNs) embed governing equations in their loss function, enabling mesh-free solutions to partial differential equations. Low training loss is treated as evidence that the learned solution is physically correct. This paper shows that assumption breaks down when encoded physics are incorrect. By perturbing PDE parameters before training, a setting we describe as physics parameter poisoning or parameter misspecification, we produce models that train to low loss but give incorrect answers; we treat the perturbation schedule as sensitivity analysis rather than only as a security threat, and none of our claims requires an adversary. Achieving low residual loss does not discriminate accurate from inaccurate solutions: poisoned models reach losses at or below the clean baseline yet differ by large margins, so driving the residual down is not evidence of physical accuracy. Across three PDE systems (Burgers equation, Navier-Stokes cavity, and convection-diffusion), poisoned models match or beat the clean-model training loss while their solutions differ by up to 71% in the fixed sweep and up to 128% under adversarial search; at Cavity Re=400 the poisoned loss falls below the clean baseline. We define a detection difficulty ratio R (solution error divided by training loss) to summarize how invisible the corruption is, though cross-PDE comparison is complicated by differences in loss scale. We test six candidate defenses, none of which reliably detects corruption across all regimes. We propose a post-hoc defense: sweeping the PDE residual loss across parameter values without retraining. The loss minimum recovers the true training parameter without external data, and generalizes across all three PDE systems. The effect holds across five network architectures (8.7K to 133K parameters), is bidirectional, and is confirmed across multiple random seeds.
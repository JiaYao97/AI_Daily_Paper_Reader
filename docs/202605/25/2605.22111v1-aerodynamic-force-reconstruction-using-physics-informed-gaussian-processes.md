---
title: Aerodynamic force reconstruction using physics-informed Gaussian processes
title_zh: 基于物理信息高斯过程的气动载荷重构
authors: "Gledson Rodrigo Tondo, Igor Kavrakov, Guido Morgenthal"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22111v1"
tags: ["query:flow-field"]
score: 9.0
evidence: 使用物理信息高斯过程进行气动力重构
tldr: 针对气动载荷建模中简化假设和噪声数据导致的模型验证困难，提出一种基于物理信息高斯过程的概率机器学习方法，从噪声结构响应中重构气动载荷，避免过拟合且支持异构多保真数据。在大贝尔特东桥模拟案例中，重构载荷与真实值在RMSE、幅值、相位角和峰值上高度一致，适用于模型验证、载荷估计和损伤预测。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.22111v1/fig-004.webp\", \"caption\": \"Figure 1: Framework for the physics-informed Gaussian process. Models for different data types are created and jointly trained (green). The force model is built using Eq. (1) (blue) and used for predictions (red).\", \"page\": 2, \"index\": 4, \"width\": 929, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2605.22111v1/fig-005.webp\", \"caption\": \"Figure 2: The Great Belt East Bridge in Denmark. Top: elevation sketch (left) and deck coordinate system (right), with the mean wind speed U , turbulence components u and w, degrees of freedom p, h and α, aerodynamic lift L, drag D and moment M , and section width B and height H . Bottom: the first sway (f = 0.052 Hz), bending (f = 0.100 Hz) and torsional (f = 0.278 Hz) mode shapes.\", \"page\": 3, \"index\": 5, \"width\": 882, \"height\": 320}, {\"url\": \"assets/figures/arxiv/2605.22111v1/fig-007.webp\", \"caption\": \"Figure 3: Top: turbulence spectrum in the vertical direction w (left) and spatial correlation along the bridge length (right). Bottom: the vertical turbulence time history at midspan.\", \"page\": 4, \"index\": 7, \"width\": 674, \"height\": 477}, {\"url\": \"assets/figures/arxiv/2605.22111v1/fig-006.webp\", \"caption\": \"Figure 4: Modal responses for the first bending mode shape (f = 0.100 Hz) and the selected training points (TPs). From top to bottom: modal displacements, velocities and accelerations.\", \"page\": 4, \"index\": 6, \"width\": 675, \"height\": 434}, {\"url\": \"assets/figures/arxiv/2605.22111v1/fig-008.webp\", \"caption\": \"Figure 5: True modal force signal F (t) for the first structural mode, along with mean and 95% confidence interval of predictions in time (top) and the correspondent power spectral density (bottom).\", \"page\": 5, \"index\": 8, \"width\": 673, \"height\": 436}, {\"url\": \"assets/figures/arxiv/2605.22111v1/fig-002.webp\", \"caption\": \"Figure 6: Comparison metric values between the original forces from the linear unsteady model and the mean values obtained from the physics-informed Gaussian process model.\", \"page\": 6, \"index\": 2, \"width\": 668, \"height\": 228}, {\"url\": \"assets/figures/arxiv/2605.22111v1/fig-003.webp\", \"caption\": \"Figure 7: Left: the numerical model’s third mode shape (f = 0.123 Hz). Right: the PSD of the linear unsteady modal force and the correspondent physics-informed Gaussian process mean regression result.\", \"page\": 6, \"index\": 3, \"width\": 746, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2605.22111v1/fig-001.webp\", \"caption\": \"Figure 8: Global responses at midspan in drag direction (p, top) and lift direction (h, bottom) for the original LU model and computed with the GP regressed forces.\", \"page\": 6, \"index\": 1, \"width\": 683, \"height\": 346}]"
motivation: 气动载荷模型常因简化假设和不完整噪声数据而难以验证，需要一种鲁棒的重构方法。
method: 采用物理信息高斯过程，将物理约束嵌入概率框架，从噪声动态响应中重构气动载荷，无需正则化且支持多保真数据。
result: 在大贝尔特东桥线性非定常模拟中，重构载荷与真实值在均方根误差、幅值、相位角和峰值上高度一致。
conclusion: 该方法能准确重构气动载荷，具有模型验证、载荷估计和结构损伤预测的广泛适用性。
---

## 摘要
精确建模气动载荷对于理解和预测复杂结构系统的响应至关重要。然而，这些模型通常依赖于真实物理力的简化，引入的假设可能限制其准确性。在存在噪声或不完整数据的情况下，验证此类模型变得尤其具有挑战性。为此，我们引入了一种基于概率的物理信息机器学习方法，旨在从结构动力响应的噪声测量中重构底层气动载荷。该模型避免了过拟合，消除了正则化方案的需求，并允许在训练过程中使用异质和多保真数据。通过在大贝尔特东桥（Great Belt East Bridge）上模拟线性非定常假设下的气动载荷重构，验证了该方法的有效性。结果显示，真实载荷与预测载荷之间高度一致，特别是在均方根误差、幅值、相位角和信号峰值方面。该载荷重构方法具有广泛的适用性，例如模型验证、未来载荷估计和结构损伤预测。

## Abstract
Accurate modeling of aerodynamic loads is essential for understanding and predicting the responses of complex structural systems. However, these models often rely on simplifications of the true physical forces, introducing assumptions that can limit their accuracy. Validating such models becomes particularly challenging in the presence of noisy or incomplete data. To address this, we introduce a probabilistic physics-informed machine learning approach designed to reconstruct the underlying aerodynamic loads from noisy measurements of structural dynamic responses. The model avoids overfitting, eliminates the need for regularization schemes, and allows for the use of heterogeneous and multi-fidelity data during the training process. The efficacy of the approach is demonstrated through the reconstruction of aerodynamic loads on the Great Belt East Bridge, simulated under a linear unsteady assumption. Results show a strong agreement between true and predicted loads, particularly related to root mean squared errors, magnitude, phase angle and peak values of the signals. The method for load reconstructing holds broad applicability, such as modeling validation, future load estimation, and structural damage prognosis.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究动机**：气动载荷的精确建模对于长跨度桥梁等复杂结构的分析与设计至关重要，但现有模型多基于简化假设，且实测数据往往含有噪声或不完整，导致模型验证困难。
- **问题定义**：如何从有限、含噪的结构动态响应测量值中，稳健地重构出底层气动载荷，同时避免过拟合和人工正则化。
- **整体含义**：提出一种基于物理信息高斯过程（Physics-Informed Gaussian Processes, PI-GP）的概率框架，将物理方程嵌入协方差核中，实现载荷的贝叶斯重构。该方法不仅提供载荷的均值估计，还能量化预测不确定性，适用于模型验证、载荷估计和损伤预测等场景。

## 2. 论文提出的方法论

- **核心思想**：利用高斯过程回归（GPR）作为概率机器学习的核心，将结构动力学方程（谐波振荡器）的线性微分关系融入协方差核的构建中，从而在不显式需要载荷数据的情况下，从位移、速度、加速度的噪声测量中推断载荷。
- **关键技术细节**：
  1. **基核选择**：假设位移响应是零均值高斯过程，协方差核采用平方指数（Squared Exponential, SE）核：  
     $$k_{zz}(t,t'; \sigma_s, \ell) = \sigma_s^2 \exp\left(-\frac{1}{2}\frac{(t-t')^2}{\ell^2}\right)$$
  2. **核的导数关系**：利用线性微分关系自动生成速度、加速度与位移之间的交叉协方差核，例如 $k_{z\dot{z}} = dk_{zz}/dt'$，$k_{\ddot{z}z} = d^2k_{zz}/dt^2$ 等。
  3. **联合模型训练**：将所有可用的测量类型（位移、速度、加速度）及其对应的噪声方差作为超参数，通过最大化对数边际似然进行估计：
     $$\theta_{\text{opt}} = \arg\max_{\theta} \log p(\mathbf{y} | t, \theta)$$
     其中 $\theta = \{\sigma_s, \ell, \sigma_z, \sigma_{\dot{z}}, \sigma_{\ddot{z}}\}$。
  4. **载荷预测**：基于训练好的高斯过程，利用结构方程（式(1)）构造载荷与响应间的联合高斯分布，条件化于观测数据得到载荷的后验均值与协方差：
     $$p(F | \mathbf{y}) = \mathcal{N}(\mu = \mathbf{K}_*^\top \mathbf{K}^{-1} \mathbf{y}, \; \Sigma = K_{**} - \mathbf{K}_*^\top \mathbf{K}^{-1} \mathbf{K}_*)$$
     其中 $\mathbf{K}_* = [k_{Fz}(t_*, t),\, k_{F\dot{z}}(t_*, t),\, k_{F\ddot{z}}(t_*, t)]^\top$，$K_{**} = k_{FF}(t_*, t_*)$，而 $k_{Fi}$ 通过对基核应用微分算子得到。

- **流程说明**：首先从测量数据中仅使用响应信号训练GP模型（绿色模块，图1），然后基于训练好的核参数构建载荷与响应的联合模型（蓝色模块），最终条件化得到载荷的后验分布（红色模块）。

## 3. 实验设计

- **测试对象**：大贝尔特东桥（Great Belt East Bridge）的数值模型，采用线性非定常（LU）气动模型生成结构响应。
- **数据集/场景**：
  - 湍流风场按von Kármán谱生成，平均风速 $U=30\ \mathrm{m/s}$，湍流强度 $I_u=8\%$，$I_w=6\%$，积分尺度 $L_u=L_w=60\ \mathrm{m}$。
  - 结构分析采用22阶模态，时间步长 $\Delta t = 0.05\ \mathrm{s}$。
  - 响应在全局坐标系中加入信噪比 SNR=20 的白噪声以模拟真实传感器噪声。
  - 训练点稀疏选取，间隔 $\Delta t_{\text{train}} = 1.25\ \mathrm{s}$（每1.25秒取一个数据点）。
- **Benchmark 与对比方法**：
  - 真实的模态气动力由LU模型直接计算，作为“真值”。
  - 未与其他现有方法（如Kalman滤波、正则化方法）进行直接对比，仅与真值比较，并采用四个归一化指标（RMS、幅值、相位角、峰值）评估重构质量（图6）。
- **实验数量**：仅有一个主要实验案例（大贝尔特东桥的22阶模态分析），并针对其中几个模态（第一弯曲模态、第三模态）进行了详细展示。没有进行不同噪声水平、不同稀疏采样率、不同桥梁的消融实验或交叉验证。

## 4. 资源与算力

- 论文中未明确说明所使用的计算硬件（GPU型号、数量、训练时长等）。仅提到高斯过程训练的时间复杂度为 $O(N^3)$（$N$ 为数据点数量），并指出稀疏选择可以减少训练时间，但未提供实际运行时间或资源消耗。

## 5. 实验数量与充分性

- **实验数量**：单一桥梁案例、单一风场条件、单一噪声水平（SNR=20）、单一训练点间隔。没有设计对比实验（如不同噪声强度、不同采样策略、不同基核选择）。
- **充分性与公平性**：
  - 作为方法验证，论文的结果展示了重构载荷在时域和频域上的良好吻合，尤其对低频部分拟合很好。但缺乏与经典方法（如Kalman滤波、正则化反卷积）的系统对比，使得方法优势的客观证明不够充分。
  - 仅使用一种基础核（SE核），未探讨其他核（如Matern、周期核）的影响。
  - 未进行消融实验（例如，仅使用位移数据 vs. 使用全部响应数据）。
  - 总体而言，实验设计偏向于“展示”而非“全面验证”，在严谨性和覆盖面上有提升空间。

## 6. 论文的主要结论与发现

- 所提出的物理信息高斯过程方法能够从噪声结构响应中准确重构气动模态力，在大多数模态上，四个归一化指标（RMS、幅值、相位、峰值）均接近1，表明与真值高度一致。
- 重构力的功率谱密度与真值在低频区域几乎重合，高频成分被模型归入不确定性区间，且预测信号的频谱在高于 $1/\Delta t_{\text{train}}=0.8\ \mathrm{Hz}$ 的频段振幅极低，说明训练点间距决定了可恢复的频率上限。
- 对于振幅较小的模态（如第三阶侧向振动模态），噪声对重构质量的负面影响更显著，高频部分出现过拟合噪声的现象。
- 该方法无需正则化参数，自动平衡数据拟合与模型复杂度，并且能处理不规则采样的异构数据。

## 7. 优点

- **概率性质**：提供预测均值与完整的不确定性量化（95%置信区间），有助于评估重构的可靠性。
- **物理约束嵌入**：通过解析导数的协方差核自动满足动力学方程，无需显式求解逆问题，避免了数值不稳定性。
- **数据灵活性**：支持多类型（位移、速度、加速度）和多保真度数据的同时训练，且允许非均匀时间采样。
- **无需正则化**：贝叶斯框架下的边际似然优化自然避免了过拟合，无需用户定义正则化参数。
- **可解释性**：核参数（如长度尺度 $\ell$）具有物理意义，反映了结构响应的平滑特性。

## 8. 不足与局限

- **计算复杂性**：标准GP训练复杂度 $O(N^3)$，即使稀疏采样，对于长序列或高帧率数据仍可能成为瓶颈。论文未尝试稀疏近似方法（如FITC、SoR）来加速。
- **噪声假设**：假定测量噪声为独立同分布高斯白噪声，而实际桥梁监测可能包含有色噪声、异常值或时变噪声，模型缺乏对此的鲁棒性处理。
- **高频重建能力有限**：训练点间距 $1.25\ \mathrm{s}$ 限制了可恢复频率上限为 $0.8\ \mathrm{Hz}$，对于更高频的载荷成分无法准确重构。
- **单案例验证**：仅在一个桥梁模型、一种风场条件下测试，未评估方法在不同结构类型（如高层建筑、风机）、不同湍流强度、不同噪声水平下的泛化能力。
- **缺少对比实验**：未与经典的载荷重构方法（如Kalman滤波、正则化反卷积、深度学习代理模型）进行定量比较，方法在实际中的相对优势缺乏支撑。
- **训练点选择策略**：论文使用固定均匀间隔稀疏采样，未讨论自适应采样或最优采样方案对重构质量的影响。
- **线性假设**：所嵌入的物理模型是线性动力学方程，对于显著非线性或高马赫数气动弹性问题，本方法的适用性存疑。

（完）

---
title: Reducing Experimental Testing in Space Propulsion Film Cooling Analyses by Pixelwise Generative Image Interpolation
title_zh: 通过逐像素生成图像插值减少空间推进薄膜冷却分析中的实验测试
authors: "Adam T. Müller, Philipp J. Teuffel, Konstantin Manassis, Nicolaj C. Stache"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.29911v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 推进剂薄膜冷却分析的机器学习图像回归
tldr: "本文提出一种基于轻量级前馈神经网络与位置编码的机器学习方法，通过稀疏实验测量实现像素级图像回归，用于航天推进薄膜冷却分析。该方法可减少30%的物理测试，在真实与合成数据上达到RMSE<8%和SSIM>93%的高图像相似度，并引入知识引导扩展提升局部适应性。成果显著降低实验成本，支持冷却剂注入器的高效优化，可推广至其他领域。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.29911v1/fig-003.webp\", \"caption\": \"Figure 1: Neural network architecture, where (a) shows the full architecture from input parameters to output and (b) depicts a residual block as implemented in (a).\", \"page\": 3, \"index\": 3, \"width\": 983, \"height\": 189}, {\"url\": \"assets/figures/arxiv/2605.29911v1/fig-004.webp\", \"caption\": \"Figure 2: Schematic diagram of the proposed neural network, illustrating (left) the training phase, where the network learns on sampled pixels from ground truth images, and (right) the inference phase, where the trained model generates images conditioned on input parameters of a new operating point (OP).\", \"page\": 3, \"index\": 4, \"width\": 983, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2605.29911v1/fig-005.webp\", \"caption\": \"Figure 3: Experimental setup for conducting cold flow tests of a film cooling setup with a single coolant injector on a semi-opened combustion chamber. (a) shows the full setup whilst (b) provides a close-up view of the combustion chamber, injector and lighting setup.\", \"page\": 4, \"index\": 5, \"width\": 773, \"height\": 297}, {\"url\": \"assets/figures/arxiv/2605.29911v1/fig-007.webp\", \"caption\": \"Figure 7: PixCOIN regression results on real images showing the variation of the injection angle φ. Line (1.) shows extrapolation, where no operating point with φ of 0 was given as training data. Line (2.) shows extrapolation where one single operating point with φ of 0 was given as training data. All red-framed images are training images, whilst all other images were generated using PixCOIN.\", \"page\": 9, \"index\": 7, \"width\": 983, \"height\": 212}, {\"url\": \"assets/figures/arxiv/2605.29911v1/fig-002.webp\", \"caption\": \"Figure 9: Exemplary comparison of real (1.) and generated (2.) image data for two operating points. Column (3.) shows the difference of the foreground of images in (1.) and (2.) in red, with overlays of (3.) with (1.) shown in (4.) and (3.) and (2.) shown in (5.).\", \"page\": 10, \"index\": 2, \"width\": 713, \"height\": 218}, {\"url\": \"assets/figures/arxiv/2605.29911v1/fig-001.webp\", \"caption\": \"Figure 10: Histogram of pixel values in bins of intensity values of multiples of 10 and their contribution to the RMSEValue of the operating point shown in figure 9(a)\", \"page\": 11, \"index\": 1, \"width\": 693, \"height\": 387}, {\"url\": \"assets/figures/arxiv/2605.29911v1/fig-006.webp\", \"caption\": \"Figure 11: Schematic flowchart of the extended training workflow to enable an expert informed adaptation.\", \"page\": 12, \"index\": 6, \"width\": 983, \"height\": 239}]"
motivation: 减少航天推进薄膜冷却分析中对大量物理实验的依赖，通过机器学习从稀疏测量中生成高质量图像，降低测试成本。
method: 采用轻量级前馈神经网络结合位置编码，以输入参数为条件生成图像，并添加知识引导扩展实现局部适应性。
result: "在真实和合成数据上，图像回归误差RMSE<8%、结构相似度SSIM>93%，测量减少30%时仍保持高精度。"
conclusion: 该方法显著减少实验测试，同时保持数据质量，可高效优化冷却剂注入器配置，应用前景超越航空航天领域。
---

## 摘要
我们提出了一种基于机器学习的图像回归方法，利用稀疏实验测量数据。我们展示了该方法在推进系统开发中的薄膜冷却研究中的应用，旨在减少对大量物理测试的需求。我们的方法采用轻量级前馈神经网络结合位置编码，生成由输入参数条件化的图像。在真实和合成数据上的验证表明，该方法在减少30%测量量的情况下，仍能保持高图像相似度（RMSE < 8%，SSIM > 93%）。我们进一步提出了一种知识引导的扩展，用于生成图像的局部自适应。该方法显著减少了所需测试次数，同时保持了高质量数据，从而能够高效优化冷却剂喷射器配置，其应用范围可超越航空航天领域。

## Abstract
We propose a machine learning approach for image regression from sparse experimental measurements. We show the application of the proposed method on film cooling studies in propulsion system development, aiming to reduce the need for extensive physical testing. Our method employs a lightweight feed-forward neural network with positional encoding to generate images conditioned by input parameters. Validated on real and synthetic data, it achieves high image similarity (RMSE < 8 %, SSIM > 93 %) while maintaining accuracy with a 30 \% reduction of measurements. We further propose a knowledge-informed extension for local adaptability of the generated images. This approach significantly reduces required tests while preserving high-quality data, enabling efficient optimization of coolant injector configurations with applications beyond aerospace.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：在航天推进系统中，薄膜冷却是一种关键的主动热防护技术，但其性能高度依赖于注入器几何、冷却剂性质、流动条件等多因素交互，传统实验验证成本高、周期长。高维参数空间下的实验覆盖度严重不足，限制了设计空间的高效探索。
- **核心问题**：如何通过机器学习方法，利用少量稀疏的物理实验测量，生成像素级的高质量冷却膜分布图像，从而大幅减少对大规模物理测试的依赖。
- **整体含义**：提出一种**条件图像生成回归**框架，将物理参数和空间坐标作为输入，逐像素预测图像强度，实现从稀疏测量到新工况图像的插值。该方法可显著降低实验成本，加速冷却剂喷射构型优化，并可推广至其他基于图像诊断的实验领域。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将图像生成建模为 **像素级回归任务**，输入包括归一化像素坐标 $(x, y)$ 与对应物理参数 $p$，输出为该位置的像素强度值（标量）。网络学习一个连续映射 $f(x, y, p) \to I_{x,y}$，从而在新物理参数下可逐像素合成整个图像。
- **技术细节**：
  - **网络架构**：采用全连接前馈神经网络（FFNN），包含三个全连接层，并插入两个残差块，总参数量约 **350 万**（中等模型）。激活函数为 ReLU，输出层未使用 Sigmoid（通过裁剪保证 $[0,255]$）。
  - **位置编码**：直接将归一化的 $(x, y)$ 坐标作为输入，充当隐式位置编码，使网络学习空间依赖模式，无需卷积层。
  - **训练方式**：不直接输入完整图像，而是将每幅图像拆解为 **像素级样本**（每个样本 = (x, y, p, 像素强度)），在训练时随机采样这批样本，防止过拟合。损失函数为均方误差（MSE）：
    $$ \text{MSE Loss} = \frac{1}{n} \sum_{i=1}^n (y_i - \hat{y}_i)^2 $$
  - **知识引导扩展（IML）**：训练过程中，利用一个轻量级 CNN 分类器检测分割缺陷（如径向流区 RFZ 的漏检），通过 Grad-CAM 定位病灶像素，在这些像素上改用参考值回归损失 $\text{Loss}_{\text{Ref}} = \frac{1}{n} \sum (y_i - r)^2$（$r$ 为预期前景值），其他像素仍用标准 MSE。
- **流程说明**：训练时，从所有工况的预处理图像中随机抽取像素坐标和对应强度，连同该工况的物理参数一起送入网络；推理时，固定物理参数 $p$，遍历所有像素坐标 $(x, y)$，网络输出对应强度，重组成完整图像。

### 3. 实验设计：数据集、基准、对比方法

- **数据集**：
  - **合成数据**：使用参数化函数 $f(x) = u_1 e^{-(u_2 (x-u_3))^2} \cos(u_4 x) + u_5$ 生成三类图像：
    1. 二进制函数曲线图；
    2. 填充积分表示；
    3. 灰度梯度积分版。
    图像尺寸 50×50，参数域 $u_1 \in [1.5,8.5], u_2 \in [1.7,3.1], u_3 \in [1.0,1.3]$，添加 $\pm5\%$ 噪声模拟元数据误差。
  - **真实数据**：来自 DLR 的冷流薄膜冷却实验，使用去离子水+墨水代替高浓度过氧化氢（HTP），单注入器构型。共 **480 个工况点**（5 个参数：制造方法、燃烧室直径、喷嘴直径、注入角、压力），每个工况点采集 10 张图像，总计 4800 张灰度图（1024×1024），经预处理后下采样至 **120×84**。参数取值见表 2。
  - **灰度验证子集**：少量灰度数据用于测试灰度连续性输出。
- **基准与对比**：
  - 未提供传统方法（如响应曲面、几何模型）的直接对比，而是通过 **消融实验** 评估模型：
    - 不同模型规模（小 ~92.5 万、中 ~350 万、大 ~670 万参数）。
    - 数据集缩减策略：减少流速参数设置（5档→3档或2档）、仅减少注入角设置、所有参数最大3档、随机去掉30%工况点。
    - 插值与外推对比（如 $u_3$ 参数、注入角 $0^\circ$ 的外推）。
  - **评估指标**：RMSE、SSIM、PSNR、余弦相似度、感知哈希（Hamming距离）。所有指标均使用开源库计算。
- **对比方法**：未与现有生成模型（如GAN、扩散模型）进行直接定量比较，论文指出 PixCOIN 更轻量、可解释，且适用于小样本场景。

### 4. 资源与算力

- **GPU 型号**：NVIDIA RTX 3500 Ada Generation Laptop GPU。
- **训练时长**（50 个 epoch，batch size=64，学习率 0.0001，Adam 优化器）：
  - 中等模型（~350 万参数）：**每 epoch 7:02 分钟**。
  - 大模型（~670 万参数）：**每 epoch 8:54 分钟**。
  - 随机减少 30% 工况点后，训练时间降至 **4:52 分钟/epoch**。
- **推理时间**：中等模型每张图像 **5.98 秒**，大模型 **8.77 秒**（10 次平均）。
- **未明确指出**：使用的 GPU 数量（推测单卡），训练总耗时（50 epochs × 7 min ≈ 5.9 小时），是否采用混合精度、数据并行等优化未提及。

### 5. 实验数量与充分性

- **实验数量**：
  - 合成数据：3 种类型 × 多参数消融（外推、插值对比，表 3）。
  - 真实数据：基础训练（全数据集）；流速缩减实验（3档、2档）；注入角缩减（3档）；所有参数最大3档（162个工况点）；随机30%缩减；灰度数据验证。
  - 知识引导扩展：定性展示两个工况点的修复效果。
  - 模型规模对比（小/中/大）以及输入归一化、样本量减少的效果。
- **充分性评价**：
  - **优点**：涵盖合成和真实场景，多种缩减策略系统测试了稀疏性对性能的影响；定量指标全面（5 个），并附带误差组成分析（图 10）和定性对比；专门分析了插值与外推的差异。
  - **不足**：
    - 未与传统图像插值方法（如样条、克里金）或最近邻/线性插值进行对比，缺少**基线基准**。
    - 未与 SOTA 生成模型（如 PAE、VQ-VAE、扩散模型）对比，仅引用了 Hwang et al. 的像素回归作为灵感，但无直接性能比较。
    - 实验主要集中在**二进制/灰度单通道**，未验证 RGB 彩色图像。
    - 知识引导扩展仅展示两例定性结果，缺少量化指标。
  - **客观性**：评估指标计算采用标准库（scikit-image 等），参数设置明确；但测试集合与训练集合的划分（70:20:10）虽提及，但未提供具体随机种子或交叉验证，泛化误差估计可能不够稳健。

### 6. 论文的主要结论与发现

- **核心发现**：PixCOIN 能在训练数据减少 **30%~40%**（如流速 3 档）时，仍保持 RMSE < 8%、SSIM > 93%；即使所有参数最多 3 档（减少 66.25% 工况点），RMSE 仍 < 12%、SSIM > 89%。
- **插值优于外推**：外推工况（如注入角 0° 未在训练中出现）质量显著降低，引入一个邻近训练点可部分改善。
- **误差主要来源**：实时性误差集中在前景/背景边界和动态区域（如液滴区），且多为单像素级偏移，对整体 RMSE 贡献主要由少数大误差像素主导。
- **知识引导扩展有效**：通过 Grad-CAM 定位分割缺陷区域并修改损失函数，可修复径向流区（RFZ）的虚假背景，提升视觉连贯性。
- **灰度连续性可行**：模型可生成平滑灰度梯度，反映膜厚度变化。

### 7. 优点

- **轻量化与高效**：中等模型仅 350 万参数，在笔记本 GPU 上可快速训练和推理，适合实际工程部署。
- **像素级无假设**：不依赖空间连续性或几何先验，可捕捉湍流、液滴等非线性特征，比传统响应面法更灵活。
- **可解释性与知识融合**：提出 IML 扩展，通过 Grad-CAM 定位缺陷区域，允许领域专家直接引导网络局部修正，提升模型可信度。
- **数据利用率高**：在像素级上训练，无需完整图像对，天然支持样本随机采样，对部分遮挡或缺失数据有鲁棒性。
- **全面消融与定量分析**：系统评估了数据集缩减策略、模型规模、外推 vs 插值，并深入分解 RMSE 来源。

### 8. 不足与局限

- **实验基线缺失**：未与传统插值（如双线性、样条）或经典机器学习回归（高斯过程、K 近邻）进行比较，无法确认其相对优势。
- **未与 SOTA 生成模型对比**：尽管作者指出 PixCOIN 更轻量，但未提供与 GAN、扩散模型在相似数据上的定量性能对比。
- **单模态数据**：仅验证了单通道灰度/二值图像，实际应用中可能需要 RGB 或多光谱图像。
- **外部有效性**：实验基于简单冷流模拟（水+墨水代替 HTP），流变性质差异较大，且仅考虑单喷射器。结果向真实推进剂、多喷射器、全尺寸燃烧室的推广性待验证。
- **系统偏差未消除**：地面真值数据中包含了持续性液滴、制造缺陷等非目标特征，模型学会了重现这些偏差（图 8），可能影响可迁移性。
- **分割质量限制**：预处理分割依赖传统计算机视觉方法，RFZ 区域易漏检，导致训练标签不准确，进而影响灰度输出质量（尽管 IML 扩展可部分缓解）。
- **资源描述不充分**：未提供完整训练总时长、GPU 数量、是否使用混合精度等细节，复现性受限。
- **知识引导扩展的局限性**：Grad-CAM 二值化阈值选择主观，且仅能在训练后期启用（第 5 epoch 后），未与自动阈值方法对比。

（完）

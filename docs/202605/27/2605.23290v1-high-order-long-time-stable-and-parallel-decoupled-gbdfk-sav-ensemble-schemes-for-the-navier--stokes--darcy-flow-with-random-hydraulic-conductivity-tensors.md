---
title: "High-order, long-time stable and parallel decoupled GBDF$k$ SAV ensemble schemes for the Navier--Stokes--Darcy flow with random hydraulic conductivity tensors"
title_zh: 针对具有随机水力传导率张量的纳维-斯托克斯-达西流的高阶、长时间稳定且并行解耦的GBDF$k$ SAV集合方案
authors: "Wei-Wei Han, Fukeng Huang, Changxin Qiu"
date: 2026-05-22
pdf: "https://arxiv.org/pdf/2605.23290v1"
tags: ["query:flow-field"]
score: 9.0
evidence: 纳维-斯托克斯-达西流动的数值格式
tldr: 针对含随机水力传导率张量的非稳态Navier-Stokes-Darcy系统，提出高阶GSAV-GBDFk-Ensemble方案，融合分区解耦、广义标量辅助变量和广义BDFk方法，实现高阶时间精度、长时间稳定性和高效并行计算，并建立最优误差估计。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.23290v1/fig-005.webp\", \"caption\": \"Table 1: Convergence results of GSAV-GBDF2-Ensemble scheme, ∆t = h\", \"page\": 20, \"index\": 5, \"width\": 527, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2605.23290v1/fig-004.webp\", \"caption\": \"Table 2: Convergence results of GSAV-GBDF4-Ensemble scheme, ∆t = h\", \"page\": 21, \"index\": 4, \"width\": 527, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2605.23290v1/fig-003.webp\", \"caption\": \"Table 3: Computational Efficiency and Speed-up Analysis (h = 1/32)\", \"page\": 21, \"index\": 3, \"width\": 830, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2605.23290v1/fig-001.webp\", \"caption\": \"Figure 2: Mean velocity from GSAV-GBDF3-Ensemble algorithm for Q1 = −1, Q2 = −1, and different Q0: Q0 = 2 (left), Q0 = 1 (middle) , and Q0 = 3 (right).\", \"page\": 22, \"index\": 1, \"width\": 854, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2605.23290v1/fig-002.webp\", \"caption\": \"Figure 3: Mean velocity from tradition algorithm for Q1 = −1, Q2 = −1, and different Q0: Q0 = 2 (left), Q0 = 1 (middle) , and Q0 = 3 (right).\", \"page\": 22, \"index\": 2, \"width\": 854, \"height\": 252}]"
motivation: 现有方案在处理不确定参数和非稳态耦合系统时效率低或稳定性受限，需开发长时间稳定且可并行的高阶方法。
method: 将分区解耦策略与广义标量辅助变量（GSAV）及广义BDFk离散化结合，通过共享单一系数矩阵实现高效集合计算。
result: 数值解满足一致时间界限且无需时间步长限制，误差分析证明最优阶收敛性，数值实验验证精度、稳定性和效率。
conclusion: 提出的GSAV-GBDFk-Ensemble方案兼具高阶精度、长时间稳定和计算高效性，适合处理随机参数耦合系统。
---

## 摘要
我们针对具有不确定初始条件、强迫项、水力传导率张量以及Lions-Beavers-Joseph-Saffman界面条件的非定常纳维-斯托克斯-达西系统，开发并分析了一类高阶集合方案。所提出的方案称为GSAV-GBDF$k$集合方案，其整合了分区解耦策略、广义标量辅助变量方法和广义BDF$k$离散。该框架实现了高阶时间精度和长时间稳定性，允许对非线性项进行显式处理，并且通过在每个时间步共享单一统一系数矩阵，促进了针对多个参数实现的高效集合实现。此外，数值解被证明满足时间一致有界性，无需时间步限制。由于采用集合形式，生成的线性系统共享公共系数矩阵，从而显著提高了计算效率。我们进一步为所提出的高阶方案建立了最优阶误差估计。数值结果验证了理论分析，并说明了所提方法的准确性、稳定性和效率。

## Abstract
We develop and analyze high-order ensemble schemes for the unsteady Navier--Stokes--Darcy system with uncertain initial conditions, forcing terms, hydraulic conductivity tensors, and Lions-Beavers-Joseph-Saffman interface conditions. The proposed schemes which are called GSAV-GBDF$k$-Ensemble schemes integrate a partitioned decoupling strategy, the generalized scalar auxiliary variable (GSAV) approach, and generalized BDF$k$ discretizations. This framework achieves high-order temporal accuracy and long-time stability, permits explicit treatment of the nonlinear term, and facilitates an efficient ensemble implementation for multiple parameter realizations by sharing a single, unified coefficient matrix at each time step. Moreover, the numerical solutions are shown to satisfy uniform-in-time bounds without time-step restrictions. Owing to the ensemble formulation, the resulting linear systems share common coefficient matrices, which significantly improves computational efficiency. We further establish optimal-order error estimates for the proposed high-order schemes. Numerical results are included to confirm the theoretical analysis and to illustrate the accuracy, stability, and efficiency of the proposed methods.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究问题**：针对具有随机初始条件、强迫项、水力传导率张量以及 Lions–Beavers–Joseph–Saffman 界面条件的非定常 Navier–Stokes–Darcy 耦合系统，如何设计高效、高阶时间精度、长时间稳定的数值格式，能够同时处理多个参数实现（ensemble）的不确定性量化问题。
- **背景与动机**：
  - Navier–Stokes–Darcy 系统是连接自由流和多孔介质流的基础模型，广泛应用于地下水、油藏、地表–地下水耦合等问题。
  - 实际应用中，水力传导率等参数往往具有随机性，需要大量采样（Monte Carlo、随机配置等）进行不确定性量化，但每个样本均需求解一个强耦合、非线性系统，计算代价极高。
  - 已有方法（如降阶模型）虽能降低单次成本，但引入额外误差；传统 implicit 处理非线性项导致非线性求解，explicit 处理则受限于稳定性条件；分区解耦方案可能引入时间步长限制。
  - **核心挑战**：在保证高阶精度和长时间稳定性的前提下，实现各样本间共享系数矩阵（避免重复矩阵分解），并显式处理非线性项而不牺牲稳定性。

### 2. 论文提出的方法论（核心思想、关键技术细节、算法流程）

- **核心思想**：将分区解耦策略、广义标量辅助变量（GSAV）方法和广义 BDF$k$（GBDF$k$）时间离散三者结合，构建 GSAV-GBDF$k$-Ensemble 方案。
- **关键技术细节**：
  - **分区解耦**：通过将界面项在显式部分处理，使自由流子问题（9）和多孔介质子问题（10）在每个时间步内独立求解，无需联立大系统。
  - **GSAV 方法**：引入辅助变量 $r_j(t) = E_j(\mathbf{u}_j,\phi_j) + C_{r,j}$（$E_j$ 为动能加势能），并构造辅助变量方程（8），使得原本需要隐式处理的非线性项（如对流项）可以显式计算，同时通过参数 $\alpha_j,\gamma_j$ 保证长时稳定性。
  - **广义 BDF$k$ 离散**：采用 $\beta \ge 2$ 的 GBDF$k$（$k=2,3,4$）公式（13–15）离散时间导数，相比经典 BDF$k$ 具有更大稳定区域，允许更大时间步长。
  - **集合共享系数矩阵**：通过在动量方程（9）和达西方程（10）中将所有样本的公共部分（如 $a(\cdot,\cdot)$ 中的 $\nu$ 项、界面项中的 $\eta_i$ 平均值）集中到隐含部分，而将差异部分（如 $(\eta_{i,j}-\eta_i)$ 项、$(K_j-\overline{K})$ 项）显式处理，使得所有样本在每个时间步只需求解系数矩阵相同的线性系统，大幅提高效率。
  - **重构步骤**：修正变量 $\mathbf{u}_j^{n+1}=\eta_{k,j}^{n+1}\tilde{\mathbf{u}}_j^{n+1}$，$\phi_j^{n+1}=\eta_{k,j}^{n+1}\tilde{\phi}_j^{n+1}$，其中 $\eta_{k,j}^{n+1}=1-(1-\xi_j^{n+1})^{k+1}$，$\xi_j^{n+1}$ 由辅助变量更新后计算，以保证离散能量稳定。
- **算法流程（文字说明）**：
  1. 给定初始条件 $\mathbf{u}_j^0,\phi_j^0,r_j^0$。
  2. 对每个时间层 $n$：
     - 使用当前层和前 $k-1$ 层已知量，显式计算对流项 $d(C_{\beta k}(\mathbf{u}_j^n),C_{\beta k}(\mathbf{u}_j^n),\mathbf{v})$ 及差异界面项。
     - 求解解耦的两个线性问题（9）、（10），得到 $\tilde{\mathbf{u}}_j^{n+1},\tilde{\phi}_j^{n+1},p_j^{n+1}$。
     - 用 GSAV 更新公式（11）计算 $r_j^{n+1}$，进而得 $\xi_j^{n+1}$。
     - 计算 $\eta_{k,j}^{n+1}$，修正得到 $\mathbf{u}_j^{n+1},\phi_j^{n+1}$。

### 3. 实验设计（数据集 / 场景、基准、对比方法）

- **场景一：精度测试（矩形域）**
  - 域：$\Omega=[0,1]\times[-1,1]$，$\Omega_f=[0,1]\times[0,1]$，$\Omega_p=[0,1]\times[-1,0]$。
  - 采用人为构造的解析解（见论文 5.1 节），源项和边界条件据此设定。
  - 网格：$h=1/8,1/16,1/24,1/32$，时间步长 $\Delta t = h$。
  - 样本数 $J=3$，水力传导率 $k_j=[1.1,2.1,3.3]$。
- **场景二：效率测试与 Y 形域地下水流模拟**
  - 域：单位正方形内定义的多边形自由流区域（见图 1），代表两管道汇合。
  - 物理参数：$\alpha_{BJ}=\nu=g=S=1$，$K_j=k_jI$。
  - 随机水力传导率：$k_j(x,\omega)=(1+\omega)\times10^{-2}$，$\omega\sim U(0,1)$，$J=100$。
  - 边界条件：给定入口/出口通量 $Q_0,Q_1,Q_2$，三种情形（平衡、净入流、净出流）。
  - 网格 $h=1/32$，$\Delta t = h$，使用 GSAV-GBDF3 方案（$\beta=3$）。
- **对比方法**：
  - GSAV-Individual：每个样本独立使用 BDF2 方法求解（无共享系数矩阵）。
  - Traditional：传统全隐式求解器（可能为 Newton-type 迭代）。
- **基准**：以解析解或传统方法结果为参考，比较误差、收敛速度和 CPU 时间。

### 4. 资源与算力

- **论文未明确说明使用的 GPU 型号、数量或训练时长**。
- 仅给出了在 CPU 上的运行时间数据（表 3），针对不同 $J$（1, 100, 500, 1000），记录以秒为单位的 CPU 时间，用于比较 GSAV-Ensemble、GSAV-Individual 和 Traditional 方法。
- 所有实验均在标准台式机或服务器上完成，但具体硬件信息缺失。

### 5. 实验数量与充分性

- **实验组数**：
  - 精度测试：两组表（表 1 第二阶、表 2 第四阶），每个表中包含 3 个样本（$j=1,2,3$）在 4 种网格下的误差和收敛阶，总共 $2\times3\times4=24$ 组误差数据。
  - 效率测试：表 3 包含 $J=1,100,500,1000$ 三种方法 CPU 时间及增益百分比，共 12 个时间数据。
  - Y 形域模拟：给出了三个流量情景下的平均速度轮廓（图 2），并与传统方法（图 3）对比。
- **充分性评价**：
  - **优点**：覆盖了收敛阶验证、效率对比、实际情景模拟；理论证明与数值实验吻合；对比了多种基准（个体、传统）。
  - **不足**：未进行消融实验（如不同 $\beta$ 值、不同阶数 $k$ 对稳定性或精度的影响）；未在三维或更高维度上验证；随机场模型较为简单（均匀分布且样本仅 100）；缺少对初始步骤高精度假设的数值检验；未讨论非线性项显式处理能否在大雷诺数下保持稳定。
  - **客观性**：实验设计基本合理，误差分析基于标准做法，效率对比采用了固定网格和时间步长，避免了不公平的参数调整。

### 6. 论文的主要结论与发现

- 提出的 GSAV-GBDF$k$-Ensemble 方案实现了：
  - **高阶时间精度**：数值实验验证了第二阶和第四阶收敛速率与理论一致。
  - **长时间稳定性**：定理 3.1 证明了解的一致有界性，无需时间步长限制。
  - **计算高效性**：通过共享公共系数矩阵，相比个体方法节省 33%（$J=1000$），相比传统方法节省 90%–95% CPU 时间。
  - **最优误差估计**：定理 4.1 在条件 $\eta'_{\max}/\eta_{\min}<2\tau_{\beta k}/3$ 及 $\rho'_{\max}/k_{\min}<\tau_{\beta k}/3$ 下给出了 $\|\mathbf{e}_j^{n+1}\|+\|\phi_j^{n+1}\| \le C(\Delta t)^k$ 的估计。
- 数值实验证明了方法的准确、稳定和高效，适合处理含随机参数的大型耦合系统。

### 7. 优点（方法或实验设计的亮点）

- **方法创新性**：
  - 将分区解耦、GSAV 和广义 BDF$k$ 首度结合，同时解决了强耦合、非线性显式处理、长时间稳定性、集合共享系数四个问题。
  - 理论分析（长时稳定性和最优误差估计）在该类非线性 Navier–Stokes–Darcy 集合方案中属于首次。
  - 算法结构清晰，易于并行实现。
- **实验设计亮点**：
  - 同时验证了二阶和四阶收敛，体现了方法的高阶灵活性。
  - 效率实验中采用了递增的 $J$，展示了集合方法的可扩展性。
  - Y 形域模拟选择了三种具有物理意义的流量场景（平衡、净入流、净出流），直观验证了方法的鲁棒性。

### 8. 不足与局限

- **实验覆盖**：仅在二维简单几何上测试，未涉及三维或实际复杂地形；随机场模型较理想化（均匀分布、少量样本），对高维随机空间或非光滑相关长度情况的适用性未验证。
- **初始步骤假设**：理论误差分析假定前 $k-1$ 步足够精确，实际中需要特殊初始化程序，但文中未给出具体实现或对初始化误差的敏感性分析。
- **参数选择**：GSAV 中的 $\alpha_j,\gamma_j$ 需手动选取以满足长时稳定条件，文中仅给出了存在性证明，未提供通用自适应策略。
- **效率对比的局限性**：对比的传统方法未指明具体实现（如是否使用最优预处理），可能存在不公平；GPU 并行加速未涉及。
- **稳定性条件**：误差分析要求 $\eta'_{\max}/\eta_{\min}$ 和 $\rho'_{\max}/k_{\min}$ 满足上界，该条件在实际问题中是否总是满足需要进一步讨论，尤其当水力传导率空间变异性强时。
- **可重复性**：未公开代码或数据集，论文中部分常数 $C_0$ 的取值依赖于对时间步长的约束 $\Delta t \le 1/C_0^{k+1}$，该约束可能过紧或难以在事前估计。

（完）
